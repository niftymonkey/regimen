# Regimen Four-Harness Evaluation Readiness

## CLOSED (2026-07-03): this driver is DONE and inactive

> DoD D (per-OS install acceptance) CLOSED BY DECISION 2026-07-03: the per-OS re-verification runs are dropped; OS-specific gaps that surface in real use become follow-up bug tickets instead. With A, B, C, E, and F already done, that closes the last open item, so this driver document is DONE and inactive. The active driver is now `plans/finish-line.md`. Everything below is the historical record.

> The driver for the next push. Two goals: (1) make Regimen usable across all four agent CLIs the company is evaluating, not just Codex, and (2) use Regimen's own Feedback layer to ANSWER the company's evaluation questions from recorded sessions across all four. This document, not the board, decides what gets worked on next. Successor to `plans/codex-trial-readiness.md`, which is now the historical record: the Codex judgment-layer spine it delivered is the proven template this push generalizes.

## Readiness: Definition of Done (the bar for "I can, and my Windows teammates can, just use it")

This is the contract for when this whole document is DONE. DONE = on macOS (my trial Mac) AND on native Windows (teammates' machines), a person can install all three pillars (Feedback, Guidance, Enforcement) for all four harnesses (Claude, Codex, Copilot, Gemini) and have capture fire, the judge read, skills install, and gates enforce in normal day-to-day use. The document is DONE when every box below is checked. Scope set 2026-06-21: target OSes macOS + native Windows (Linux/WSL2 is the dev foundation, already proven, not a target surface); all three pillars; all four harnesses. Status is producer/code-verified (audit 2026-06-21 with file:line evidence), not doc-prose-trust.

### A. Feedback: capture, store, judge (all four harnesses)
- [x] Claude, Codex, Copilot: capture-install + live-capture translator + judge/read path, all DONE in production (audit-verified; all four translators registered in `loader/translators/index.ts`; readers+resolvers in `harness/support.ts`).
- [x] Gemini live-capture translator + judge/read path: DONE in production (resolves the real `~/.gemini/tmp`).
- [x] Gemini capture-install planner: DONE (commit `2bf06467a9`, merged to `main`). Writes PROJECT-LEVEL `<cwd>/.gemini/settings.json` with `name`+`matcher` per group, driven by a descriptor `groupDecoration` (no literal harness branch in generic code); `install-gemini.test.ts` rewritten to the firing shape. FIRING-PROVEN end-to-end on Linux via the REAL installer (2026-06-21): install -> real headless `gemini -p` -> 7 envelopes captured to an isolated buffer (`REGIMEN_DATA_DIR` proven to pass through gemini 0.47.0's hook env, so no wrapper needed) -> loader/`translateGemini` -> 5 events, 0 quarantined. [feedback]

### B. Enforcement: gate hooks (all four harnesses)
- [x] Claude, Codex gate-install: DONE (nested-matcher-groups format).
- [x] Copilot gate-install: DONE (commit `4569c2f911`, merged). `gate-hooks.ts` `planVersionedGateHooks` emits `versioned-command-leaves` (flat leaves, `role:gate`, `REGIMEN_HARNESS` baked); round-trip removal covered. Artifact-verified the real CLI writes `$COPILOT_HOME/hooks/hooks.json` in the firing shape. [enforcement]
- [x] Gemini gate-install: DONE (commit `4569c2f911`, merged). Project-level `<cwd>/.gemini/settings.json`, `name`+`matcher` group on `BeforeTool`, `role:gate`. Mirrors the firing shape ADR-0011 proved. [enforcement]

### C. Guidance: skills (all four harnesses)
- [x] Skill install DONE for all four (skillsSubdir per contract; install tests cover each). Re-confirm during the per-OS install acceptance below.

### D. Per-OS install acceptance (the real end-to-end gate, both OSes must pass)
- [ ] macOS (trial Mac, available ~Mon 2026-06-23): locator/installer re-validation + three-pillar install + a real capture-to-judge run per harness. [validation]
- [~] native Windows (the work VDI, NON-ADMIN PowerShell, Bedrock GovCloud): CAPTURE PROVEN end-to-end for ALL FOUR harnesses 2026-06-22 (`regimen list` = 7 real sessions; see the "NATIVE-WINDOWS DoD-D CONTEXT" block in Start-here for the full fix chain + install procedure). The three wrinkles below are now RESOLVED: `bun <path>` DOES fire as a hook under each harness (after the capture-command forward-slash fix); the `schtasks` daemon does NOT work for non-admins ("Access is denied") so it is skipped via the new `--no-daemon` flag (drain the buffer by hand for now), with a NO-ADMIN HKCU-`Run`-key daemon as the tracked follow-up; path/config-home resolution works. REMAINING for full Windows acceptance: (1) BUILT + MERGED 2026-06-22 (`origin/main` = `3f9b54f39a`, merge `3f9b54f39a` / feat `a4bbca660d`, CodeRabbit 0 findings on the exact commit): the no-separate-key Claude Code CLI judge adapter (`packages/feedback/src/judged/claude-cli-adapter.ts`). `regimen assess` now auto-selects: `ANTHROPIC_API_KEY` present -> the HTTP adapter (unchanged); key ABSENT but `claude` on PATH -> a NEW adapter that shells `claude --print --output-format json --max-turns 1 --system-prompt <rubric>` with the transcript PIPED TO STDIN, reusing Claude Code's own auth (Bedrock/OAuth/Vertex) with NO key; `--judge-via cli|api` forces a backend (CLI path bypasses the PATH check = the VDI escape hatch). STILL TO VERIFY ON THE VDI: run `regimen assess --session <id>` with no key set and confirm it judges via local Bedrock Claude Code (CANNOT be tested from the Linux dev box). And (2) the no-admin daemon so draining is not manual. [validation] Windows-specific wrinkles to verify (do not assume the Linux/macOS shape carries over):
  - the capture/gate hook COMMAND: every harness's hook invokes `bun <path>` via the standard `captureCommand` (the Gemini wrapper script in `e2e-gate.sh` is an ISOLATION-ONLY test artifact, production Gemini uses the same plain `bun <path>`, no wrapper, because production uses the default data dir). Confirm `bun <path>` actually fires as a hook command under each harness on Windows, or whether a PowerShell / cross-platform command leaf is needed. This is cross-cutting (all four harnesses), not Gemini-specific.
  - the loader daemon as a Windows scheduled task (`schtasks`) actually runs and drains the buffer.
  - path/locator resolution (separators, `COPILOT_HOME`/`GEMINI_CONFIG_DIR` defaults) on Windows.

### E. The answering pass (the headline payoff)
- [x] REDESIGNED + DELIVERED (2026-06-21, user-directed). The `answer` synthesis command was DROPPED: it failed the pre-deletion test (it concentrated no complexity the in-conversation LLM cannot compose). Instead the answering is COMPOSITIONAL: the agent SELECTS sessions, calls the per-session primitives, and synthesizes prose live in the conversation. Per-session judgments persist (`assess`); multi-session answers are composed on the fly and NEVER stored. The three composable primitives: `evidence` (quantitative, free, deterministic), `assess` (qualitative judge, per session), and the NEW `list` selection primitive (commit `06160ac243`, merged): `listSessions(db,{harness,model,since,until,outcome})` + `feedback list [...] [--json]`, spanning judged + unjudged, the inverse of an `answer` command (data only). This answers "how did my gemini sessions go this week", "any problems with copilot", "assess this session" by composition. The corpus enriches as real four-harness usage accrues (today the store is ~181 real claude sessions, judged on demand). The rubric-scoring path is likewise compositional (the agent scores against a supplied rubric in-conversation), not a baked feature.

### F. Unified `regimen` CLI + install/update/remove lifecycle (scope added 2026-06-21, user-directed)
The monorepo merged the repos into `packages/` (ADR-0010) but the `regimen` CLI still LOCATES + SPAWNS the feedback/enforcement CLIs as if they were sibling repos (`packages/cli/src/{locator.ts,runner.ts}` + `realSpawn` + four bins `regimen`/`feedback`/`enforcement`/`start`), a multi-repo fossil. User decision: collapse to ONE user-facing `regimen` CLI dispatching to the packages as LIBRARIES in-process; the hook/gate/daemon ENTRYPOINT SCRIPTS stay standalone (harness-invoked by absolute path, so installed hooks/daemon are unaffected). The single `regimen` CLI owns install/update/uninstall.
- DESIGN DONE (architect-deep, 2026-06-22) and captured in ADR-0012 (`docs/adr/0012-unified-regimen-cli-dispatches-in-process.md`; supersedes ADR-0009's "shape of the move"). The locked decisions + the executable build DAG are in the Start-here block below. These checkboxes are now BUILD (overnight autonomous), not design.
- [x] CLI consolidation [LANDED 2026-06-22: facades A `cf903f8a2d` + B `4ba53c4e63`, dispatcher collapse C `e962f2b3f5`, skill/doc repoint E `fed52e541c`+`f3a225627b`; on main `545c7adc2a`]: one `regimen` CLI dispatching to `@regimen/feedback` + `@regimen/enforcement` libs IN-PROCESS; delete `packages/cli/src/{locator.ts,runner.ts,plan.ts}` + `realSpawn` + the `feedback`/`enforcement` bins; B-lite command surface; repoint the bundled guidance skills + any doc that shells `feedback <x>` to `regimen <x>`. [cli]
- [x] Install lifecycle pass (ON the unified CLI) [LANDED 2026-06-22: D-core `9f46a10baa` manifest module + D-wire `dc9951c387` lifecycle, on main `8915a78b60`]: an install MANIFEST at `<dataDir>/install-manifest.json` (regimenVersion from the CLI package.json = the version stamp; installedAt/updatedAt, clonePath, loaderPath, per-harness entries with pillars + SCOPE); `regimen update` (re-resolve clonePath/loaderPath from the current clone, re-install recorded harnesses, cycle the daemon, restamp; no-manifest fallback = install); `regimen install --all`/`--harnesses` (loop per-harness; gemini per-workspace + one-line notice). [cli]
- [x] README + program-doc + skill sync (LAST) [LANDED 2026-06-22: E `fed52e541c` + `f3a225627b` on main `545c7adc2a`; grep-clean of stale `feedback <x>`/`enforcement <x>` user-commands; feedback 553/0 + cli 41/0 green; CodeRabbit "No findings" on the exact final commit (orchestrator found+fixed 2 minor hyphenated `feedback-evidence` command refs the agent's spaced grep missed, then re-confirmed clean). External `niftymonkey/skills` local clone grepped clean; confirm the remote matches if certainty wanted.]: README.md, install.sh header, ARCHITECTURE.md operational refs, and the bundled `feedback-evidence`/`feedback-judgment` skills repoint off the "shells out" model + `feedback <x>`/`enforcement <x>` user commands to the in-process unified `regimen <x>` surface. Leave the historical ADRs + docs/plan.md as records (ADR-0012 is the new record). [docs]
- VDI UPDATE ANSWER (recorded, do not re-derive): UNINSTALL-first TODAY, because a bare re-install only adds/replaces and never removes. The new uninstall DOES clean old artifacts (the stable identifiers, Windows task `regimen-feedback`, data dir `%APPDATA%\regimen`, `_regimen` hook markers, skill names, are byte-identical old->new), and the data dir survives so capture history is preserved. After the lifecycle pass, in-place `regimen update` is safe. Root cause of the ambiguity: there is NO version stamp and NO update verb today, which is exactly what F builds.

### Known constraints to document (not blockers; captured so no one is surprised)
- Gemini capture is PER-WORKSPACE (project-level install): it must be wired into each workspace where gemini is run; there is no install-once for gemini unless a global gemini-extension path is found (ADR-0011 left that open).
- Gemini judge assumes the default `~/.gemini` home (it ignores `GEMINI_CONFIG_DIR` for transcripts); fine in normal use, breaks only if someone overrides the config dir.
- Gemini free-tier model quota can crash sessions mid-run (limit ~20 per window); affects whether a session is complete enough to judge.
- Codex: freshly-installed hooks are untrusted; interactive Codex needs a one-time manual trust approval or capture silently will not fire (`exec` needs `--dangerously-bypass-hook-trust`). RESOLVED (commit `e0c7c307db`, merged): `feedback wire-hooks` for codex now prints a one-line trust notice at install time (descriptor `firstUseNotice`), so first-run capture is not silently empty.

### Remaining work queue (ordered; everything here is now tracked, nothing is "authorized but unscheduled")
1. DONE (`2bf06467a9`) Gemini capture-install planner, project-level + `name`+`matcher`; firing-proven on Linux.
2. DONE (`4569c2f911`) Enforcement gate-install: Copilot `versioned-command-leaves` + Gemini project-level/`name`+`matcher`.
3. DONE (`e0c7c307db`) Codex first-use hook-trust notice.
4. Windows capture wrinkles: confirm the `bun <path>` hook command fires under each harness on Windows (cross-cutting, NOT Gemini-specific; the bash wrapper in the gate is test-only), and that the `schtasks` loader daemon + path/locator resolution work. Pin the exact gap during the Windows acceptance run; a speculative pass is possible but low-value before a real Windows run reveals it. (Sub-part of D.)
5. DONE-REDESIGNED (`06160ac243`) answering pass: dropped the `answer` command, delivered the `list` selection primitive + the compose-in-conversation model (see E above). No baked synthesis, no stored answers.
6. CLOSING GATES: macOS install acceptance (~06-23) AND native-Windows install acceptance (work VDI). Both must pass: three pillars x four harnesses, real capture-to-judge per harness.

Items 1-3 + 5 DONE + merged + pushed (`origin/main` = `06160ac243`). REMAINING (updated 2026-06-21 as the user expanded scope): (F) the unified-`regimen`-CLI consolidation + the install/update/remove lifecycle pass, BUILDABLE NOW, architect-deep first, this is the NEXT conversation; then (D) the per-OS acceptance gates, hardware-blocked (trial Mac ~06-23 + Windows VDI), execution not building. (`origin/main` = `bb91fb251e`.)

## Start here (read this first, do not re-derive it)

### NEXT CONVERSATION (ACTIVE pointer, set 2026-06-22 night) = VERIFY `regimen assess` (the new no-separate-key judge) on the Bedrock VDI, then the remaining DoD-D work (no-admin daemon + macOS). The judge adapter is BUILT + MERGED. Read this WHOLE block first; it is the full handoff from the Windows-VDI setup session, do not re-derive it.

STATE: `origin/main` = `3f9b54f39a`, clean (only this living driver doc + `plans/monorepo-consolidation-playbook.md` + `.claude/` are uncommitted). DoD F (the unified in-process `regimen` CLI + install lifecycle) is COMPLETE + merged (see the "DoD F COMPLETE" banner below). The active frontier is DoD D, the per-OS acceptance. On native Windows it is now MOSTLY PROVEN (capture fires + lands in the store for ALL FOUR harnesses, details below). The headline GAP was the JUDGE (needed a separate Anthropic key, could not run on Bedrock GovCloud); that adapter is now BUILT + MERGED and the next step is to VERIFY it on the VDI.

#### THE PRIMARY NEW WORK: a Claude Code CLI judge adapter (no separate key) = DONE + MERGED 2026-06-22 night
LANDED: merge `3f9b54f39a` / feat `a4bbca660d` on `origin/main`; built off-thread TDD in an isolation worktree, isolated checks green (feedback 577/0, cli 46/0), CodeRabbit 0 findings on the exact commit, merged --no-ff + pushed autonomously, worktree+branch cleaned up.
WHY (the problem it solves): `regimen assess` (the qualitative LLM judge) previously resolved ONLY `packages/feedback/src/judged/anthropic-adapter.ts`, which reads `ANTHROPIC_API_KEY` and POSTs the Anthropic API (`x-api-key`, optional `ANTHROPIC_BASE_URL`). It had NO AWS Bedrock auth. The work VDI runs Claude via Bedrock GovCloud (captured model `us-gov.anthropic.claude-sonnet-4-5-20250929-v1:0`), has NO direct Anthropic key, and the public API is blocked, so the judge could not run there.
WHAT SHIPPED: a SECOND `JudgeModelPort` impl `packages/feedback/src/judged/claude-cli-adapter.ts` (`claudeCliJudgeModel`) that shells the engineer's already-authenticated Claude Code CLI, reusing WHATEVER auth Claude Code has (Bedrock/OAuth/Vertex/direct key) with NO separate key. VERIFIED argv (real `claude` 2.1.185 on the dev box): `claude --print --output-format json --max-turns 1 --system-prompt <rubric>` with the transcript (the port's `user`) PIPED TO STDIN (not a positional, so a large transcript never hits ARG_MAX / the Windows arg-length cap). It maps `stdout.result` -> `text` and the first key of `stdout.modelUsage` -> `model` (there is no top-level model field; on Bedrock this is the real Bedrock id); `--model` is appended ONLY when `--judge-model` was passed (else Claude Code uses its configured default); `responseSchema` is ignored (no CLI structured-output flag, the Judge validates the verdict out of `text` and retries on parse error). The spawn is injected (`RunClaudeCli`) so it is unit-tested with zero real spawn.
SELECTION (in `resolveDefaultJudgeModel`, anthropic-adapter.ts, callers unchanged): auto-picks by env. Key PRESENT -> HTTP adapter (unchanged). Key ABSENT but `claude` on PATH -> CLI adapter. Key absent AND no `claude` -> throws naming BOTH `ANTHROPIC_API_KEY` and the `claude` CLI. `--judge-via cli|api` forces a backend, threaded `regimen assess --judge-via` -> cli dispatcher -> feedback `assess` facade -> resolver. The `cli` force BYPASSES the PATH check = the VDI escape hatch if `Bun.which` misbehaves on Windows. The PATH check is injected (`claudeOnPath`, default `Bun.which("claude", {PATH: env.PATH})`).
NEXT = VERIFY ON THE VDI (one round-trip, like every Windows fix below): with NO `ANTHROPIC_API_KEY` set, run `regimen assess --session <id>` against a captured session and confirm it judges via the local Bedrock Claude Code and returns prose + signals. If auto-detect fails to find `claude` (e.g. it resolves as `claude.ps1`/`claude.cmd` that `Bun.which`/`Bun.spawn` does not pick up), fall back to `regimen assess --session <id> --judge-via cli`. CANNOT be tested from the Linux dev box (no Bedrock).
CAVEAT: shelling to Claude Code per `assess` is heavier than a raw API call (fine for occasional judging). Generally valuable: ANY Bedrock/Vertex/OAuth user gets the judge for free this way, not just this VDI. TRACKED LATENT Windows risk (untested): whether `Bun.which("claude")`/`Bun.spawn(["claude",...])` resolve the VDI's `claude` shim (the `--judge-via cli` flag still spawns `claude`, so a `.ps1`-only shim could need a different launcher); pin this during the VDI run.

#### THE NATIVE-WINDOWS DoD-D CONTEXT (everything learned 2026-06-22 on the user's work VDI; do not re-derive)
ENVIRONMENT (ground truth, confirmed via a read-only preflight): the VDI is **native Windows, PowerShell 5.1, a NON-ADMIN corporate account, NO WSL**, ExecutionPolicy Unrestricted. Bun 1.3.14, git, node/npm/npx, jq (winget `jqlang.jq`), schtasks all on PATH; all four harness CLIs present (`claude` in `.local\bin`, `codex`, `copilot` winget, `gemini` npm `gemini.ps1`) and all four config homes exist. Claude is Bedrock GovCloud. Helpers committed THIS session: `check-windows-env.ps1` (a read-only env preflight), `install.ps1` (PowerShell twin of install.sh), `docs/install-windows.md` (runbook, also at md.niftymonkey.dev `/v/kMCykcTF`).
INSTALL PROCEDURE that works for a non-admin Windows user (capture-only): run `install.ps1` from the clone. Three config-home harnesses at once (REPEAT `--harnesses` per harness, space-separating silently installs only the FIRST): `.\install.ps1 --harnesses claude --harnesses codex --harnesses copilot --gate rm-rf --no-daemon`. Gemini per-workspace with the BARE `regimen` (NOT install.ps1, which cd's back to the clone): `cd <gemini workspace>; regimen install --harnesses gemini --gate rm-rf --no-daemon`. The daemon is SKIPPED (`--no-daemon`) because a non-admin CANNOT create a scheduled task (`schtasks /Create` = "Access is denied", confirmed even for a trivial task). Drain the buffer MANUALLY: `bun packages/feedback/src/loader/run.ts` (Ctrl+C after it drains) -> `regimen list`.
PROVEN: capture fires + lands in the SQLite store for ALL FOUR harnesses on the non-admin Windows VDI (`regimen list` showed 7 sessions: claude Bedrock 31 events, codex gpt-5.5 up to 29, copilot x2, gemini 2 events). Gemini's CLI auth is BROKEN on the VDI (`IneligibleTierError`, deprecated free tier) yet its hooks still fired before the crash, so the capture MECHANISM works regardless of gemini's runnability.
THE WINDOWS FIX CHAIN (all TDD + CodeRabbit-clean + merged this session; the acceptance run found each, unit tests never could; commits on `origin/main`):
1. Enforcement gates are POSIX-only (`REGIMEN_HARNESS=x bun/bash "<path>"` env-prefix + `assertSafeClonePath` rejects the Windows `\`) -> SKIPPED on win32 with a notice (capture unaffected). [enforcement facade]
2. schtasks `/Create /XML` needs UTF-16 LE+BOM (we wrote UTF-8 -> "unable to switch the encoding") -> write UTF-16LE+BOM + an `encoding="UTF-16"` prolog (Linux/macOS stay UTF-8). [windows.ts + install/index.ts `serviceFileEncoding`]
3. The task `<Arguments>` had unescaped `&&`/`>` -> XML-escape `&`/`<`/`>`. [windows.ts `xmlEscape`]
4. `set REGIMEN_DATA_DIR=val &&` left a trailing space in the cmd value -> `set "VAR=val"`. [windows.ts]
5. NEW `regimen install --no-daemon` flag (threads cli->feedback, skips the daemon step + prints a drain note) because non-admins cannot create the schtasks daemon. [cli + feedback]
6. THE capture-firing fix: `captureCommand` built `bun <join-path>` with Windows backslashes; the harness fires the hook through a POSIX shell that STRIPS `\` (path became `C:Users...` -> bun "Module not found") -> forward-slash the path (`.replaceAll("\\","/")`; bun accepts `/` on Windows, no-op on POSIX). [capture-hooks.ts]
KEY ARCHITECTURE FACTS for the next session: capture command is `bun <forward-slash-path>` (NO env-prefix, unquoted); gate commands are `REGIMEN_HARNESS=x bun/bash "<path>"` (POSIX-only, skipped on win32); the daemon is a `schtasks` LOGON task at `%APPDATA%\regimen\regimen-feedback.task.xml` (blocked for non-admins); data dir is `%APPDATA%\regimen` (capture + the manual loader both use the default, so they stay consistent).

#### THE OTHER REMAINING DoD-D WORK (after / alongside the judge adapter)
- **NO-ADMIN Windows daemon** (the headline UX gap, the user drains by hand until this lands): replace the schtasks daemon (non-admins cannot use it) with a per-user **HKCU `Run` key** that launches the loader hidden on logon (no admin, no Task Scheduler). Adapt start/stop/status (the pid-file liveness check still works) + a hidden launcher. Untestable from the dev box; build it informed by the now-proven capture, verify on the VDI.
- **macOS trial-Mac acceptance** (the other half of DoD D, hardware ~06-23): locator/installer re-validation + three-pillar install + capture-to-judge per harness.
- TRACKED LATENT Windows issues (not live today): (a) the enforcement gate-command builders need the SAME forward-slash treatment as capture when Windows gates are built; (b) spaced-path quoting in the capture/gate commands (user's path has no spaces); (c) `regimen update` does NOT yet honor `--no-daemon` (passes daemon:true) so update would fail the daemon step on a non-admin box; (d) copilot + gemini sessions show model `-` (their hook payloads may not carry the model, or the translators do not extract it, a data-completeness gap, not a capture failure); (e) gemini CLI auth is broken on the VDI (Google deprecation, the user's problem, but flag for the eval: is gemini CLI even usable there?).

DISCIPLINE (unchanged): off-thread TDD in isolation worktrees off `origin/main`; the `tdd` skill (stub-before-red, minimal-green, refactor); isolated per-package `bun run check` (NEVER the workspace `--filter '*'`); CodeRabbit (`coderabbit:code-review`) on the EXACT final commits, re-run after any fix, triage against the code (recent findings trend false-positive); single-line conventional commits `--no-verify`; NO em dash (U+2014); `/stay-within-limits`. LANDING POSTURE: each Windows fix this session was built off-thread, CodeRabbit-clean, merged+pushed, then the user re-ran on the VDI; the judge-adapter build lands the same way, then the user verifies `regimen assess` on the VDI. CodeRabbit free-tier rate-limits ESCALATE under repeated reviews, budget ONE clean review per unit. This driver doc stays a LIVING uncommitted file.

### DoD F COMPLETE 2026-06-22 (overnight autonomous run). All six units landed CodeRabbit-clean on `origin/main` = `545c7adc2a`; integrated checks green (cli 41/0, feedback 553/0, enforcement 78/0+1skip). The ONLY remaining DoD item is D (per-OS acceptance gates), hardware-gated on the trial Mac (~06-23) + Windows VDI = a turnkey RUN, not a build. The block below is now the HISTORICAL runbook (it was executed). New active pointer: DoD D.

WHAT LANDED (waves, in merge order): WAVE 1 (parallel) B enforcement facade `4ba53c4e63`, D-core manifest module `9f46a10baa`, A feedback facade `cf903f8a2d`; WAVE 2 C dispatcher collapse `e962f2b3f5` (deleted locator/runner/plan + the feedback/enforcement bins; `regimen` now dispatches in-process); WAVE 3 D-wire install lifecycle `dc9951c387` (manifest write on install/uninstall, `regimen update`/`install --all`/`--harnesses`, manifest-aware `status`); WAVE 4 E doc/skill sync `fed52e541c`+`f3a225627b`. Each TDD-to-green in an isolation worktree, CodeRabbit-clean on its exact final commits, merged one-at-a-time + pushed autonomously. The hook/gate/daemon ENTRYPOINT SCRIPTS were never touched (the safety property held). cli `package.json` version is still `0.0.0`, so `regimen status`/manifest stamp `0.0.0` until a real version is set (flag for the per-OS run). External `niftymonkey/skills` clone grepped clean for stale `feedback <x>` refs (confirm the remote if certainty wanted).

### (historical) NEXT CONVERSATION = EXECUTE DoD F AS AN OVERNIGHT AUTONOMOUS RUN. The design is DONE (ADR-0012); this is a BUILD, not a design. (executed 2026-06-22; see the COMPLETE banner above)

DESIGN LOCKED 2026-06-22 (architect-deep complete, user-approved via question-form). Captured in ADR-0012 (`docs/adr/0012-unified-regimen-cli-dispatches-in-process.md`), which SUPERSEDES ADR-0009's "shape of the move" section (subprocess -> in-process, because ADR-0010 removed the repo boundary that justified subprocess-only). Read ADR-0012 once, then build. Do NOT re-open the design. ALL DESIGN QUESTIONS ARE ANSWERED; there is nothing to ask the user. If a genuinely new fork appears mid-build, pick the option most faithful to ADR-0012, note it, and keep going.

LOCKED DECISIONS (do not re-derive):
- IN-PROCESS dispatch: `regimen` imports `@regimen/feedback` + `@regimen/enforcement` as LIBRARIES and calls typed command functions directly. DELETE `packages/cli/src/{locator.ts,runner.ts,plan.ts}` + `realSpawn` + their tests (they only bridged a process boundary that no longer exists).
- COMMAND SURFACE = B-lite: `regimen install [--all|--harnesses ...]` / `update` / `uninstall` / `status` (flat lifecycle); `regimen daemon start|stop|restart|status` (grouped); `regimen assess` / `evidence` / `list` (flat primitives). The wiring verbs (wire-hooks, wire-gates, install-daemon, install-skill) become INTERNAL steps of `regimen install`, NOT user verbs. The pillar nouns (feedback/enforcement) leave the user surface entirely.
- BIN COLLAPSE: drop the `feedback` + `enforcement` bins; `regimen` is the one bin (one `bun link` self-link inside `regimen install`).
- LIB FACADE: each package exports per-command functions taking a parsed OPTIONS object; the dispatcher owns ALL argv parsing. Do NOT keep `runCli(argv)` + rebuild-argv (explicitly rejected in ADR-0012).
- MANIFEST at `<dataDir>/install-manifest.json` (dataDir from `@regimen/shared` resolveDataDir). Schema `{ schemaVersion, regimenVersion (from the cli package.json), clonePath, loaderPath, installedAt, updatedAt, entries: [{ harness, pillars, scope }] }`. `scope` is LOAD-BEARING: gemini installs per-workspace (ADR-0011), so an entry records `config-home` vs `workspace:<path>` or `regimen update` cannot re-place gemini.
- `regimen update`: read manifest -> re-resolve clonePath/loaderPath from the CURRENT clone (this rewrites the absolute paths baked into installed hooks/gates/service def) -> re-run idempotent per-harness install for each entry -> cycle the daemon -> restamp. No-manifest fallback = behave like install.

HARD CONSTRAINTS (do not violate): the HOOK/GATE/DAEMON ENTRYPOINT SCRIPTS STAY standalone, harness-invoked by ABSOLUTE PATH and NEVER through the dropped bins (verified): `packages/feedback/hooks/capture-*.ts`, `packages/feedback/src/loader/run.ts`, `packages/enforcement/examples/{rm-rf-gate.ts,em-dash-gate.sh,inline-message-guard.sh}`, `packages/enforcement/hooks/emit-denial.ts`. Capture/gate commands embed `bun <clonePath>/<script>` directly; the daemon runs from the service def's loaderPath. So dropping the bins CANNOT break an installed machine. Only the USER-FACING CLI unifies; the packages stay LIBRARIES.

BUILD DAG (off-thread TDD in isolation worktrees off `origin/main` = `ca2627c135`; isolated per-package `bun run check`, NEVER the workspace `--filter '*'` under load):
- STEP 0 DONE (`ca2627c135`): ADR-0012 is committed + pushed to main; branch the worktrees off `origin/main` = `ca2627c135`.
- WAVE 1 (PARALLEL; independent packages, zero file overlap; branch each off origin/main; merge in any order):
  - UNIT A (feedback lib facade, `packages/feedback`): refactor each internal command (start/stop/restart/status/install-daemon/uninstall-daemon/install-skill/purge/evidence/assess/list/wire-hooks/unwire-hooks/install/uninstall) into an EXPORTED `fn(options)` over a typed options object; rewrite the existing `runCli(argv)` to parse argv into options and delegate (keeps the bin + tests green during transition). FAITHFUL refactor over the green suite. DONE-WHEN: isolated feedback check green + new tests asserting the command fns are callable in-process with options; no behavior change. [LANDED 2026-06-22: `cf903f8a2d` (refactor `b93736a217` + 1 CodeRabbit fix exporting/covering `uninstallSkill`) merged to main `f2aa65a7ad`; feedback check green 547/0, CodeRabbit "No findings" on the exact final commit. NOTE: CodeRabbit rate-limits escalate (6m -> 13m) under repeated reviews; budget ONE clean review per remaining unit, agents self-review hard before invoking it.]
  - UNIT B (enforcement lib facade, `packages/enforcement`): same for wire-gates/unwire-gates/install/uninstall. DONE-WHEN: isolated enforcement check green + callable-in-process tests. [LANDED 2026-06-22: `4ba53c4e63` merged to main `829a0a5ad7`; isolated check green 80/80 (+4 in-process facade tests), CodeRabbit 0 findings.]
  - UNIT D-core (manifest module, in `packages/cli`): pure read/write + schema for the manifest (readManifest/writeManifest/recordInstall/recordUninstall), category-2 (temp data dir in tests), no port. DONE-WHEN: round-trips in a temp dir incl. the `scope` field; isolated check green. [LANDED 2026-06-22: `9f46a10baa` merged to main `79b97f0461`; cli check green 41/0, CodeRabbit 0 findings (scoped `--base-commit ca2627c135`). Surface: `manifestPath/readManifest/writeManifest/recordInstall/recordUninstall` + `Manifest/ManifestEntry/InstallMeta` types; `recordUninstall` has NO time-restamp (deferred to D-wire write-site, by design). Worktree note: a fresh isolation worktree needs `bun install` at root before `bun run check` (runner.test.ts real-spawn needs @regimen/shared link).]
- WAVE 2 (after A + B are MERGED to origin/main):
  - UNIT C (dispatcher collapse, `packages/cli` + drop bins in feedback/enforcement): rewrite the cli to import the A/B facades in-process and dispatch the B-lite surface; DELETE locator.ts/runner.ts/plan.ts + their tests; in-process orchestration (install = feedback.install then enforcement.install with capture-before-gate ordering PRESERVED + tested; uninstall reverse; single `regimen` self-link); DROP the `feedback`/`enforcement` bins from their package.json + delete each package's now-dead `runCli`/bin. DONE-WHEN: isolated cli (+ feedback + enforcement) checks green; in-process tests for dispatch + ordering + fail-fast(install)/best-effort(uninstall); the deleted modules' coverage migrated. [LANDED 2026-06-22: `e962f2b3f5` merged to main `a365f12434`; checks green cli 26/0, feedback 550/0, enforcement 78/0+1skip; CodeRabbit "No findings" (confirming run by orchestrator on exact commit, after a flaky 4-vs-0 agent run; the lone major finding independently verified false-positive: env save/restore via MANAGED_ENV beforeEach/afterEach). Single self-link via `selfLink?:boolean` opt on feedback install/uninstall; capture-before-gate ordering proven by an injected `InstrumentSteps` call-order test. SCOPE-OUT findings deferred to E: feedback's own output still says "feedback status"/double-prints "dry run complete"; an enforcement jq-preflight test is `test.skip` (Bun.which ignores in-process PATH; covered by DoD D turnkey run); a loader-acceptance integration test repointed to the `regimen` bin.]
- WAVE 3 (after C + D-core MERGED):
  - UNIT D-wire (lifecycle on the unified CLI, `packages/cli`): `regimen update` (the algorithm above), `regimen install --all`/`--harnesses` (loop per-harness; gemini per-workspace + one-line notice), `regimen status` (read the manifest), and write the manifest on every install/update. DONE-WHEN: update re-resolves paths + re-installs recorded entries + cycles daemon + restamps in a temp env; install --all loops; tests; check green. [LANDED 2026-06-22: `dc9951c387` merged to main `8915a78b60`; cli check green 41/0 (+15 lifecycle tests), feedback 553/0; CodeRabbit "No findings" first pass (deterministic). Design: a `LifecycleDeps` seam (injected now/version/clonePath/loaderPath/installScope/installableHarnesses/cycleDaemon) mirrors C's `InstrumentSteps`; per-workspace knowledge stays in feedback via exported `installScope`/`installableHarnesses` (gemini -> `workspace:<cwd>`, others `config-home`); update re-runs recorded entries verbatim so gemini's workspace scope is honored. FINDINGS: C's 5 ordering tests were env-coupled (read host data dir, depended on ambient CLAUDECODE); D-wire made them deterministic (temp data dir + pinned REGIMEN_HARNESS). cli package.json version is still `0.0.0` so status/manifest stamp `0.0.0` until a real version is set (plumbing correct; flag for per-OS run).]
- WAVE 4 (LAST, after everything lands):
  - UNIT E (README + program-doc + skill sync): repoint the USER-FACING surface off the "shells out" model + `feedback <x>`/`enforcement <x>` to `regimen <x>`: `README.md` (the "### Core" shell-out wording; the "### Verify" block `feedback status` -> `regimen daemon status`, `feedback evidence` -> `regimen evidence`; add `regimen update` + `regimen install --all`), `install.sh` (header comment), `ARCHITECTURE.md` (the line ~24 `feedback status` ref), and the bundled guidance skills `packages/feedback/skills/feedback-{evidence,judgment}/SKILL.md` (`feedback evidence`/`feedback assess` -> `regimen evidence`/`regimen assess`, + their install-skill tests). SCOPE OUT (leave as historical records, do NOT rewrite): the ADRs (ADR-0012 is the new record) and `docs/plan.md`/`docs/feedback-health.md` (phase/design history). FLAG as a separate follow-up (cannot edit from here): the external `niftymonkey/skills` repo, if any curated skill shells `feedback <x>`. DONE-WHEN: no stale `feedback <x>`/`enforcement <x>` USER-command refs in README/install.sh/ARCHITECTURE/bundled skills; feedback + cli checks green. [LANDED 2026-06-22: `fed52e541c` (repoint) + `f3a225627b` (orchestrator fix: 2 hyphenated `feedback-evidence` command refs in the judgment skill -> `regimen evidence`) merged to main `545c7adc2a`; skill IDENTITY names preserved (only command invocations repointed); CodeRabbit "No findings" on the exact final commit.]

ROUTING (the executing conversation's call via /work-router, NOT prescribed here): what is FIXED is the dependency DAG (A/B/D-core parallel -> C -> D-wire -> E, forced by imports + merge order), the per-unit done-contracts, and the landing posture below. HOW to route each unit is yours to decide LIVE via /work-router (the standing disposition, [[work-router-design]]): apply it per unit, do not just obey this list. It will almost certainly confirm each BUILD unit routes OFF-THREAD in an isolation worktree, but that is a call to make, not a rote, and re-route any sub-task that surfaces mid-build. Judgment calls deliberately left OPEN: how far to fan out a large unit (e.g. the ~15-command feedback facade in A might split), and whether the small doc-only Unit E stays IN-THREAD rather than spawning an agent. The wave list is a skeleton of hard constraints, not a script that replaces work-router.

LANDING POSTURE (user-set 2026-06-22, AFK overnight): AUTONOMOUS merge+push. Each unit TDD-to-green in its worktree, then CodeRabbit (`coderabbit:code-review`) on the EXACT final commits, clear findings (verify each against the code, recent findings have trended false-positive), then MERGE to main + PUSH without waiting. Land merges ONE AT A TIME so CodeRabbit-on-exact-commits stays clean; re-run CodeRabbit after any post-review fix. Priority if budget runs short: A+B+C deliver the unified CLI (ship that first); D-wire is the lifecycle headline; E is last. Update this driver doc's DoD F checkboxes in place as units land (do NOT commit the driver doc).

DISCIPLINE (unchanged): TDD via the `tdd` skill (stub-before-red, minimal-green, refactor-after-green); off-thread isolation worktrees off `origin/main`; producer-before-consumer; isolated per-package `bun run check`; single-line conventional commits `--no-verify`; NO em dash (U+2014); `/stay-within-limits` across the long run.

STATE AT HANDOFF: `origin/main` = `ca2627c135` (clean; ADR-0012 committed + pushed `ca2627c135`; STEP 0 done). After F: only the per-OS acceptance gates (DoD D, hardware: trial Mac ~06-23 + Windows VDI) remain, execution not building.

### READINESS EXECUTION SESSION = COMPLETE (2026-06-21). Units 1-3 + the `list` primitive landed (`origin/main` = `06160ac243`); the answering pass redesigned to compositional. The spec that was executed follows as the historical record.

GOAL (historical): close work-queue items 1-3 from the "Readiness: Definition of Done" section above (the gaps that need NO Mac/Windows), so that once the trial Mac (~06-23) and the Windows VDI are in hand, only the per-OS acceptance gates (DoD section D) and the answering pass (E) remain. Do NOT start the answering pass (WORKSTREAM 2 / B.3), it is separate, design-first, and needs a real corpus. Do NOT attempt the per-OS gates, they need the machines.

DISCIPLINE (unchanged from Session 1): TDD via the `tdd` skill (stub-before-red, minimal-green, refactor-after-green); each unit OFF-THREAD in its own isolation worktree off `origin/main`; producer-before-consumer; isolated per-package `bun run check`; CodeRabbit on the EXACT final commits (re-run after any fix); single-line conventional commits `--no-verify`; NO em dash (U+2014); `/stay-within-limits`. The driver doc + `plans/monorepo-consolidation-playbook.md` stay uncommitted living files.

LANDING POSTURE: set by the launching prompt. Default = present each merge for the user's explicit go before pushing to `main`; if the user says they are AFK, merge+push CodeRabbit-clean units autonomously (like the overnight runs).

ROUTING / PARALLELISM: Unit 1 (feedback) and Unit 2 (enforcement) touch DIFFERENT packages with zero file overlap, so run them as TWO PARALLEL off-thread TDD units in separate worktrees off `origin/main`. Unit 3 is tiny (feedback, separate small branch). Land the MERGES one at a time (Unit 1, then Unit 2, then Unit 3) so CodeRabbit-on-exact-commits stays clean; the branches run concurrently.

UNIT 1 (feedback): Gemini capture-install -> PROJECT-LEVEL `.gemini/settings.json` + `name`+`matcher`.
- WHY: ADR-0011. The planner today writes config-home nested hooks with no name/matcher, the exact shape the Session-1 probe proved does NOT fire headless.
- PRODUCER-FIRST reference (proven firing shape to mirror): `packages/feedback/scripts/e2e-gate.sh` ~139-159 hand-builds the firing hooks, project-level `<workspace>/.gemini/settings.json`, per event `[{"matcher":"*","hooks":[{"name":"regimen-capture-<event>","type":"command","command":"<cmd>"}]}]`. NOTE the wrapper script in the gate is an ISOLATION-ONLY artifact (it injects REGIMEN_DATA_DIR because gemini sanitizes the hook env); PRODUCTION uses the standard `captureCommand` (`bun <clonePath>/hooks/capture-gemini.ts`) directly, no wrapper. So Unit 1 changes only the STRUCTURE (project-level path + name+matcher per group), not the command.
- FILES: `packages/feedback/src/cli/install/capture-hooks.ts` (branch gemini: emit name+matcher groups), `packages/feedback/src/cli/index.ts` (~722-725: gemini install target becomes `<workspace>/.gemini/settings.json`, not `<configHome>/settings.json`), and FIRST rewrite `packages/feedback/tests/install-gemini.test.ts` (it asserts the non-firing no-name/no-matcher config-home shape today; rewriting it to expect the project-level name+matcher shape IS the clean TDD red).
- DECIDE IN-UNIT (keep minimal): gemini capture is per-workspace, so the install verb needs a workspace target, default to cwd, mirror the gate.
- DONE-WHEN: planner + CLI emit project-level name+matcher; `install-gemini.test.ts` rewritten + green; isolated `feedback` + `shared` `bun run check` green; CodeRabbit clean on exact commits; merged per posture. (End-to-end FIRING is re-proven at the per-OS gate, not here; the Session-1 differential already proved the shape fires on Linux.)

UNIT 2 (enforcement): gate-install -> Copilot `versioned-command-leaves` + Gemini project-level/`name`+`matcher`.
- WHY: with all-three-pillars in scope, `packages/enforcement/src/install/gate-hooks.ts` is nested-only, so Enforcement cannot install for Copilot or Gemini.
- PRODUCER-FIRST reference: the CAPTURE planner already solved both shapes, mirror it. Copilot versioned: `capture-hooks.ts` `planVersionedCaptureHooks` (~199-219). Gemini: mirror Unit 1's shape (prefer landing Unit 1 first, or mirror the e2e-gate shape). Gate leaves carry `role:"gate"` (the enforcement marker), not `"capture"`.
- FILES: `packages/enforcement/src/install/gate-hooks.ts` + its tests.
- DONE-WHEN: gate-install emits the correct shape+scope for all four; enforcement tests green; isolated `enforcement` + `shared` `bun run check` green; CodeRabbit clean; merged per posture.

UNIT 3 (feedback, small): Codex first-use hook-trust notice.
- WHY: interactive Codex silently skips untrusted fresh hooks, so first-run capture can be empty until the user trusts the hook once; the installer does not say so.
- SCOPE: print a one-line notice at codex install/wire pointing the user to trust the Regimen hook (and/or a doc line). Keep it tiny.
- DONE-WHEN: the notice prints on codex install/wire; a test covers it; check green; CodeRabbit clean; merged per posture.

NOT IN THIS SESSION (need a machine, or separate): item 4 Windows capture wrinkles (pin during the Windows gate), item 6 the CLOSING GATES (macOS Mac ~06-23 + Windows VDI), and E / WORKSTREAM 2 the answering pass (design-first, needs a corpus).

### SESSION 1 of 2 = COMPLETE (2026-06-21). A + B both DONE; `origin/main` = `05dba4a1ac`. The active start-here pointer is now the READINESS EXECUTION SESSION block directly above this one.

SESSION 1 RESULT: Task A DONE, the gemini hooks-firing probe is settled. A controlled differential (the raw user-level run was contaminated by a free-tier quota crash, so the known-good project-level recipe was run under the identical crash as a control) showed project-level `.gemini/settings.json` fires headless (`SessionStart`/`BeforeAgent`/`SessionEnd` captured even under the crash) while a user-level `GEMINI_CONFIG_DIR/settings.json` fires nothing, so gemini lifecycle hooks fire before the model call and the user-level empty buffer is a real negative. Decision: gemini capture hooks install PROJECT-LEVEL, a divergence from install-once-into-config-home, recorded in ADR-0011 and the gemini row of `docs/harness-divergences.md` (no longer "user-level unresolved"). The user-level capture-hooks planner is therefore NOT taught name+matcher; teaching the planner to write project-level `.gemini/settings.json` with name+matcher is deferred follow-up (ADR-0011 authorizes it, does not build it). A full paid `e2e-gate.sh gemini` pass stays deferred for two reasons unrelated to the decision (free-tier quota exhausted today; the gate's judge face reads the real `~/.gemini/tmp` ignoring the isolated home); the capture-face behavior is proven by the differential, satisfying the done-when's project-level alternative. Task B DONE, the four reference docs (`docs/{event-schema,store-write-contract,harness-descriptor-contract,harness-divergences}.md`) plus ADR-0011 are on `main`; the three stale `contract.ts` comments are corrected (plus the now-stale gemini "no divergence" comment the probe disproved) on a CodeRabbit-clean (0 findings) branch merged to `main`; `plans/four-harness-e2e-recap/` discarded per the user (`.plan-url` never committed). The original SESSION 1 spec follows as the historical record.

### (historical) NEXT CONVERSATION = SESSION 1 of 2: do A + B ONLY, then tee up session 2. Do NOT start Workstream 2 here.

SCOPE OF THIS SESSION: exactly Task A and Task B below, nothing else. Workstream 2 (the headline answering pass) is the SEPARATE session 2, the deliberate kickoff staged in the dormant "### WORKSTREAM 2 KICKOFF" block further down. It is genuinely new design-first work and must not be started by momentum here; keeping it in its own conversation is what keeps the boundary clean. The "NEXT OVERNIGHT RUN" block below this is DONE (e2e gate built, translators + copilot planner merged, `origin/main` = `a9637cc0bd`).

TASK A: the GEMINI hooks-firing PROBE (decision made: "probe user-level, prefer uniform"). One clean producer-first test to settle whether USER-level gemini hooks fire headless, which decides the install model. STEPS: (1) `mktemp -d` an isolated root with `home`/`data`/`work`; seed `home` with `~/.gemini/{gemini-credentials.json,google_accounts.json,settings.json}` (copy only); `touch data/feedback.enabled`; write `work/fizzbuzz.py` (the deliberate-bug fixture, see the gate script). (2) Write a USER-LEVEL hooks file at `home/settings.json` in nested-matcher-groups shape BUT with a `name` AND `matcher:"*"` on EACH group, and each leaf's `command` pointing at a wrapper script that `export REGIMEN_DATA_DIR=<iso data>` then `exec bun <repo>/packages/feedback/hooks/capture-gemini.ts` (gemini SANITIZES the hook env, so the data dir must be set inside the command, not inherited). Keep the seeded auth in settings.json (merge hooks into it). (3) Fire from `work`: `GEMINI_CLI_TRUST_WORKSPACE=true GEMINI_CONFIG_DIR=home REGIMEN_DATA_DIR=data timeout 240 gemini -p "<bug-fix prompt>" --yolo </dev/null`. (4) Check `data/buffer/current.jsonl`. ISOLATION NOTE: this is exactly the project-level recipe that FIRED in the e2e run, moved to user-level scope with everything else held constant, so a fire-vs-no-fire result is a clean user-vs-project answer. OUTCOME: if it FIRES -> teach the capture-hooks planner (`packages/feedback/src/cli/install/capture-hooks.ts`) to emit `name`+`matcher` for the nested format when `harness==="gemini"` (TDD, CodeRabbit on exact commits, merge), update the gemini row in `docs/harness-divergences.md`, and write ADR-0011 (gemini joins the uniform install-once-into-config-home model). If it does NOT fire -> the install is project-level `.gemini/settings.json`; design that and write ADR-0011 recording the divergence. Either way the gemini translator is already merged + proven; this is purely the install path. Full gemini edge facts: `docs/harness-divergences.md`. A IS DONE WHEN: `packages/feedback/scripts/e2e-gate.sh gemini` PASSES both faces (or the divergence is recorded as project-level with the gate run that way), ADR-0011 is written recording the install decision, and the gemini row in `docs/harness-divergences.md` no longer says "user-level unresolved."

TASK B: COMMIT the documentation produced this session (currently uncommitted in the working tree). B1 (docs-only, can go straight to `main`): the four new reference docs `docs/{event-schema.md,store-write-contract.md,harness-descriptor-contract.md,harness-divergences.md}` (event-schema/store-write/descriptor-contract backfill the three docs the code already referenced-but-never-wrote; harness-divergences is the new living harness-edge reference). B2 (CODE change, so feature branch + `bun run check` + CodeRabbit): `packages/shared/src/harness/contract.ts` has THREE now-STALE comments my merged copilot-planner work invalidated -- they say the `format` value is "descriptive metadata: no code switches on it today" and that "no install planner writes the versioned-command-leaves shape yet" / "Copilot hooks WRITING is unimplemented". All three are now FALSE (the capture-hooks planner branches on `format` and writes the versioned shape). Fix those comments to match reality. B3 (the user's call): keep or discard `plans/four-harness-e2e-recap/` (the visual-recap MDX artifacts); if keeping, do NOT commit `.plan-url` (it holds a local bridge token). The driver doc + `plans/monorepo-consolidation-playbook.md` stay uncommitted (living working-tree files, by convention). B IS DONE WHEN: the four docs are on `main`, the `contract.ts` comments are accurate (B2 merged), and the recap-artifact keep/discard call is made.

CLOSING STEP (do this LAST, only once A and B are both DONE): edit this block to mark SESSION 1 complete, and flip the "### WORKSTREAM 2 KICKOFF" block below from DORMANT to ACTIVE so it becomes the start-here pointer for the NEXT (session 2) conversation. That hand-off is how session 1 ends: A done, B done, session 2 teed up.

### WORKSTREAM 2 KICKOFF = the answering pass (QUEUED, not the next session). Reprioritized 2026-06-21: the next conversation does the READINESS EXECUTION SESSION (above) first, because "I can just use it" (the readiness builds + per-OS gates) precedes the answering pass, and B.3's value needs a real corpus that only accrues once capture+judge are installed and in daily use across the four harnesses. Start this AFTER the readiness builds land and some real usage has accumulated. It is still genuinely new design-first work, so START WITH the `architect-deep` skill on the design before any code.

This is the deliberate kickoff of the HEADLINE payoff and the reason the whole effort exists: B.3, the cross-harness answering pass. It is genuinely new orchestration, so START WITH the `architect-deep` skill on the design before any code; do not jump to building.

FRAMING (do not get this wrong, it is the user-corrected mistake): Regimen's Feedback is GENERAL-PURPOSE with TWO first-class faces, QUANTITATIVE (scored/boolean signals like Intent/Outcome) and QUALITATIVE (the judge layer's PROSE narrative). B.3 must keep BOTH: it can SCORE against a supplied structured rubric AND it ALWAYS surfaces the prose, both grounded with provenance (which sessions fed each answer), per-harness PLUS a cross-harness view. The company's `~/docs` rubric CSVs are ONE CONSUMER and an INPUT, NOT the model of evaluation; do NOT build the schema/design around them.

INPUTS (all in place): the two rubric CSVs in `~/docs` (structured capability rubric, ~20 rows R001+, Score Type Pass/Fail 0-1 or Pass/Fail+Satisfaction 0-4, Feature Weight 1-5); the B.1 read-time slice `listJudgedSessions`/`sessionHarnessModel` in `packages/feedback/src/judged/slice.ts` to group/filter the judged corpus by harness+model; the judged store; `docs/feedback-surfacing.md` for the judge surfacing model.

DELIVERABLES: (1) the architect-deep design captured durably (an ADR for the decision, and a `docs/` reference doc for the rubric -> signals mapping, a natural member of the reference tier alongside `harness-divergences.md`); (2) the answering pass built TDD (feed the harness's judged corpus to the LLM; when a rubric is supplied also emit per-capability scores in its Score Type; the prose answer is first-class regardless of whether any rubric is present); (3) a minimal B.4 CLI surface that prints the answers. CAVEAT to state honestly in the output: the corpus is THIN today (the e2e sessions plus whatever real sessions have been captured), so the MACHINERY is the deliverable; the valuable cross-harness answers come from re-running over a fuller corpus after real four-harness usage accrues.

DONE WHEN: the design is captured (ADR + rubric-mapping reference doc), the answering pass + B.4 CLI are merged TDD/CodeRabbit-clean, and the CLI produces a (thin) per-harness and cross-harness answer over the current judged store. DISCIPLINE: same as always (TDD via the `tdd` skill, off-thread builds in isolation worktrees, producer-before-consumer, CodeRabbit on the EXACT final commits, no em dash, single-line conventional commits, isolated per-package `bun run check`, `/stay-within-limits`).

### NEXT OVERNIGHT RUN (DONE 2026-06-21; this block is the historical plan, the run is complete -- see AFK RUN PROGRESS. Originally: a FRESH CONVERSATION EXECUTES THIS, do not execute in the planning conversation)

CONTEXT: the four-harness JUDGE/READ path is DONE+MERGED for all four (see AFK RUN COMPLETE below; `origin/main` = `92f9a9d46d`). What remains for breadth: the LIVE-CAPTURE path (hooks -> capture envelope -> translator -> store, real-time) is wired only for codex+claude; copilot+gemini have readers but NO live-capture translator, and copilot also cannot have its hooks WRITTEN by the installer yet. The user wants a real END-TO-END GATE across all four as the final acceptance, and provided the official eval rubric. The user will START A NEW CONVERSATION to run this; it reads this block first.

AUTHORIZATIONS CAPTURED (user, 2026-06-21):
- FIRE REAL (paid) headless model calls on this box to drive each CLI for the e2e gate. Keep the task SMALL but assessable in BOTH faces of Feedback at once: a CHECKABLE structured outcome (a pass/fail or score) AND enough real process that the judge's PROSE narrative has something true to say about how it went ("did it do it right / did it have a problem"). CANDIDATE PROMPT (executor may refine): a tiny BUG-FIX, which beats write-from-scratch because the prose is substantive even on success and it maps loosely to a real capability (debugging). E.g.: "Here is `fizzbuzz.py` with a deliberate bug (the order of the multiple-of-3 / multiple-of-5 checks is wrong, so `fizzbuzz(15)` returns the wrong thing) and a few asserts that currently FAIL (`fizzbuzz(15)=='FizzBuzz'`, `fizzbuzz(3)=='Fizz'`, `fizzbuzz(5)=='Buzz'`, `fizzbuzz(7)=='7'`). Fix the bug so all asserts pass, then run them to confirm." This yields a clean boolean outcome (asserts pass) AND prose the judge can evaluate (correct diagnosis? minimal fix? did it verify, or flail?). Keep it this small; do NOT build the rubric's questions into the task, the rubric is just one consumer (see Workstream 2 framing).
- GEMINI AUTH WORKS (user-confirmed by running `gemini -p "does this work?"` successfully; my earlier probe wrongly concluded auth was gone because `oauth_creds.json` was absent, the real auth lives elsewhere, do NOT repeat that wrong conclusion). So ALL FOUR harnesses are in scope.
- EVAL QUESTIONS = the two CSVs in `~/docs` (`...Claude...Scoring Workbook...csv` and `...OpenAI Codex...Scoring Sheet...csv`). They are a STRUCTURED CAPABILITY RUBRIC, not free-text: ~20 rows (R001+), columns CapabilityID/Category/Required Capability/Description/Suggested Benchmark/Score Type/Score/Feature Weight(1-5); Score Type is Pass/Fail (0-1) or Pass/Fail+Satisfaction (0-4); identical structure across both sheets (likely same across all four harnesses). B.3 can SCORE against this rubric when it is fed one, but Regimen's answering is GENERAL and ALWAYS also surfaces the PROSE judgment, the rubric is one consumer, NOT the model of evaluation (see Workstream 2 framing).

WORKSTREAM 1 (PRIMARY, the user's named acceptance gate): the four-harness END-TO-END validation gate, which also completes live capture. Per harness, in an ISOLATED config home (`CLAUDE_CONFIG_DIR`/`CODEX_HOME`/`COPILOT_HOME`/`GEMINI_CONFIG_DIR` = a temp dir) and an ISOLATED Regimen data dir (so the user's live `~/.<harness>` and live feedback store are NEVER touched):
  1. Install Regimen capture into the isolated home (`feedback install`, `REGIMEN_HARNESS` set). For COPILOT this REQUIRES first building the hooks-WRITING planner that emits the `versioned-command-leaves` format (the existing planner only writes `nested-matcher-groups`; this is where the `HooksFile.format` literal I added becomes load-bearing, the planner branches on it). Codex/Claude/Gemini use `nested-matcher-groups`, the existing planner works.
  2. Fire the small-but-assessable headless prompt in a scratch cwd with LEAST-PRIVILEGE auto-approve and a timeout: Claude `-p --dangerously-skip-permissions`; Codex `exec` with a sandbox/full-auto; Copilot `-p --allow-all-tools` (scope as tight as works); Gemini `-p --yolo` (or `--approval-mode yolo`).
  3. Confirm hooks fired -> envelopes in the buffer -> loader translates -> events in the store. For COPILOT and GEMINI this requires the live-capture translators `translateCopilot`/`translateGemini`, built PRODUCER-FIRST from the REAL hook payloads THIS run captures (bootstrapping loop: run once with capture installed to capture raw hook payloads into the buffer, INSPECT them, build the translator against the observed shapes, re-run to confirm events land). Register each in `src/loader/translators/index.ts`.
  4. Run `feedback assess` + `feedback evidence` on the session and confirm BOTH faces of Feedback are present and correct: (a) the QUANTITATIVE signals (Intent, Outcome, etc.) landed and are right (e.g. Outcome reflects the asserts passing), AND (b) the QUALITATIVE judge NARRATIVE is a coherent, evidence-anchored PROSE account of how it went (correct diagnosis? minimal fix? verified or flailed?). The user wants BOTH the structured/scorable result AND the prose, so the gate asserts both, not just that capture mechanics fired.
  5. Assert + report per harness. OPTIONAL extension (the old A.4 fuller bar): also land one gate.denial in the store via enforcement, and confirm both skills install.
  DE-RISK: validate ONE harness fully first (CLAUDE, everything already works) to prove the whole gate, THEN fan out. STOP+report if any harness can't run cleanly headless (auth/permission/trust hang) rather than thrashing. This workstream COMPLETES: `translateCopilot`, `translateGemini`, the Copilot hooks-writing planner, and the e2e gate itself (make the gate a repeatable script/test, the durable final acceptance).

WORKSTREAM 2 (SECONDARY, if budget; the headline payoff): B.3 minimal answering pass. FRAMING (user correction 2026-06-21, do NOT get this wrong): Regimen's Feedback is GENERAL-PURPOSE and has TWO first-class faces, a QUANTITATIVE one (deterministic signals that are boolean or scored, e.g. Intent/Outcome and the reserved signals) and a QUALITATIVE one (the judge layer's PROSE narrative, the in-words understanding of how it went). The company's `~/docs` CSV rubric is just ONE CONSUMER that happens to want capability scores; it is an INPUT you can feed, NOT the model of evaluation, and the schema/design must NOT be built around it. B.3 must keep BOTH faces: it can SCORE against a supplied structured rubric (like these CSVs) AND it always surfaces the PROSE judgment, both grounded with provenance (which sessions fed each answer), per-harness plus a cross-harness view. architect-deep the design first (genuinely new orchestration). Feed the harness's judged corpus (harness/model-sliceable via the B.1 `listJudgedSessions`/`sessionHarnessModel` that landed) to the LLM; when a rubric is supplied, also emit per-capability scores in its Score Type, but the prose answer is first-class regardless of whether any rubric is present. Add a minimal B.4 surface (a CLI that prints the answers). CAVEAT to state honestly: any output over a thin corpus (just the e2e sessions + whatever real sessions are captured) is thin; the machinery is the deliverable, re-run over a fuller corpus later.

DISCIPLINE for the run: same as the run below (TDD via the `tdd` skill, off-thread builds in isolation worktrees, producer-first for every translator/reader, CodeRabbit-gate on the EXACT final commits, no em dash, single-line conventional commits `--no-verify`, isolated per-package `bun run check`, driver doc is a living uncommitted file, respect usage limits via `/stay-within-limits`). The LANDING POSTURE (merge+push autonomously vs stop at the gate) is set by the launching prompt of the new conversation. Note: the live e2e runs are sensitive (they auto-approve a headless agent); keep the prompt trivial + the cwd a scratch dir + a timeout on each, and never point capture install at the user's real `~/.<harness>`.

### AFK RUN PROGRESS (2026-06-21, autonomous; live status, newest first)
- E2E GATE RUN (2026-06-21 night, COMPLETE for Workstream 1's code + the gate script; `origin/main` = `a9637cc0bd`, tree clean). FOUR units landed sequentially, each off-thread TDD-to-green in an isolation worktree (translators/planner) or main-thread-validated (gate script), CodeRabbit-gated on the EXACT final commits, merged+pushed+cleaned by the main thread: (1) `c1d17ca757` translateGemini; (2) `67b58de5c1` translateCopilot; (3) `65fa0a4405` copilot versioned-command-leaves hooks-WRITING planner; (4) `a9637cc0bd` the repeatable e2e gate script `packages/feedback/scripts/e2e-gate.sh`. Final main check green: shared 11/0, feedback 504/0. GATE RESULT: the script PASSES both faces (live capture + judge) end-to-end for CLAUDE, CODEX, and COPILOT (e.g. copilot: 13 envelopes -> 13 events 0 quarantined, 5 tools, judge intent=bug-fix outcome=accomplished-cleanly model=claude-haiku-4.5 -- the FULL new copilot chain: merged planner wire-hooks -> real copilot hooks -> new translator -> store -> judge). GEMINI: live-capture path PROVEN (translator merged; real captured envelopes drain to events + evidence) but the all-in-one script gate is deferred pending the gemini hooks-writing design (name+matcher+scope; gemini also writes its transcript to the real ~/.gemini/tmp so the script's judge face won't resolve from the isolated home). Landing posture per the launching prompt: merge+push verified CodeRabbit-clean units autonomously. Isolation pattern proven: per-harness `mktemp -d` root with `home`(config) + `data`(REGIMEN_DATA_DIR) + `work`(scratch cwd); enable by `touch data/feedback.enabled`; `wire-hooks` into the isolated home; fire the real headless agent; drain by running `src/loader/run.ts` foreground (drains pre-existing buffer on startup); `feedback evidence --session <id>` (quantitative/capture face) + `feedback assess --session <id>` (judge face). Session id pulled from the captured envelope `payload.session_id`. AUTH: each isolated home is seeded with a COPY of only its credential file(s) from the real home (real home never mutated) so the authorized headless run can authenticate. Per-harness launch flags discovered: Claude `-p --dangerously-skip-permissions` (auth lives outside the config dir, no seed needed); Codex `exec --dangerously-bypass-approvals-and-sandbox --dangerously-bypass-hook-trust --skip-git-repo-check </dev/null` (untrusted fresh hooks are silently skipped without the hook-trust bypass; open stdin hangs it; seed `auth.json`+`config.toml`).
  - CLAUDE: PROVEN end-to-end. 9 events (session.start/user_prompt/Read+Edit+Bash pre+post/session.end), evidence 1 prompt/3 tools, judge Intent=bug-fix Outcome=accomplished-cleanly model=claude-opus-4-8 + accurate evidence-anchored prose.
  - CODEX: PROVEN end-to-end. 12 events, evidence 5 tools (4 Bash, 1 apply_patch), conv codex/gpt-5.5, judge Intent=bug-fix Outcome=accomplished-cleanly + prose (noted codex reproduced the failure first). Codex live capture (translator already registered) confirmed working.
  - GEMINI: LIVE CAPTURE PROVEN. `translateGemini` built producer-first + MERGED (`c1d17ca757`, off-thread TDD, 482/0, CodeRabbit clean). Real 7 captured envelopes drain cleanly (0 quarantined) -> 7 events (session.start/user_prompt/update_topic+read_file pre+post/session.end), evidence 1 prompt/2 tools. Maps by `hook_event_name` (SessionStart/BeforeAgent/BeforeTool/AfterTool/SessionEnd; payload Claude-like, no model, no tool-call-id). HOOKS-WRITING CAVEAT (producer-discovered, needs the planner work + a design call): gemini did NOT fire our wired user-level settings.json hooks; gemini requires each hook group to carry a `name` AND a `matcher` field (the current nested-matcher-groups planner omits BOTH), uses a `gemini hooks` subcommand + Claude-migrate, fingerprint-trusts hooks, and SANITIZES the hook env (strips REGIMEN_DATA_DIR; prod uses the default dir so fine, but test isolation needs the data dir set inside the command via a wrapper). Only a NAMED+MATCHER project-level `.gemini/settings.json` hook fired headless in my tests; whether user-level fires headless is unresolved. The live gemini agent run also crashed mid-task on a Gemini API error (retryDelayMs/details), so its captured session is incomplete (capture mechanics proven regardless). Gemini also writes its transcript to the REAL ~/.gemini/tmp ignoring GEMINI_CONFIG_DIR.
  - COPILOT: BOTH FACES PROVEN, clean run. `translateCopilot` built producer-first + MERGED (`67b58de5c1`, off-thread TDD, 492/0, CodeRabbit clean). Copilot's hook stdin payload carries NO event name (per GitHub docs "scripts must be dedicated to specific hooks"), so the translator INFERS the event from payload field-shape (toolName[+toolResult]/initialPrompt|source/prompt/reason); epoch-ms timestamp, no model, no tool-call-id. Real 13 captured envelopes drain cleanly (0 quarantined) -> 13 events, evidence 1 prompt/5 tools (view x2, bash, edit, report_intent); assess judge Intent=bug-fix Outcome=accomplished-cleanly model=claude-haiku-4.5 + accurate prose. HOOKS firing: only USER-level hooks at `$COPILOT_HOME/hooks/*.json` fire in headless `-p` (repo-level `.github/hooks` do NOT); per-leaf `env` block sets REGIMEN_DATA_DIR cleanly; COPILOT_HOME isolates the config home (seed config.json+permissions-config.json for auth). Format = `versioned-command-leaves` `{version:1,hooks:{event:[{type,command,env,...}]}}`, confirmed by a real 13-envelope run; hooks-WRITING planner still TODO (below).
  - REMAINING (NOT done this run, for the user): (a) the GEMINI hooks-WRITING planner change. DECISION MADE (user, 2026-06-21): "probe user-level, prefer uniform." NEXT ACTION = one clean producer-first probe: wire user-level `GEMINI_CONFIG_DIR/settings.json` capture hooks WITH `name`+`matcher` and NOTHING else changed, fire gemini `-p --yolo` headless, check the buffer. If they fire -> teach the capture-hooks planner to emit `name`+`matcher` for the nested format when the harness is gemini, and gemini joins the uniform install-once-into-config-home model (write ADR-0011 documenting it). If they definitively do NOT fire -> fall back to project-level `.gemini/settings.json` install (ADR-0011 records the divergence). Either way the gemini translator is already merged + proven; this is purely the install path. Full divergence facts now live in `docs/harness-divergences.md` (the new living harness-edge reference doc). (b) WORKSTREAM 2 / B.3 the minimal answering pass (needs architect-deep design + is "if budget", which this run did not reach; the judged corpus is also still thin); (c) the gemini live agent run is flaky (a Gemini API `retryDelayMs`/`details` crash mid-task in one run) and gemini writes its transcript to the real ~/.gemini/tmp ignoring GEMINI_CONFIG_DIR; (d) macOS re-validation; (e) sibling-archive + OTLP bridge re-point. PER-HARNESS LAUNCH FLAGS for the gate: Claude `-p --dangerously-skip-permissions`; Codex `exec --dangerously-bypass-approvals-and-sandbox --dangerously-bypass-hook-trust --skip-git-repo-check </dev/null` (+seed auth.json/config.toml); Gemini `-p --yolo` + env `GEMINI_CLI_TRUST_WORKSPACE=true` + project-level named+matcher hooks via wrapper (+seed gemini-credentials.json/google_accounts.json/settings.json); Copilot `-C <work> -p --allow-all-tools </dev/null` + COPILOT_HOME isolated user-level hooks (+seed config.json/permissions-config.json).
- AFK RUN COMPLETE (2026-06-21). `origin/main` = `92f9a9d46d`, working tree clean (only `main`, all worktrees/branches cleaned up). THREE units landed sequentially, each TDD-to-green off-thread in an isolation worktree, CodeRabbit-gated on the EXACT final commits, then merged+pushed+cleaned up by the main thread:
  1. A.2 COPILOT (`4a7176b773`, `0e2b4aeacb`, + `da889fb3b3` hooks-format fix). CodeRabbit clean after the fix.
  2. A.3 GEMINI (`4e2fb790a7`, `74d24362db`). CodeRabbit: 1 verified FALSE POSITIVE (hallucinated circular dep), no code change.
  3. B.1 read-time harness/model slice (`92f9a9d46d`). CodeRabbit clean.
  All four harnesses (codex, claude, copilot, gemini) now have a judge-time reader + resolver + registry/contract/descriptor rows; assess/evidence select the right reader per `REGIMEN_HARNESS`. The judged read path is now harness/model-aware (B.1). Usage stayed healthy throughout (~$48 in the 5h window, well under cap).
  - LEFT FOR THE USER (per handoff, NOT AFK-able): (a) the live-capture hook translators `translateCopilot`/`translateGemini` (deferred; live-captured envelopes for those two quarantine until built; judge/assess path is fully functional); (b) Copilot hooks-WRITING format support, i.e. an install planner that emits the `versioned-command-leaves` shape (B.1-unrelated; design call on whether/how to extend the capture/gate planners; see the COPILOT follow-up note below); (c) B.2 corpus-selection + B.3 the answering pass (need the official eval questions + an architect-deep design pass); (d) macOS locator re-validation (needs the Mac, ~Mon 2026-06-23); (e) sibling-archive + OTLP bridge re-point (operational, needs explicit go).
- B.1 read-time harness/model slice: DONE + MERGED + PUSHED (`92f9a9d46d`, single commit). Pure read-time JOIN per ADR-0008 (NO schema change/migration/CLI surface). New `src/judged/slice.ts`: `sessionHarnessModel(db, sessionId) -> {harness, model|null}|null` (the join helper) + `listJudgedSessions(db, {harness?, model?}) -> JudgedSessionSummary[]` (one row per judged session, sliced/filterable by harness+model, carries sessionId/harness/model/complete/outcome) the grouping foundation for B.2/B.3; and `JudgedDigest` (judged branch) now carries `harness`/`model` recovered by the same join. Isolated check feedback 471/0. CodeRabbit clean (0 findings). Edge: a judged session with no `conversations` row defaults to `harness:""`/`model:null` (test-only artifact; production judged sessions always have a conversation row).
- A.3 GEMINI bring-up: DONE + MERGED + PUSHED. 2 commits (`4e2fb790a7` reader+event-sourced `$set` fold, `74d24362db` filesystem resolver + registry/contract + end-to-end assess). Producer-first confirmed against real `~/.gemini/tmp/<alias>/chats/session-*.jsonl` (gemini 0.47.0): event-sourced fold upserts messages by `id` across `$set.messages` snapshots AND append lines (deduped, file order); `$set{lastUpdated}`-only patches ignored; `info`/unknown types skipped+counted; polymorphic content (gemini=string answer, user=array of `{text}`/`{functionResponse}` parts); `thoughts[]` excluded; tool spans synthesized from `functionResponse` (name+id+output, NO call args persisted so no tool_args); same-timestamp collisions handled via `seq`. Resolver is FILESYSTEM (Codex-style, no env session id): `transcriptsSubdir:"tmp"`, locate matches by reading line-0 `sessionId`, resolveCurrent derives the project-alias from cwd basename + newest-by-mtime. Contract row: `GEMINI_CONFIG_DIR`/`.gemini`, `settings.json`/`nested-matcher-groups` (NO divergence). Isolated checks green (feedback 460/0, shared 11/0). CodeRabbit: 1 finding, a VERIFIED FALSE POSITIVE (claimed a circular dep / missing `gemini/adapter.ts` via a hallucinated "layer 6/layer 10" ordering; the file exists, same import pattern as claude/copilot, no back-edge to support.ts, typecheck+460 tests green), no code change so the green-on-exact-commits review stands. NOTE: registering gemini broke 4 pre-existing tests that used `"gemini"` as the unregistered-harness sentinel; the build correctly repointed them to `"cursor"` (still in the enum, still unregistered). `translateGemini` DEFERRED like Copilot. Gemini hook events are Gemini-native CamelCase (SessionStart/.../BeforeTool/AfterTool/PreCompress/Stop).
- A.2 COPILOT bring-up: DONE + MERGED + PUSHED. `origin/main` = `da889fb3b3` (3 commits: `4a7176b773` reader+events, `0e2b4aeacb` resolver+registry+assess, `da889fb3b3` hooks-format fix). Producer-first confirmed against the real `~/.copilot/session-state/<id>/events.jsonl`; judge path + resolver (via `COPILOT_AGENT_SESSION_ID`) + registry/contract/descriptor rows + capture descriptor/producer; reader excludes encrypted `reasoningOpaque`/`encryptedContent`. Isolated checks green (feedback 427/0, shared 11/0). CodeRabbit clean (0 findings) on the exact final commits. Branch + agent worktree cleaned up.
  - FOLLOW-UP for the user (design + build, NOT done here): (1) `translateCopilot` (live-capture hook-translator) DEFERRED, so live-captured Copilot envelopes quarantine until it lands (judge/assess path reads the transcript directly and is fully functional); the Copilot hook event names are camelCase (`sessionStart`/`sessionEnd`/`userPromptSubmitted`/`preToolUse`/`postToolUse`/`preCompact`), only `postToolUse` observed firing. (2) Copilot's hooks-file format is genuinely DIFFERENT from Codex/Claude: I extended the shared `HooksFile.format` union with a harness-neutral structural literal `versioned-command-leaves` and named the Copilot row accurately (the prior placeholder said `nested-matcher-groups`, which was false). This is descriptive metadata ONLY: NO install planner writes the `versioned-command-leaves` shape yet (capture/gate planners hardcode `nested-matcher-groups`, run only for codex/claude), so Copilot hooks WRITING (capture-install + gate-install producing `{version,hooks:{event:[flat-leaf]}}`) is unimplemented future enforcement/install work. If you'd rather keep the union single-membered, the change is a ~2-line revert.

### AFK HANDOFF (2026-06-20 evening; a FRESH CONVERSATION READS THIS BLOCK FIRST and can begin immediately)

CURRENT TRUNK: `origin/main` = `601a409595`, working tree clean (only `main`). Everything from the 2026-06-20 push is MERGED and the foundation is solid + the gate is trustworthy: A.0 complete (enforcement + cli env-driven), A.1 Claude harness (transcript reader + session resolver + registry + install, and the harness-neutral `reader-types.ts` extraction), the scoped pre-commit hook (per-changed-package), the feedback gate DE-FLAKE + SPLIT (fast in-process `check` is ~402 tests in ~3.5s and reliable under load; the genuine subprocess integration tests live in `packages/feedback/tests/integration/` and run via `bun run test:integration`, OUT of the fast gate; issue #39 closed), Unit 5 (harness resolution unified into `@regimen/shared`: `resolveHarnessFromEnvironment` + `HARNESS_ENV_MARKERS`; enforcement detects-at-install and bakes `REGIMEN_HARNESS` into every gate command; NO `claude` default anywhere), and the enforcement per-harness hooks-file fix (gates land in `contract.hooksFile.relativePath`, e.g. Claude `settings.json`, merge-preserving co-hosted config).

THE PROVEN TEMPLATE for a new harness is A.1 Claude. Each bring-up: (a) PRODUCER-FIRST, read a REAL on-disk transcript to confirm the format BEFORE building; (b) a `TranscriptReader` behind the existing port in `packages/feedback` producing the same v1 events + content chunks; (c) a `SessionResolver`; (d) register the harness in `packages/feedback/src/harness/support.ts` AND add its contract row in `packages/shared/src/harness/contract.ts` (configHome env var + default dir, hooksFile relativePath + format, skillsSubdir) plus its capture descriptor; (e) installer wiring. Enforcement now works per-harness automatically (detect + per-harness hooks file already landed). The CLI marker is already in `HARNESS_ENV_MARKERS`.

AFK QUEUE (sequential, each routed off-thread + TDD + producer-first):
1. COPILOT bring-up (do FIRST, the CLEANEST reader). Real transcripts on THIS box at `~/.copilot/session-state/<session-id>/events.jsonl`: plain one-event-per-line JSONL, uniform envelope `{type,id,timestamp,parentId,data}`, taxonomy incl `session.start`, `user.message`, `assistant.message` (model, toolRequests, ENCRYPTED `reasoningOpaque` so chain-of-thought is auto-excluded from judge content, tokens), `tool.execution_start`/`tool.execution_complete`, `permission.*`. A straight line-by-line fold (no `$set`, no rollout dedup). Config home `~/.copilot` (override `COPILOT_HOME`); skills symlink into `~/.agents/skills`; marker `COPILOT_CLI`. DONE-CONTRACT: `packages/feedback` + `packages/shared` checks green in isolation PLUS Copilot reader tests over a real fixture PLUS `copilot` resolving end-to-end (evidence/assess select the Copilot reader for `REGIMEN_HARNESS=copilot`). TDD, CodeRabbit-clean on the exact final commits.
2. GEMINI bring-up (SECOND, the TRICKIER reader). Real transcripts at `~/.gemini/tmp/<project-alias>/chats/session-*.jsonl`: EVENT-SOURCED `$set`-patch JSONL (line 0 = session-init; later lines = a message object OR a `{"$set":{...}}` mutation). The reader must take the LATEST `messages` snapshot (each `$set` re-embeds the full array) or fold the message objects, and EXCLUDE `thoughts[]` (reasoning) from judge content. `content` is polymorphic (a string OR a parts array); tool results arrive as `type:"user"` with a `functionResponse` part. Config home `~/.gemini` (override `GEMINI_CONFIG_DIR`); hooks in `settings.json` (nested-matcher-groups); marker `GEMINI_CLI`. DONE-CONTRACT: same shape as Copilot, for `gemini`.

SEQUENCING: Copilot then Gemini are SEQUENTIAL because both add a row to `support.ts` + `contract.ts` (shared mutable files); parallel worktrees would conflict on those. Route the scout + build off-thread WITHIN each unit. (The B.1 stretch below is independent of the harness registry and MAY run in parallel for more fan-out.)

STRETCH (only if both bring-ups land with budget to spare): B.1 read-time harness/model slice on the judged store (issue #23): join judged signals + narratives back to `conversations.harness` so reads group and compare by harness. This is on the judged READ path (not the harness registry), so it is independent of the bring-ups, and it does NOT need the eval questions. Foundation for Workstream B.

NOT AFK-ABLE (do NOT attempt autonomously, leave for the user / a HITL pass): B.3 the question-to-answer pass (needs the OFFICIAL EVALUATION QUESTIONS, still a PLACEHOLDER in this doc, plus an architect-deep design pass); B.2 the corpus-selection primitive (design-y, pairs with B.3); the macOS locator re-validation (needs the real Mac, none until about Mon 2026-06-23); archiving the old sibling repos + re-pointing the OTLP bridge daemon (operational, needs the user's explicit go, see `plans/monorepo-consolidation-playbook.md`).

DISCIPLINE (settled): route scout + build OFF-THREAD (isolation worktree off origin/main, correct now); TDD via the `tdd` skill (real red-green, stub-before-red, minimal-green); producer-before-consumer; harness names live ONLY in descriptor/contract/marker DATA; per-package `bun run check` IN ISOLATION (fast + reliable now); commit `--no-verify` with single-line conventional messages (no Co-Authored-By / Generated-with footers); CodeRabbit on the EXACT final commits, re-run after any fix; NO em dash (U+2014) anywhere; the driver doc + playbook are LIVING uncommitted working-tree files (update in place, do NOT commit); respect usage limits via `/stay-within-limits` over the long run. The LANDING POSTURE (merge+push autonomously vs stop at the gate) is set by the launching prompt.

### Why Regimen exists (the main point; everything below serves this)

An engineer using an AI coding agent every day has a problem that is mostly about their own practice, not the model. When the work goes badly they often cannot say why; when it goes well they cannot reliably repeat what they did. There is no observability for the interaction between the engineer and the agent. **Regimen exists to close that gap.** The one question it is built to answer: *did the agent do what I wanted, or did I have to do a lot of correcting?* The only honest way to answer that is to record how the work actually went and be able to ask.

That answer-seeking is the **Feedback** pillar, and it is why the other two pillars exist in the order they do:

- **Guidance** instructs the agent through skills it is asked to follow. Advisory; the agent may or may not comply. Useful on its own. This is how you make a spotted problem go away once.
- **Enforcement** compels an outcome through any deterministic mechanism that takes the choice away from the model (hooks, permission and tool gating, pre-merge gates, sandboxing, schema-constrained output). Added when a behavior matters too much to leave to the model, for the iffy cases where Guidance alone is not reliable.
- **Feedback** observes how the work actually went and shows where the interaction is strong and weak. Added when you want to know whether Guidance and Enforcement are actually working, and what else needs sharpening. **Feedback is what makes the other two improvable.**

Regimen is **harness-agnostic and model-agnostic by design**: every schema, signal, interface, and config holds for any agent CLI and any model, and harness-specific detail is confined to a thin capture/adapter edge per harness. In the design's own words, adding a harness is meant to be "one new capture hook file plus one new translator." This push is the first real test of that promise across four harnesses at once.

### What changed (2026-06, confirmed 2026-06-17)

The original plan was a solo trial of **one** harness. The user was told they were in the Gemini evaluation, then moved to the Codex evaluation specifically to guarantee a CLI option, which is why the Codex trial-readiness work came first. While the user was on vacation (early June 2026) the company decided they will evaluate **all four** harnesses:

1. **Codex** (OpenAI) - the spine is built and live-proven.
2. **Claude Code** (Anthropic) - the original capture path; chosen as the next harness to bring up (lowest lift, the user's daily driver).
3. **GitHub Copilot CLI** (`copilot`, `@github/copilot`; the current agentic CLI, not the deprecated `gh copilot` suggester).
4. **Gemini CLI** (`gemini`, google-gemini/gemini-cli).

This turns Regimen's latent multi-harness design (the PRD already carries user stories about "trying a new agent CLI or model" and "signals comparable across harnesses," sliceable by model and harness) into the actual job.

### The headline outcome this push is for

The company has already landed on a fixed set of **evaluation questions** every engineer must answer about each harness. The payoff that makes this push worth doing: **feed those questions to an LLM that answers them grounded in the feedback Regimen recorded across all four harnesses.** That is the core "did this harness serve me?" question asked at evaluation scale, across weeks and across four CLIs, producing the deliverable the company actually needs instead of a from-memory guess at the end of the trial.

The user's stated bare minimum for this push: understand and execute the next steps to make the three-pillar installer work for all four harnesses, and stand up a minimal version of that cross-harness answering capability.

### What already exists (the template; reuse, do not rebuild)

- **The Codex judgment-layer spine is complete and live-proven** (S1 schema/rubric, S2 content reader, S3 `feedback assess`, S4 in-session skill). A real `feedback assess` over a real Codex conversation returned an accurate, evidence-anchored verdict. This is the template every other harness follows.
- **The hub + installer restructuring (ADR-0009) is done**: `regimen install` is a one-command three-pillar front door; `regimen-enforcement` is its own repo; `feedback install` is Feedback-only.
- **The store, envelope, and translator-dispatch spine is already harness-parametric.** The `Harness` enum already lists `claude`, `codex`, `gemini`, `copilot` (and more); the buffer, validation, and store accept all four today.
- **Every captured conversation already records its harness** (`conversations.harness`, `events.harness`). The data needed to slice and compare by harness already lands; nothing downstream reads it yet.
- **Claude already has a translator and a reference gate.** It is the closest harness to ready.

The honest gap carried in from the Codex push: the full install-to-judge loop has still not been exercised end to end on the trial Mac (no machine in hand). That validation (Codex's old "1.12 acceptance") now generalizes to all four harnesses.

### Status and next action (2026-06-17)

Phase 0 and 0b forensics are COMPLETE for all four harnesses: Codex done, Copilot mapped (the cleanest transcript), Gemini mapped (with the version-pin caveat and a tested work-machine `.ps1` at md.niftymonkey.dev `/v/KoX46TJT`), Claude reader still to build. User-side items: the Gemini work-VDI forensics are now DONE (see Phase 0b, the work VDI runs gemini 0.47.0 WITH hooks and the transcript format is confirmed) and the Gemini surface is RESOLVED (the regular `gemini` CLI; Antigravity DROPPED, user-confirmed 2026-06-18); the Mac install in Phase 2 (next week) remains. CHOSEN NEXT BUILD (user, 2026-06-17): Workstream A.0, the harness-parametric refactor, starting with an architect-deep design of the harness-descriptor boundary, then a routed TDD build on a feature branch. Repo sequencing and branch/merge cadence to be confirmed before any build or merge.

UPDATE (2026-06-18, design done overnight, NO builds): the architect-deep pass plus three read-only code scouts are COMPLETE, and the full resolved design is in the "A.0 RESOLVED DESIGN" block under Workstream A below. Headline: the first open question is settled as per-repo descriptors with a published shared-data contract (option b), NOT hub-owned, the scouts overturned the doc's earlier "hub-owned is likely" guess on the ADRs' own terms. The descriptor splits into a pure-DATA part (the cross-repo shared surface) and feedback-private BEHAVIOR (the reader and resolver ports). Feedback-first repo sequencing and the standard per-repo-branch cadence are PROPOSED. AWAITING the user's confirmation of the sequencing and cadence (and one open sub-question, where the shared-data contract doc lives) before any build starts.

GATE CLEARED (2026-06-18, user via the recap question-form): repo sequencing CONFIRMED as feedback then enforcement then hub; branch/merge cadence CONFIRMED as proposed; the shared-data contract doc lives in the HUB's `docs/`. NEW DIRECTION from the same answer: the user is leaning toward collapsing the multi-repo layout into a MONOREPO (feedback, enforcement, the OTLP bridge, and the rest as subdirectories or workspace packages under `regimen`), likely as the NEXT step AFTER this readiness work, because the multi-repo split has created more complexity than it bought. DECISION (mine, accepted unless the user overrides): do A.0 NOW in the current multi-repo layout but build it CONSOLIDATION-FRIENDLY, and run the monorepo consolidation as its own architect-deep plus ADR effort AFTER readiness, not first. Full reasoning and the consolidation-friendliness constraints are DECISION 5 in the A.0 RESOLVED DESIGN block below. Builds may now start on the feedback repo.

HANDOFF / START HERE (2026-06-20, post-merge; a FRESH CONVERSATION READS THIS FIRST and can begin immediately). The MONOREPO CONSOLIDATION is DONE and MERGED to `main` (PR #38, squash commit `c162a4b468`). Everything now lives in ONE repo at `/home/mlo/dev/niftymonkey/regimen`, work on `main`, code under `packages/{cli,feedback,enforcement,otlp-bridge,shared}`. The live feedback capture daemon runs from `packages/feedback` and is healthy. The full consolidation record (and the two remaining consolidation follow-ups: macOS locator re-validation before the trial, and archiving the three old sibling repos read-only) is in `plans/monorepo-consolidation-playbook.md`.

WHERE A.0 STANDS (clears a recurring confusion): A.0, the harness-parametric refactor, spans ALL THREE code instruments and has THREE legs. FEEDBACK leg: DONE + merged (descriptor/ports/registry/env-driven in `packages/feedback`; shared surface in `packages/shared`). ENFORCEMENT leg = Unit 2: NOT started. CLI leg = Unit 3: NOT started (the deferred `--codex-home`/`--harness` removal). Unit 1 (the old contract DOC) is GONE: `packages/shared/src/harness/contract.ts` is the single source now, so there is nothing to author. Unit 4 (Claude) is A.1, a NEW harness, NOT part of A.0.

MORNING SESSION (2026-06-20, this conversation, IN PROGRESS): the three overnight branches are MERGED into LOCAL `main` (a0-cli-forward fast-forwarded bringing both A.0 commits; a1-claude via merge commit `fa887e4c17`), then the scoped pre-commit hook fix committed locally (HEAD `b6f7e5048f`). NOTHING IS PUSHED yet: the push is deliberately held and the auto-mode classifier also blocked a bare push (needs an explicit go on a trustworthy gate). Merged `main` verified green for shared/enforcement/cli; feedback is 423/424, the one failure being the RECURRING flaky test (the `install-capture-hooks-cli` unwire SUBPROCESS test) hanging under the live daemon's load. That flake (hit ~10x over weeks) is now tracked as GitHub issue #39; a clean-room experiment PROVED an in-process test is the fix (an independent agent re-derived it in-process and saw 0 failures across ~115 runs incl. under load; the subprocess cold-start vs the 30s per-test timeout is the cause). An in-process REBUILD of the whole `install-capture-hooks-cli.test.ts` is IN FLIGHT (branch `fix/deflake-install-capture-hooks-cli`, preserving all 10 tests). SEQUENCING: land the de-flake -> push `main` -> build Unit 5. Cleanup pending: the merged local branches (a0-enforcement, a0-cli-forward, a1-claude) and the agent worktrees under `.claude/worktrees/` are not yet removed. Also: the new scoped hook still runs feedback's `check` on feedback commits, so until the de-flake lands a feedback commit can hang if the daemon is up (sibling subprocess test files `install-cli.test.ts`/`install-skill.test.ts` are the same flake class, candidate follow-ups noted on #39). UPDATE (later 2026-06-20): the de-flake was cherry-picked onto local `main` (HEAD now `e479f0207c`) and VERIFIED passing in the main tree, but the flake is SYSTEMIC, the full check then failed on `loader-acceptance.test.ts` (same subprocess-cold-start-vs-timeout class). The whole feedback subprocess-integration suite is affected (9 files). User chose the STRUCTURAL SPLIT (issue #39 broadened): convert the 4 PURE-CLI tests (`cli`, `cli-assess`, `install-cli`, `install-skill`) to in-process so they stay in the fast `check` gate; move the 4 GENUINE integration tests (`capture`, `capture-codex`, `concurrent-producers`, `loader-acceptance`, which spawn the hook/loader) into a separate deliberate `test:integration` step OUT of the fast gate. This is now the unit BEFORE Unit 5. BLOCKER (RESOLVED): worktree agents branch off `origin/main`, so the merged+deflaked `main` was PUSHED (user confirmed) and is now `origin/main = e479f0207c`; the merged feature branches (a0-enforcement, a0-cli-forward, a1-claude) + the de-flake branch + all agent worktrees were cleaned up (only `main` remains locally). The STRUCTURAL-SPLIT agent is IN FLIGHT on branch `fix/feedback-test-gate-split` off the fresh origin/main (convert the 4 pure-CLI tests in-process, move the 4 hook/loader integration tests to a deliberate `test:integration` out of the fast `check`). NEXT: verify + land the split (fast gate reliably green), then build Unit 5 on the trustworthy gate. DONE: split landed in local `main` (`d3ebeaf850`), fast gate PROVEN reliable in the MAIN TREE under the live daemon (3/3 green, 402 tests ~3.3-3.7s each, vs the prior 30-min hang); the 4 hook/loader integration tests now live in `packages/feedback/tests/integration/` run via `bun run test:integration` (deliberate, clean-env). Issue #39 RESOLVED (close on push). Awaiting explicit push of `d3ebeaf850` to origin, then Unit 5 builds off the updated origin/main.

OVERNIGHT RUN COMPLETE (2026-06-20, both tracks finished and independently verified; ALL THREE branches pushed, UNMERGED, awaiting the morning HITL merge gate; `main` untouched at `c162a4b468`). Both ran as parallel autonomous background agents, each in its own worktree off `main`, TDD to green + CodeRabbit-clean, committed with `--no-verify` (the husky pre-commit IS the workspace-wide `bun run --filter '*' check` that guardrail 2 forbids under concurrent load, so the isolated per-package `bun run check` was the gate instead). RESULTS:
- TRACK A (A.0 finish): Unit 2 `a0-enforcement` (1 commit `0b4a9a6419`) folds the rm-rf gate pair into one harness-agnostic gate, imports the contract from `@regimen/shared` (no copy), `resolveCodexHome` to `resolveHarnessHome`, drops `--codex-home`/`--harness`, resolves from `REGIMEN_HARNESS` only and fails closed. Isolated check 62/0 (+8 tests). CodeRabbit 0 findings. Unit 3 `a0-cli-forward` (stacked off `a0-enforcement`, 1 commit `ee6c138144`) makes the cli set `REGIMEN_HARNESS` on the spawned child env (config home rides the existing `process.env` forwarding, no `@regimen/shared` import in the cli), drops the `--codex-home` parse/forward. Isolated check 29/0 (+6 tests). CodeRabbit 0 findings. A.0 IS NOW COMPLETE (feedback merged earlier; enforcement + cli are these two branches).
- TRACK B (A.1 Claude): `a1-claude` (off `main`, 2 commits `749fa727e2` then `4d38a3fe02`) adds the producer-first Claude transcript reader (confirmed against a LIVE `~/.claude/projects/<slug>/<session-id>.jsonl`: one JSON object per line, only `user`/`assistant` carry conversation, assistant blocks are `thinking`/`text`/`tool_use`, `thinking` excluded from judge content, no session-meta line so start/end are synthesized), the direct-session-id resolver (no stamp/scan), the `claude` registry + contract row (`CLAUDE_CONFIG_DIR`/`.claude`, verified against docs) in `packages/shared`, installer wiring, and Decision 4's reader-type extraction (`RolloutReadResult`/`RolloutReadOptions`/`ContentChunk`/`AnchorRef`/`QuarantinedRecord` to `src/loader/reader-types.ts`, codex-reader re-exports so codex tests stay green). End-to-end `assess`/`evidence` select the Claude reader for `REGIMEN_HARNESS=claude`. Isolated check feedback 424/0 (+30), shared 1/0. CodeRabbit 1 finding (merge the two sibling folds) verified and DISMISSED: the Claude reader deliberately mirrors the Codex reader's two-fold structure (the anchor rules must not drift from the event fold), guarded by the anchor-resolution test, not by code-sharing. NO code changed after review, so the green review stands on the exact pushed commits.
- MERGE ORDER for the morning: `a0-enforcement` first, then `a0-cli-forward` (stacked, rebase onto the new main and RE-RUN the gate if `a0-enforcement` is squash-merged); `a1-claude` is fully independent (zero file overlap with Track A: A touches `packages/{enforcement,cli}`, B touches `packages/{feedback,shared}`, no conflict). Each branch is pushed and CodeRabbit-clean on its exact commits.
- NEXT after these merge: (1) Unit 5, unify harness resolution in `@regimen/shared` (architect-deep done 2026-06-20, user chose "detect everywhere"; fixes the rm-rf `claude` mislabel surfaced in this review; full spec in "## Next work queue"). (2) A.2 Copilot and A.3 Gemini fan out (they depend on Decision 4's `reader-types.ts`, which lands with `a1-claude`). Plus the two consolidation follow-ups (macOS re-validation, sibling-archive + bridge re-point). ALSO pending the user's go: a scoped pre-commit hook fix (`.husky/pre-commit`, edited in the working tree, uncommitted, to run only changed packages' checks instead of the workspace-wide run, which is the three-repo holdover that flakes under parallel load).
- WORKTREE NOTE: the two agent worktrees persist under `.claude/worktrees/agent-*` (Track A holds `a0-cli-forward`, Track B holds `a1-claude` checked out). Branches also live as local refs + on origin, so merging from `main` does not need them; `git worktree remove` them after merge if desired (`.claude/` shows as untracked in `main`, do not commit it).

THE OVERNIGHT AFK PLAN (chosen 2026-06-20 to maximize parallel autonomous progress; user wants to wake to as much BUILT as possible). Run TWO PARALLEL autonomous tracks, each on its own branch in a WORKTREE off `main`, each TDD over its package's green suite, each brought to green + CodeRabbit-clean and then STOPPED at the merge gate (user merges in the morning; never auto-merge, never auto-push a merge):
- TRACK A (finish A.0): Unit 2 enforcement env-driven FIRST, then Unit 3 cli forward leg STACKED on Unit 2's branch. Unit 3 needs Unit 2's code: the cli stops forwarding `--codex-home`/`--harness` and instead sets `REGIMEN_HARNESS` + the harness config-home env var in the spawned child env, which only works once enforcement READS them (Unit 2). So 2 then 3, chained.
- TRACK B (A.1 Claude): Unit 4 Claude bring-up, IN PARALLEL with Track A. Independent (feedback-only, builds on feedback's done A.0). Highest dogfooding value (user's daily driver) and the deepest unit (the judge-time transcript reader), so de-risk it first. PRODUCER-FIRST: read a REAL `~/.claude` transcript (this box runs Claude Code; sessions at `~/.claude/projects/<slug>/*.jsonl`, including the very conversation that launches the work) to CONFIRM the on-disk format BEFORE building the reader, then TDD it. Claude is the LOWEST-lift new harness (a `translateClaude` translator and a reference gate already exist; `hooks/capture.ts` already stamps `claude`).

WHY parallel, not strict A.0-first: Claude does not need enforcement/cli A.0; serializing leaves the highest-value unit idle all night. A new harness exercises FEEDBACK's A.0 (done); any gap it reveals is a feedback/shared fix, orthogonal to enforcement/cli, so there is no unstable-foundation risk. Copilot (A.2) and Gemini (A.3) are NOT in tonight's parallel set: they depend on Unit 4 extracting the shared reader types (Decision 4 below), so they fan out AFTER Claude.

OVERNIGHT GUARDRAILS (do not skip, all learned the hard way on 2026-06-19): (1) WORKTREE ISOLATION for the parallel tracks, they share one repo tree. (2) Run each package's `bun run check` IN ISOLATION (`cd packages/<x> && bun run check`), NEVER the parallel workspace `bun run --filter '*' check` under load: the feedback and enforcement spawn-per-assertion suites FLAKE and even HANG under concurrent CPU pressure (the live capture daemon adds load too); single isolated runs are reliably green (feedback 394/0, enforcement 54/0, cli 23/0, bridge 84/0, shared 1/0). Do NOT hammer a suite back-to-back; one clean isolated run is the signal. (3) Respect the 5h/weekly usage limits over a long run via the `/stay-within-limits` skill (check between waves, pause near the cap, resume when clear). (4) GATE THE MERGES: build + commit to the branch, run CodeRabbit on the EXACT final commits (the CLI reviews the LOCAL diff; scope under the 150-file free-plan cap with `--base-commit <parent> -t committed`), clear findings (verify each against the code, recent findings have been false positives), then STOP and present. Pushing a feature branch is fine; merging to `main` is the morning HITL gate.

STANDING DISCIPLINE (settled, do not re-confirm): route scout + build OFF-THREAD; TDD via the `tdd` skill (real red-green, stub-before-red, minimal-green; the suite stays green throughout). Harness names live ONLY in descriptor/contract/marker DATA, never a generic code path. No em dash (U+2014) anywhere. Single-line conventional commits, no Co-Authored-By / Generated-with footers. The driver doc and the playbook are LIVING uncommitted working-tree files, update in place, do NOT commit them. Re-read each target file region LIVE before editing (this doc's line numbers and code shapes drift). The per-unit specs are in "## Next work queue" below (now re-homed to `packages/*`); the A.0 design rationale is in the "A.0 FEEDBACK BUILT", "ENV-DRIVEN REFACTOR BUILT", and A.0 DECISIONS reference blocks further down.

CONSOLIDATION RESOLVED (2026-06-20): the monorepo consolidation that this sequencing note debated is now DONE and MERGED (see START HERE above and `plans/monorepo-consolidation-playbook.md`). The readiness units have re-homed into the monorepo and are the active work; start them per the OVERNIGHT AFK PLAN in START HERE. The "## Monorepo consolidation" section below is kept as the historical/execution record of that effort.

A.0 FEEDBACK BUILT (2026-06-18, overnight AFK run; branch `a0-harness-descriptor` in regimen-feedback, NOT pushed, NOT merged, `main` untouched; driver doc left uncommitted as intended). The feedback half of A.0 is implemented and green via a TDD producer-then-consumer relay, three commits on the branch:
- `bcb1fcb670` (judge core): new `src/harness/` module: `contract.ts` (the byte-identical-across-repos shared DATA, Decision 5 constraint 1, pure data, header-marked with its hub-doc reference), `descriptor.ts` (feedback-private capture data: events, producer script, leaf marker), `ports.ts` (the `TranscriptReader` and `SessionResolver` interfaces, Decision 3), and `support.ts` (the `harnessSupport(harness)` registry mirroring `TRANSLATORS`, plus `resolveHarnessHome` and `DEFAULT_HARNESS`). `src/codex/adapter.ts` lifts the Codex reader (`rolloutRead`) and session resolution (`resolveCurrentSession` + `locateRolloutFile` + the newest-is-open fold) behind the ports with no logic change. `assess.ts` now does one `harnessSupport` lookup, fails closed on an unregistered harness before any store write, and reads via `reader.read(content, {complete: !open})` with openness owned by `resolver.locate`. The stamp path generalized from `dataDir/codex/sessions` to `dataDir/<harness>/sessions`.
- `35f16df7be` (install wiring): the capture-hooks planner (`planCodexHooks` to `planCaptureHooks`; file `codex-hooks.ts` to `capture-hooks.ts`) and the skill installer now read events/producer/marker/hooks-file/skills-subdir from the descriptor/contract; install/uninstall derive the home via `resolveHarnessHome` and accept `--harness` (default codex). The produced Codex `hooks.json` and skill targets are byte-identical to before (verified by a real wire-hooks run).
- `f54943ef31` (evidence): `feedback evidence` session resolution routed through the registry too, removing the last hardcoded `"codex"` from a generic CLI path.
DONE-CONTRACT MET: `bun run check` green (388 pass, 0 fail; was 372, +16 new tests) on the final HEAD; the new tests cover the registry, both ports, and the unknown-harness fail-closed (a valid-but-unregistered harness like `gemini`). The existing suite stayed green throughout (faithful refactor). No em dashes. Harness-agnostic invariant holds: a harness name now lives ONLY in the descriptor/contract/registry (plus `DEFAULT_HARNESS`, the untouched translator registry per Decision 3, and the deferred `--codex-home` flag below).

ENV-DRIVEN REFACTOR BUILT (2026-06-19, sequencing step 2 / decision 1+2; same branch `a0-harness-descriptor`, two more commits, now PUSHED to origin, UNMERGED, `main` untouched, driver doc left uncommitted). CodeRabbit ran on the exact 5-commit branch vs `main` and returned ONE finding, a verified FALSE POSITIVE (it flagged `unknownDigest("", Date.now, ...)` at `src/cli/index.ts:481`, claiming `Date.now()`; but that param is a clock FUNCTION `now: () => number = Date.now`, so `Date.now` is correct and `Date.now()` would fail both typecheck and runtime, do NOT "fix" it). No code change; not re-run. Routed off-thread (fresh read-only scout of the seams, then one TDD build agent over the green suite); independently verified in the main thread (clean `bun run check`, residual-flag and em-dash greps, marker-map and skill-neutrality reads). Two commits:
- `7b55e0aa7a` (resolver + marker data): `HARNESS_ENV_MARKERS: ReadonlyMap<Harness, string>` added to `src/harness/descriptor.ts` (feedback-private descriptor DATA, all four CLI-set markers: claude `CLAUDECODE`, codex `CODEX_THREAD_ID`, gemini `GEMINI_CLI`, copilot `COPILOT_CLI`). New `resolveHarnessFromEnvironment(env): Harness | undefined` in `src/harness/support.ts`: explicit `REGIMEN_HARNESS` wins (validated via `asHarness`, THROWS on an unknown value rather than falling through to detection), else first present-and-non-empty marker, else `undefined` (fail closed). The marker map covers all four even though only codex has a support entry; a detected-but-unsupported harness (e.g. `claude`) fails closed at the existing `harnessSupport` lookup. No fake capture/descriptor data invented for the three unbuilt harnesses.
- `d22fb29956` (CLI + skills): dropped `--harness` and `--codex-home` from every command (install/uninstall/wire-hooks/unwire-hooks/install-skill/assess/evidence) and REMOVED `DEFAULT_HARNESS`; every call site now resolves via `resolveHarnessFromEnvironment(process.env)` and fails closed with a shared `could not determine the harness` message. `--session` and `--dry-run` kept. The legacy hardcoded `process.env.CODEX_HOME` check in the assess path was generalized to the resolved contract's `configHome.envVar` (no harness literal left in a generic CLI path; home override is now the harness's own config-home env var). Both bundled skills (`feedback-evidence`, `feedback-judgment`) made fully harness-NEUTRAL: example commands are bare `feedback evidence` / `feedback assess` (no `--harness`), no harness named in title/description/prose. Subprocess tests that drove `--harness codex` now set `REGIMEN_HARNESS=codex` in the child env; the install-skill assertions updated to the neutral commands.
DONE-CONTRACT MET: `bun run check` green (394 pass, 0 fail; was 388, +6 net) on the final HEAD, re-confirmed by a clean main-thread run. New unit tests in `tests/harness-support.test.ts` cover each of the four markers resolving, the `REGIMEN_HARNESS` override winning over a present marker, the invalid-`REGIMEN_HARNESS` throw, and the fail-closed (no marker + no override) path; tests pass `env` as a plain object (no `process.env` mutation). No em dashes; no residual `--harness`/`--codex-home`/`DEFAULT_HARNESS` in `src/`. FINDING (pre-existing, orthogonal, NOT introduced here): the integration suite is contention-sensitive under sustained back-to-back runs (subprocess-per-assertion tests racing fixed timeouts; already documented in `tests/loader-acceptance.test.ts`'s header). A different unrelated lifecycle/daemon/install test can flake under CPU pressure; single clean runs and the pre-commit gate are reliably 394/394, and none of the new harness tests ever flaked. Left untouched as out of scope; candidate follow-up.

A.0 DECISIONS, surfaced by the build and RESOLVED 2026-06-18 (user, via remote-control):
1 + 2 RESOLVED as VENDOR-AGNOSTIC, ENV-DRIVEN config with PER-INVOCATION HARNESS DETECTION (updated 2026-06-19 after a four-harness detection forensic). NO per-harness defaults and no harness flag; config comes from the environment. The harness is resolved by: explicit `REGIMEN_HARNESS` wins (the same env var the enforcement gates already set), ELSE descriptor-driven ENV-MARKER DETECTION (the CLI checks which harness's CLI-set marker is present in its environment), ELSE fail closed. `--harness` and `--codex-home` are DROPPED (`--codex-home` gives way to the harness's own config-home env var, e.g. `CODEX_HOME`); `DEFAULT_HARNESS` is REMOVED. `--session` and `--dry-run` STAY as flags, but the bundled skills go fully NEUTRAL (`feedback assess` / `feedback evidence`, no harness named), which the auto-detection makes possible and which fits the shared `~/.agents/skills` dir.

WHY detection and not a static `.env`/default: the user runs UP TO FOUR harnesses CONCURRENTLY on ONE machine sharing one data dir (see [[regimen-multi-harness-per-machine]]), so no single static value can disambiguate; the harness must be detected per call. THE DETECTION FORENSIC (a probe run inside all four agents on the dev box, 2026-06-19; raw outputs at `/tmp/harness-probe-*.out`) found every harness sets a distinctive CLI-set env marker in the shell-command context: Claude `CLAUDECODE` (+ `CLAUDE_CODE_*`), Codex `CODEX_THREAD_ID` (+ `CODEX_*`), Gemini `GEMINI_CLI`, Copilot `COPILOT_CLI` (+ `COPILOT_AGENT_SESSION_ID`). Detect on these CLI-set markers, NOT generic provider keys (`ANTHROPIC_API_KEY` is present in Codex's env too, it is the ambient key). Process-ancestry detection was REJECTED: Codex runs the shell in a `bwrap --unshare-pid` sandbox so the `codex` process is OUTSIDE the visible /proc tree (ancestry fails for Codex), and /proc-walking is per-OS whereas env markers are cross-platform. The markers are descriptor DATA (one sanctioned home for harness specifics; likely the feedback-private descriptor since enforcement gets `REGIMEN_HARNESS` set explicitly and does not detect, the exact contract-vs-descriptor placement is a small build call). BONUS forensic findings (record, do NOT act on yet): Codex exposes `CODEX_THREAD_ID` to the shell, contradicting the old "Codex exposes no session id" assumption, so the Codex resolver might later drop the filesystem stamp if `CODEX_THREAD_ID` maps to the rollout id; Gemini is the only one with no session id in the shell env (keeps filesystem resolution).

This is THE IMMEDIATE NEXT BUILD UNIT (A.0 was reviewed and APPROVED by the user via a visual recap, 2026-06-19), landing on the same `a0-harness-descriptor` branch BEFORE the enforcement leg. It deletes both shims the A.0 build flagged, so the surface the hub later forwards to is env-driven with nothing to unwind (the hub sets `REGIMEN_HARNESS`/config-home in the child's env instead of forwarding flags). BROADER PRINCIPLE the user set, applies repo-wide: Regimen config is vendor-agnostic, no per-harness defaults, env over a proliferation of CLI flags (`--judge-model`, already an env-default-plus-override, is a later candidate to fold the same way).
3 SCHEDULED: the shared-data contract DOC (hub `docs/harness-descriptor-contract.md`) is authored at the START of the enforcement leg (user-confirmed), so enforcement reproduces the shared subset from a WRITTEN spec, not by reading feedback's source. Purpose recap (the user had lost the thread on this, so keep it): it is the canonical spec both repos' DUPLICATED shared-data copies are checked against (config home, hooks-file path + nested format, skills subdir), preventing drift on the single shared `hooks.json` that BOTH repos write capture and gate leaves into; the program-level analog of `store-write-contract.md`, and the monorepo extraction target (Decision 5).
4 SCHEDULED for A.1 (Claude): the reader-type extraction (move `RolloutReadResult`/`RolloutReadOptions`/`ContentChunk`/`AnchorRef` out of `codex-reader.ts` into a harness-neutral module) happens when the second real reader makes the codex-named home actively misleading rather than merely untidy. Deferred with an explicit WHEN, per the user.

NEXT (sequencing): (1) DONE: A.0 feedback built and reviewed/APPROVED by the user (2026-06-19, via the visual recap). (2) DONE (2026-06-19): the env-driven + env-detection refactor on branch `a0-harness-descriptor` (harness from `REGIMEN_HARNESS` override OR descriptor-driven env-marker detection, fail closed; dropped `--harness`/`--codex-home`/`DEFAULT_HARNESS`; markers added to the descriptor; bundled skills neutralized; install/skill/CLI tests updated). Built green via TDD over the suite; `bun run check` 394/0, +6 new tests. See the "ENV-DRIVEN REFACTOR BUILT" block above. PUSHED to origin, CodeRabbit-clean, UNMERGED (awaiting the user's merge call). (3) ENFORCEMENT leg, (4) HUB leg, then A.1 Claude. These are now spelled out as ready-to-execute units with per-unit done-contracts in "## Next work queue" below, the actionable version of this sequencing; start there.

## Monorepo consolidation (LEAD EFFORT; do this BEFORE the readiness units)

STATUS (2026-06-20): DONE and MERGED to `main` via PR #38 (squash `c162a4b468`; the 6 branch commits collapsed into one). ADR-0010 ACCEPTED. 556/0 green; the live feedback daemon runs from `packages/feedback` and is healthy. Completion gates A (hub fully out of code), B (shared eslint/prettier config with per-package overrides), C (operational cutover) all DONE; a post-merge audit + cleanup (broken `install.sh`, redundant per-package files, stale docs) also landed and is CodeRabbit-clean. REMAINING consolidation follow-ups (NOT readiness work): (1) macOS re-validation of the locator path resolution on the real Mac topology, required pre-trial, not doable on WSL (user has no Mac access until Monday 2026-06-23); (2) archive the three source siblings read-only on the user's explicit go, which also re-points the OTLP bridge daemon (still running from the old sibling path) to `packages/otlp-bridge`. Full execution record + per-gate evidence in `plans/monorepo-consolidation-playbook.md`. The readiness units (in "## Next work queue") have re-homed into the monorepo and are the ACTIVE work now.

DECISION (2026-06-19, user chose "consolidate first, scout-sized"; read-only sizing scout DONE; GO recommended): fold `regimen-feedback`, `regimen-enforcement`, and `regimen-otlp-bridge` into the hub `regimen` as Bun WORKSPACE packages (recommended layout `packages/*`), deleting the cross-repo duplication tax and the cwd/work mismatch (cloud conversations run in the hub; the code will finally live there too). This supersedes ADR-0004 (multi-repo by instrument); the logical instrument boundaries SURVIVE as package boundaries, only the physical layout changes.

SIZING (from the scout; primary-source, corroborated by sub-agents):
- Bottom line: mechanical and low-risk, with ONE genuine refactor and one framing correction. NO CI, NO publish/release tooling, NO cross-repo build/runtime imports anywhere (the big cost-saver). `tsconfig.json` byte-identical across all four, devDeps version-aligned, no name collisions, no dependency conflicts, four `bun.lock` collapse to one.
- Effort ~1.5 to 3 focused days: ~1 day pure-MECHANICAL (root `package.json` + `workspaces`, move the four trees, collapse lockfiles, shared base tsconfig, the locator path swap + test fixtures, doc rewrites, relocate Husky to the new git root), ~0.5 to 1 day for the shared-data package extraction, plus buffer for install re-validation.
- Risk LOW-to-MEDIUM trending low. SINGLE BIGGEST RISK: the locator path swap silently mis-resolving the instruments on the macOS trial topology. It is one constant + one join, but it is exactly the line the trial install runs and it fails LATE (exit 2, `EXIT_LOCATE_MISS`), so re-validate with a real `--dry-run` PLUS a wet install on the actual Mac layout, not just green unit tests.

THE INSTALL FRONT DOOR (load-bearing change, and the trial intersection): the "separate sibling repos" assumption is concentrated in `regimen/src/locator.ts` ONLY: the `SPECS` table `conventionalDir` values (~lines 58-71) and the convention-leg join `join(dirname(ctx.hubCloneRoot), spec.conventionalDir)` (~line 106). The explicit-flag (`--feedback-path`) and env-var (`REGIMEN_FEEDBACK_PATH`) legs are UNCHANGED; only the convention leg moves to something like `join(ctx.hubCloneRoot, "packages", name)`. Downstream is already safe: `src/runner.ts` cwd-routes each spawn by resolved `cloneRoot` (so `bun link` keeps acting on the right package), `plan.ts`/`cli` carry no path assumptions, `--with-bridge` adds no install step today. Re-run the four hub tests (`locator`/`cli`/`runner`) plus enforcement's `codex-gate-hooks` after the swap.

THE ONE REAL REFACTOR (shared-data package): the duplicated surface is NOT byte-identical copies, so this is refactoring, not copy-move. Where it lives today: the `Harness` set + `asHarness` + `traceIdFor` in feedback `hooks/event-log.ts` (note: in `hooks/`, not `src/`) AND inlined in enforcement `src/denial-store.ts`; the data-dir resolver as divergent `data-dir.ts` variants in feedback and the bridge plus inlined in enforcement; feedback `src/harness/contract.ts` (whose own header already says it "merges with zero diff" on consolidation). Blast radius ~25 import lines on the feedback side; enforcement's side is trivial (self-contained in one file). Recommendation: KEEP the published store-write seam (`feedback/docs/store-write-contract.md` + its conformance test); its value is the OPEN FORMAT for any future non-TS/external producer, independent of the repo boundary, so only de-duplicate the helper CODE.

THE OTLP BRIDGE (framing correction): NOT orthogonal to the duplication, it carries its own mirrored `data-dir.ts` and store-row types, so it is a THIRD consumer of the shared-data package. But it has zero build/runtime import coupling (only reads `feedback.db`), the readiness work does not touch it, and `--with-bridge` adds no install step, so it can fold in as a package now OR be left a separate repo for now with zero trial impact (lowest priority of the four).

PRECONDITIONS before the move (so the merge captures the intended tree state): (1) merge feedback's `a0-harness-descriptor` to feedback `main` (the A.0 + env-driven work, pushed/CodeRabbit-clean/unmerged) so feedback enters the monorepo at its intended HEAD; (2) settle the bridge's three open feature branches (`feat/bound-bridge-log`, `feat/realign-bridge-to-sqlite`, `fix/traces-emit-once-spans`).

EXECUTION ORDER (the fresh conversation runs this):
1. architect-deep + an ADR (supersedes ADR-0004): the workspace layout (`packages/*` recommended), the shared-data package's surface, the keep-the-seam call, and the git-history call (clean-cut vs `git subtree`; scout leans clean-cut given low commit counts 35/63/17/2). THIS is the deliberate design gate and the user's confirm point BEFORE any destructive move.
2. Mechanical merge (the ~1 day of MECHANICAL items above).
3. Shared-data package extraction (the one real refactor; keep the seam, de-dup helper code only).
4. Local install validation on WSL (`--dry-run` + wet), per-package `bun run check` green.
5. THEN resume the readiness units INSIDE the monorepo (Unit 1 is now the shared package, not a contract doc; Units 2-4 re-home unchanged in substance). The Mac install (Phase 2, next week) then validates the structure you KEEP.

OPEN DECISIONS for the architect-deep pass: layout (`packages/*`); keep the store-write seam (recommend yes); git clean-cut vs subtree (recommend clean-cut); fold the bridge now or later (either; lowest priority).

DESIGN GATE RESULT (2026-06-19; Step 1 DONE: architect-deep + ADR, four read-only repo scouts, AWAITING USER GO before any destructive move). The ADR is written at `docs/adr/0010-consolidate-instruments-into-a-bun-workspace-monorepo.md` (Status: proposed; supersedes ADR-0004; flip to accepted on the user's go). EXECUTION HANDOFF: `plans/monorepo-consolidation-playbook.md` carries the step-by-step (Steps 2-4), the live scout evidence (so no re-scout), the done-contracts, and the HITL gates; a fresh conversation reads ADR-0010 + that playbook and executes. The consolidation is SEQUENTIAL (shared mutable tree state; one off-thread agent per step, NOT fan-out); the readiness units fan out AFTER it lands. Resolved decisions:
- NAMING (user-confirmed 2026-06-19): drop the `regimen-` prefix; bare dir names under `packages/`, `@regimen/*` package names. The former "hub" is RENAMED to `cli` (purpose: it is now purely the `regimen` command + the locator/plan/runner subprocess orchestrator, since the program docs move to the repo root). Final layout: `packages/cli` (`@regimen/cli`, bin `regimen`), `packages/feedback` (`@regimen/feedback`), `packages/enforcement` (`@regimen/enforcement`), `packages/otlp-bridge` (`@regimen/otlp-bridge`), `packages/shared` (`@regimen/shared`). The internal `hubCloneRoot()` helper retires (rename to e.g. `cliPackageRoot()`); "hub" leaves the code.
- LAYOUT: every instrument is a package under `packages/`, INCLUDING the cli package (`packages/cli`). The repo root becomes a pure private workspace container (`workspaces: ["packages/*"]`, one `bun.lock`, one base tsconfig, relocated Husky, the program-level docs/ADRs/PRD/plans). Five packages as above.
- KEY RISK REVERSAL (refines the doc's assumption): because the cli package becomes a SIBLING under `packages/`, the parent of its own root resolves to `packages/` and the existing `src/locator.ts` convention-leg JOIN LOGIC lands on `packages/feedback` UNCHANGED. The prefix drop DOES change two string constants (conventionalDir `"regimen-feedback"`/`"regimen-enforcement"` -> `"feedback"`/`"enforcement"`) plus the locator test's temp-dir fixture names, but that is string substitution the tests catch, NOT a resolution-logic edit. The single-biggest-risk line keeps its logic intact; it becomes a re-VERIFY (dry-run + wet install) plus a constant rename. (The doc's earlier "change the join to `join(hubCloneRoot, "packages", name)`" was the cli-stays-at-root variant, rejected as higher-risk: it reworks the join itself.)
- SHARED-DATA PACKAGE (`@regimen/shared`, dir `packages/shared`; kept `shared` under the scope, alternative `@regimen/conventions` if domain flavor wanted): a pure category-1 leaf, NO ports. Members + consumer sets verified live: data-dir resolver (feedback+enforcement+bridge); `Harness`/`asHarness`/`traceIdFor` + `harness/contract.ts` (feedback+enforcement only). Enforcement's inlined copies in `denial-store.ts` become imports. Bridge keeps its private path helpers + store-row projection types. Data/behavior split survives verbatim: the `TranscriptReader`/`SessionResolver` ports + adapters stay feedback-private.
- KEEP THE STORE-WRITE SEAM: yes. Stays a doc + conformance test (open format for non-TS/external producers). De-dup HELPER code only; `RegimenEvent`/store-row shapes stay reproduced-against-the-contract, NOT a shared type. The conformance test's inline trace-id re-derivation stays as a drift guard against `@regimen/shared`'s `traceIdFor`.
- GIT HISTORY: clean-cut (working-tree copy + a small set of ordinary commits), originals archived read-only. Not subtree. Commit counts verified live: hub 39, feedback 63, bridge 17, enforcement 2.
- BRIDGE: fold it in NOW (zero TS-import coupling confirmed live; only reads feedback.db via bun:sqlite; its 3 feature branches are merged to main).
- DOES NOT CHANGE: ADR-0009 installer home (note: read its "standalone repo" trigger wording as "standalone package"); ADR-0005 open seam; ADR-0007 readers; the `skills` content repo (out of scope). tsconfig confirmed byte-identical across all four repos; Husky lives only in feedback today and relocates to the root.

## Next work queue (the readiness units; RE-HOMED behind the consolidation above)

NOW ACTIVE (2026-06-20, post-merge): the consolidation has landed, so these units run INSIDE the monorepo on `main` and ARE the current work. Execute them per the OVERNIGHT AFK PLAN in START HERE, Track A is Unit 2 then Unit 3 (chained), Track B is Unit 4 (in parallel). Unit 1 is GONE (collapsed into `@regimen/shared`). The specs below are re-homed to `packages/*`.

This section is the actionable runway. Every design call below is already made; do NOT reopen them. Knock through as many units as a conversation allows, checkpointing at each unit's done-contract.

STANDING RULES for every unit (do not re-confirm these, they are settled):
- Route the scout and the build OFF-THREAD: a fresh read-only scout of the named seams FIRST (the repo may have moved since this doc was written, so verify line numbers and shapes live, do not trust this doc's snapshot), then ONE TDD build agent per unit carrying the machine-checkable done-contract.
- TDD via the `tdd` skill: real red-green, stub-before-red, minimal-green; the existing suite stays green throughout (these are refactors over green suites).
- Harness names live ONLY in descriptor/contract/marker data, never in a generic code path. No em dash (U+2014) anywhere.
- Commit incrementally to the unit's branch (single-line conventional commits, no Co-Authored-By/Generated-with footers). Bring each unit to `<check>` green PLUS its new tests, then run CodeRabbit (`coderabbit:code-review`) on the EXACT final commits of that unit and clear findings (verify each finding against the code before "fixing", the last review's single finding was a false positive). Then STOP and present for the push/merge go. NEVER auto-push or auto-merge; a push needs an explicit "push".
- The driver doc (this file) is a living working-tree file: update the unit's status here in place as you finish, do NOT commit it.

PATHS: ONE repo at `/home/mlo/dev/niftymonkey/regimen`, work on `main`. Code at `packages/{cli,feedback,enforcement,otlp-bridge,shared}`. Each package's check is `bun run check`; run it IN ISOLATION (`cd packages/<x> && bun run check`), never the workspace `--filter '*'` under load (overnight guardrail 2). The old sibling repos still exist read-only pending archival; do NOT work in them.

DONE ALREADY (do not redo): the feedback A.0 + env-driven refactor is complete and MERGED (rode in via PR #38). Its code is at `packages/feedback/src/harness/` (descriptor, ports, `support` registry) + `packages/shared/src/harness/contract.ts` (the shared contract DATA). Units 2 (enforcement) + 3 (cli) complete A.0; Unit 4 is A.1 Claude.

### Unit 1, enforcement contract DOC: GONE (collapsed by the consolidation, do NOT do it)
- It existed only to keep two byte-identical copies of the shared harness DATA in sync across separate repos. In the monorepo, `packages/shared/src/harness/contract.ts` IS the single source: feedback imports it, and enforcement (Unit 2) now IMPORTS it too instead of reproducing it. Nothing to author. Skip.

### Unit 2, enforcement env-driven refactor (`packages/enforcement`): TRACK A, step 1
- DONE 2026-06-20 (branch `a0-enforcement`, commit `0b4a9a6419`, pushed, CodeRabbit-clean, isolated check 62/0, UNMERGED). See the OVERNIGHT RUN COMPLETE block in START HERE. Spec retained below for reference.
- GOAL: bring enforcement to the same vendor-agnostic, env-driven shape feedback now has, and parametrize the Codex-named gate seams off the SHARED contract.
- BRANCH: a feature branch off `main` in a worktree, e.g. `a0-enforcement`.
- SCOUT FIRST (verify live in `packages/enforcement`): `rm-rf-gate-codex.ts`, `rm-rf-gate.ts`, `planGateHooks`, `resolveCodexHome`, `src/install/`, and how the gates get/set `REGIMEN_HARNESS` (the shell gates already read it). Note `denial-store.ts` already imports `asHarness`/`traceIdFor`/`resolveDataDir`/`dataDir` from `@regimen/shared` (done in the consolidation), so the pattern is established.
- STEPS: (a) IMPORT the contract from `@regimen/shared` (`harnessContract`/`HARNESS_CONTRACTS` + types) directly, NO reproduced byte-identical copy (the consolidation removed that duplication, this is the payoff; the old "reproduce from the Unit 1 doc" step is obsolete). (b) Fold `rm-rf-gate-codex.ts` + `rm-rf-gate.ts` into ONE parameterized gate that reads `REGIMEN_HARNESS`. (c) Parametrize `planGateHooks` to source event/command/target from the contract. (d) `resolveCodexHome` to `resolveHarnessHome(contract, env, home)` reading `contract.configHome.envVar`. (e) Drop `--harness`/`--codex-home` flags, env-driven, keep `--dry-run`.
- PINNED DECISION (do not deliberate): enforcement resolves the harness from `REGIMEN_HARNESS` ONLY and fails closed if unset; NO env-marker detection, NO marker map in enforcement (those stay feedback-private; enforcement is always invoked with `REGIMEN_HARNESS` set by the cli or the gate command). Keeps enforcement's copy minimal.
- DONE-CONTRACT: `packages/enforcement` check green (isolated) PLUS new tests for the parameterized gate reading `REGIMEN_HARNESS` and the fail-closed (unset) path; enforcement consumes the contract by IMPORT from `@regimen/shared` (single source, no copy). TDD. Then CodeRabbit-clean on the exact commits, STOP at the merge gate.

### Unit 3, cli forward leg (`packages/cli`): TRACK A, step 2 (STACKED on Unit 2's branch)
- DONE 2026-06-20 (branch `a0-cli-forward` stacked off `a0-enforcement`, commit `ee6c138144`, pushed, CodeRabbit-clean, isolated check 29/0, UNMERGED). COMPLETES A.0. The config-home var rides the existing `process.env` forwarding (no `@regimen/shared` import in the cli); only `REGIMEN_HARNESS` is added to the child env overlay. See the OVERNIGHT RUN COMPLETE block. Spec retained below.
- GOAL: the cli stops forwarding `--codex-home`/`--harness` and instead sets `REGIMEN_HARNESS` and the harness's config-home env var in the CHILD process env when it spawns feedback/enforcement; the cli keeps importing ZERO instrument internals (harness travels as an opaque string via env, not flags).
- BRANCH: stacked on Unit 2's branch (e.g. `a0-cli-forward` off `a0-enforcement`), because the cli can only stop forwarding the flags once enforcement READS the env (Unit 2). Build it after Unit 2 is green.
- SCOUT FIRST (in `packages/cli`): `src/locator.ts`, `src/plan.ts`, `src/cli/index.ts` (it still parses `--codex-home` at ~line 68 and forwards it, this is the deferred code to remove), `src/runner.ts` (NOTE: `realSpawn` already forwards `process.env` from the consolidation, so setting env on the child is wired, the cli just needs to populate `REGIMEN_HARNESS` + config-home).
- DONE-CONTRACT: `packages/cli` check green (isolated) PLUS tests that the spawned child env carries `REGIMEN_HARNESS` (and the config-home var) and that no `--codex-home`/`--harness` flag is forwarded. TDD. This COMPLETES A.0. CodeRabbit-clean, STOP at the merge gate.

### Unit 4, A.1 Claude bring-up (`packages/feedback`): TRACK B (parallel, independent)
- DONE 2026-06-20 (branch `a1-claude` off `main`, commits `749fa727e2` + `4d38a3fe02`, pushed, isolated check feedback 424/0 + shared 1/0, CodeRabbit 1 finding verified+dismissed, UNMERGED). Producer-first reader confirmed against a LIVE Claude transcript; Decision 4 reader-type extraction landed; end-to-end assess/evidence work for `REGIMEN_HARNESS=claude`. A.2 Copilot / A.3 Gemini depend on the extracted `reader-types.ts` here. See the OVERNIGHT RUN COMPLETE block. Spec retained below.
- INDEPENDENCE: feedback-only, does NOT depend on Units 2-3; runs IN PARALLEL with Track A overnight. Own branch off `main` in a worktree, e.g. `a1-claude`.
- THE ONE DEEP UNIT: capture/install/skill are mechanical swaps, but the judge-time TRANSCRIPT READER is real format work (the load-bearing risk). PRODUCER-FIRST: read a REAL `~/.claude` transcript first (this box runs Claude Code; sessions at `~/.claude/projects/<slug>/*.jsonl`, including the conversation that launches this work), CONFIRM the on-disk format, THEN build the reader. TDD it over a real captured fixture so a misread fails the test.
- STEPS: (a) Claude `TranscriptReader` behind the existing port producing the same v1 events + content chunks from Claude's format; (b) Claude `SessionResolver` taking the session id DIRECTLY (`--session` / `CLAUDE_CODE_SESSION_ID`), no per-cwd stamp, no newest-scan; (c) register `claude` in the support registry (`packages/feedback/src/harness/support.ts`) with its contract row (config home `~/.claude` + its env var, skills subdir) and capture descriptor; (d) installer wiring for Claude's config home and skill target. Reuse: `packages/feedback/hooks/capture.ts` (already stamps `claude`), `translateClaude`, the reference gate.
- DECISION 4 LANDS HERE: extract the reader types (`RolloutReadResult`/`RolloutReadOptions`/`ContentChunk`/`AnchorRef`) out of the codex reader into a harness-neutral module now that a second real reader makes the codex-named home misleading. Do this cleanly: Copilot (A.2) and Gemini (A.3) depend on this extracted module.
- DONE-CONTRACT: `packages/feedback` check green (isolated) PLUS Claude reader tests over a real fixture PLUS `claude` resolving end-to-end (evidence/assess select the Claude reader). TDD. CodeRabbit-clean, STOP at the merge gate.

### Unit 5, unify harness resolution in `@regimen/shared` (fixes the rm-rf `claude` mislabel): DONE + MERGED
- STATUS (2026-06-20): DONE + MERGED to `main` (commit `cc98e482f4`, fast-forwarded, pushed; branch/worktree cleaned up). Shared owns `resolveHarnessFromEnvironment` + `HARNESS_ENV_MARKERS` (`packages/shared/src/harness/resolve.ts`); feedback consumes them (local copies deleted, behavior unchanged); enforcement detects-at-install via the shared resolver and bakes `REGIMEN_HARNESS` into EVERY gate command incl rm-rf; the rm-rf gate dropped the `?? "claude"` fallback (blocks unconditionally, records telemetry only when the harness is present). All packages green in isolation (shared 11, feedback 394, enforcement 62, cli 29, bridge 84), CodeRabbit 0 findings. THE rm-rf `claude` MISLABEL IS FIXED. Two follow-ups surfaced (NOT blocking, see below).
- FOLLOW-UP A: DONE + MERGED (commit `facd49ca29`). Enforcement gate install now resolves the target from `contract.hooksFile.relativePath` (Claude `settings.json`, Codex `hooks.json`); the existing planner's `structuredClone`+mutate-only-`hooks` merge already preserves co-hosted `permissions`/`env` + capture/user leaves (only the hardcoded filename was the bug). Tests: codex unchanged, claude `settings.json` merge-preserve, uninstall. Enforcement install is now correct for all four harnesses.
- FOLLOW-UP B: DONE + MERGED (commit `601a409595`). The two shell gates dropped `${REGIMEN_HARNESS:-claude}`: they always block (exit 2) and record telemetry only when `REGIMEN_HARNESS` is set. No `claude` default remains anywhere in enforcement.
- WHY: surfaced 2026-06-20 reviewing the overnight enforcement work. The `rm-rf` gate's installed command bakes NO harness (unlike the shell gates, which bake `REGIMEN_HARNESS=<harness>`), so at runtime it reads `process.env.REGIMEN_HARNESS ?? "claude"` and stamps a non-claude user's denials as `claude`; and enforcement install fails closed / the cli only copies `REGIMEN_HARNESS` (no detection), so a Codex-only user running `regimen install` without setting it gets mislabel or fail-closed. Feedback is FINE (it already detects: explicit override, else CLI-set marker, else fail closed). Root cause: the A.0 premise "enforcement is always handed `REGIMEN_HARNESS`" is false in practice and wrong for the up-to-4-concurrent-harness machine, where per-invocation detection is the only correct model ([[regimen-multi-harness-per-machine]]).
- DECISION: architect-deep 2026-06-20, user chose "detect everywhere (shared)". One vendor-agnostic resolution policy, no hardcoded harness default anywhere.
- MOVE (the new shared leaf): `resolveHarnessFromEnvironment(env) -> Harness | undefined` + the `HARNESS_ENV_MARKERS` table from `packages/feedback/src/harness/{support,descriptor}.ts` into `@regimen/shared` beside `contract.ts` (pure category-1 fn + data, no port; env passed in, tests feed fixed env objects). Feedback imports them and deletes its copies (behavior identical; the `harnessSupport` registry stays feedback-private behavior).
- ENFORCEMENT: install resolves the harness via the shared resolver (DETECT, not `REGIMEN_HARNESS`-or-fail), then bakes `REGIMEN_HARNESS=<resolved>` into EVERY gate command incl. rm-rf (gate-commands.ts: the rm-rf builder gains the bake the shell gates already have). `rm-rf-gate.ts` drops `?? "claude"`: reads the baked value, BLOCKS unconditionally, emits telemetry only when a harness is present. Install fails closed with a clear "set `REGIMEN_HARNESS`" error when undetermined (the undetermined case now exists ONLY at install, a human-facing moment, never at runtime, so NO `"unknown"` harness value and NO nullable-harness schema change, ADR-0008 untouched).
- REOPENS (refines A.0 decisions 1+2 / the env-driven refactor): markers move feedback-private -> shared; enforcement DETECTS at INSTALL only (runtime gate stays dumb, reads the baked value). The cli is UNCHANGED from Unit 3 (`REGIMEN_HARNESS` pass-through as an explicit override; no `@regimen/shared` dep added to the cli).
- DONE-CONTRACT: feedback + enforcement + shared checks green (isolated) PLUS tests: shared resolver (override wins / throws-on-unknown / marker-detect / undetermined), enforcement install detects+bakes for codex AND claude, the rm-rf gate reads the baked harness (no claude fallback) and blocks-always. TDD. CodeRabbit-clean on exact commits, STOP at merge gate.
- LANDING: a fresh branch off the MERGED `main` (after `a0-enforcement` + `a0-cli-forward` + `a1-claude` merge), because it touches the same harness files those branches touch. Brief window where `main` carries the rm-rf mislabel between those merges and this unit: acceptable (no external users, gates still block, feedback still attributes correctly).

## Phase 0: install all four locally and read what they actually write (do this first)

Before building a reader or installer for any new harness, install all four on this WSL2 dev box and look at what each one actually creates on disk, the same on-disk forensics that grounded the Codex push (its Phase 0 read the real `~/.codex` topology rather than trusting docs). Claude and Codex are already installed here; this adds Gemini CLI and Copilot CLI, so it is two installs plus one real session each. Per harness, capture the verified truth and fold it into the harness descriptor: the config home and its env override, the hooks-config file and format, a real hook payload shape on stdin, the transcript path and on-disk format (and the session id), the skill/context install target, and the pre-tool deny mechanism. This is what turns the documented surfaces (which already look low-lift and Claude-Code-shaped) into facts, and it directly closes the two unknowns the doc research could not settle: Gemini's exact transcript JSON field names and Copilot's `events.jsonl` event-log shape. The WSL2 box is for mechanics only; the corporate trial machine(s) may differ, exactly as this dev box differed from the Codex Mac, so treat anything machine-specific as reconfirmed-on-the-real-machine.

STATUS (2026-06-17): both new CLIs installed on this WSL2 box, `gemini` 0.47.0 and `copilot` 1.0.63, under the nvm Node v22.18.0 PATH (same default-node caveat as the Codex CLI: missing from a fresh default-node shell). Forensic surprise worth carrying to the trial machine: both config homes already existed BEFORE the CLI install, `~/.gemini` from Google Antigravity (it has an `antigravity/` dir and is already OAuth-authenticated) and `~/.copilot` from the VS Code Copilot integration (an `ide/` dir), so on this box the home is shared across surfaces the same way `~/.codex` is shared by CLI and app on the Mac. The cross-tool `~/.agents/skills/` dir already exists and is populated (26 skills), which is directly the skill-install seam's target. Read-only forensic sweep of the real on-disk topology (and any existing transcripts) routed off-thread.

PHASE 0 FINDINGS (2026-06-17, read-only forensics; secrets and message content redacted):
- GEMINI transcript format is CAPTURED from 3 existing Antigravity sessions at `~/.gemini/tmp/<project-alias>/chats/session-*.jsonl`. Important nuance the docs did not surface: the file is an EVENT-SOURCED state-patch JSONL, not a plain append of complete messages. Line 0 is a session-init object `{sessionId, projectHash, startTime, lastUpdated, kind}`; later lines are EITHER a message object OR a `$set` mutation (for example `{"$set": {"messages": [...], "lastUpdated": ...}}`). A message is `{id, timestamp, type: "user"|"gemini", content: [{text}], and on gemini turns thoughts[], tokens{}, model}`. So the Gemini reader must collapse the `$set` patches (or extract message objects directly) rather than treat each line as one event, and must exclude `thoughts[]` (reasoning) from judge content the way the Codex reader excludes chain-of-thought. Model observed: `gemini-3-flash-preview`. CONFIRMED 2026-06-17 by a headless `gemini --skip-trust -p` probe: the standalone CLI writes the IDENTICAL `$set` structure under `~/.gemini/tmp/<cwd-alias>/chats/`, and one trivial turn produced a 101K file (each `$set` re-embeds the full `messages` array, so the reader takes the LATEST snapshot, never folds line-by-line). Headless runs require `--skip-trust` or `GEMINI_CLI_TRUST_WORKSPACE=true` (a trust gate worth noting for any automated capture). TOOL REPRESENTATION RESOLVED 2026-06-17 from a real ~10-tool-call agentic session: records append as top-level lines with a `type` discriminator (`user`, `gemini`, and a new `info` type seen), interleaved with `$set` patches (mostly `lastUpdated`, occasionally a `messages` snapshot), so the reader must handle BOTH append-style message lines AND `$set` mutations. `content` is POLYMORPHIC: a plain string (most `gemini` turns here) OR an array of parts. Tool RESULTS appear as `type:"user"` messages with `content:[{functionResponse:{id,name,response}}]`, and one user message can BATCH several (10 messages carried 13 results, so parallel calls group), so tool name and output are recoverable. NOTABLE GAP: the structured tool CALL (a `functionCall` part with args) was NOT persisted in this chat log, only the model's prose plus the `functionResponse` round-trip, so tool-call ARGUMENTS are not derivable from the transcript alone (the hook/`BeforeTool` path carries args, an argument that Gemini capture needs the hook for full tool fidelity, with the transcript reader as the reduced-fidelity judge-time fallback). The earlier 101K-for-one-turn was injected context plus large `response` payloads, NOT snapshot duplication. CAPTURE-EDGE (hook firing) PARTIALLY PROBED 2026-06-17: hooks ARE a first-class feature on v0.47 (a `gemini hooks` command exists, including `gemini hooks migrate` from Claude Code), and enforcement has dedicated surfaces (`--approval-mode default|auto_edit|yolo|plan`, plus `--policy`/`--admin-policy` files, the Gemini enforcement seam). A headless `SessionStart` probe via an isolated `GEMINI_CONFIG_DIR` did NOT fire the hook, but that is INCONCLUSIVE: `GEMINI_CONFIG_DIR` appears NOT honored on v0.47 (no `tmp/` was written into the isolated dir, so gemini fell back to the real `~/.gemini`, which has no hooks, and never loaded the probe hook). SCHEMA NOW CONFIRMED from the docs and example bundled IN the installed package (`bundle/docs/hooks/reference.md`, `bundle/examples/hooks/`): hooks live in `settings.json` under a `hooks` object in the NESTED form `hooks.<Event> = [ { matcher?, sequential?, hooks: [ { type: "command", command, name?, timeout? } ] } ]`, NOT the flat `[{matcher,type,command}]` my first probe used (which is a second reason it never fired, on top of the config-dir issue). The v0.47 event taxonomy is `SessionStart`, `SessionEnd`, `BeforeAgent`, `AfterAgent`, `BeforeModel`, `AfterModel`, `BeforeToolSelection`, `BeforeTool`, `AfterTool`, `PreCompress`, `Notification`, `Stop` (Gemini-native names, not Claude's `Pre/PostToolUse`). The stdin payload is `{session_id, transcript_path, cwd, hook_event_name, timestamp}` plus `tool_name`/`tool_input` on `BeforeTool` and `tool_response` on `AfterTool`, essentially the SAME shape as Codex and Claude, so the Gemini capture hook plus translator is a near-trivial port. `BeforeTool.tool_input` carries the tool-call ARGS that the transcript drops, confirming hooks are the fidelity path for Gemini tool detail. ENFORCEMENT seam confirmed: a `BeforeTool` hook returns `{"decision":"deny","reason":...}` or exits 2 (stderr as the reason), alongside the `--approval-mode` and `--policy`/`--admin-policy` surfaces. STILL OPEN, the only remaining Phase 0 item: confirm a configured hook actually FIRES on this install. An automated re-run was denied, so this is a HITL step, a user-run session with the corrected nested-schema hook in a scratch project `.gemini/settings.json`. Do NOT mutate the user's authenticated `~/.gemini/settings.json`. UPDATE 2026-06-17: a user-run `gemini` session was tried but launched from the repo cwd with normal config, so the scratch hook was out of scope and `/tmp/gemini-hook-test.log` was never written; firing is still unconfirmed. This item may also RELOCATE, see the harness-target risk below: if the Gemini target becomes Antigravity CLI, the hook-firing check moves to Antigravity's surface. SCOPE/TRUST RESOLVED from the bundled `docs/hooks/index.md`: project `.gemini/settings.json` hooks ARE supported (precedence project then user then system then extensions), but Gemini FINGERPRINTS project hooks and treats a new or changed one as UNTRUSTED, warning before it executes, so a fresh scratch-project hook does NOT fire silently in a headless `-p` run (that, plus the scratch `settings.json` not actually being present on the manual attempt, is why no marker was ever written). The trusted install target is USER scope `~/.gemini/settings.json` (the analog of installing into `~/.codex`); hooks are also managed via `/hooks panel` and `/hooks enable <name>`, and run with a sanitized env exposing `GEMINI_SESSION_ID`, `GEMINI_CWD`, `GEMINI_PROJECT_DIR` (plus a `CLAUDE_PROJECT_DIR` compatibility alias). CONCLUSION (stop iterating): the hook MECHANISM is fully confirmed from the in-package docs (schema, all 11 events, payload, enforcement, trust model, management commands), so empirical fire-on-THIS-install is DEFERRED to install time on the actual Gemini target rather than chased further, since the install path is user-scope and the Gemini target itself is unsettled pending the June 18 sunset decision.
- SKILL-INSTALL SEAM SIMPLIFICATION: `~/.agents/skills/` is the real cross-tool skills dir (24 skills), and `~/.gemini/skills/`, `~/.copilot/skills/`, and `~/.gemini/antigravity/global_skills/` are all SYMLINKS into it. So the harness-agnostic skill installer can target `~/.agents/skills/` once and be picked up by Gemini, Copilot, and Antigravity through the existing convention, instead of copying per harness. Confirm Claude and Codex honor the same `~/.agents/skills/` path before treating it as universal. PRECEDENCE confirmed 2026-06-17: when a skill exists in both `~/.agents/skills/` and `~/.gemini/skills/`, Gemini loads the `~/.agents/skills/` copy and logs a `Skill conflict detected` warning, so `~/.agents/skills/` is the canonical write target and a dual-install only adds noise.
- COPILOT transcript is CAPTURED and is the CLEANEST of the four (verified 2026-06-17 from a real `client_name: github/cli` session). `~/.copilot/session-state/<session-id>/events.jsonl` is plain one-event-per-line JSONL with a uniform envelope `{type, id, timestamp, parentId, data}` and a rich, judge-time-complete taxonomy: `session.start` (carries cwd/gitRoot/branch/commit), `user.message`, `assistant.message` (model, `toolRequests[]`, encrypted `reasoningOpaque`, tokens), `assistant.turn_start`/`turn_end`, `tool.execution_start`/`tool.execution_complete` (paired by `toolCallId`, with a `success` boolean and `result`), `permission.requested`/`permission.completed` (an `approved`/`denied`/`ask_later` audit trail), `hook.start`/`hook.end`, `system.message`, `session.model_change`, and `session.shutdown` (token and cost metrics plus `codeChanges.{linesAdded,linesRemoved,filesModified}`). Implications: (a) the Copilot reader is LOW risk, a straight line-by-line fold, no `$set` collapse and no rollout-vs-event dedup; (b) reasoning is ENCRYPTED at rest (`reasoningOpaque`), so chain-of-thought is auto-excluded from judge content with no special handling; (c) ENFORCEMENT is first-class in the transcript, the `permission.*` events are a ready-made deny signal and `permissions-config.json` is the path-scoped and command-identifier allow-deny config (with fail-closed `preToolUse`/`permissionRequest` hooks per the docs); (d) `~/.copilot/session-store.db` (sessions, turns, checkpoints, trajectory) is a convenience index, NOT the source of truth, treat `events.jsonl` as canonical, the same call made for Codex's `state_5` sqlite. Auth rides the `gh` GitHub credentials (logged in as `niftymonkey`). Config home `~/.copilot` (override `COPILOT_HOME`); custom hooks (none configured yet) go under `~/.copilot/hooks/` or a `settings.json`. Skills symlink into `~/.agents/skills/` like Gemini's.
- SYNTHESIS: the transcript-reader risk is UNEVEN, not uniform. Copilot is the easiest (clean event log), Gemini is the trickier of the two new ones (the `$set` snapshot fold), Codex is done, and Claude still needs its reader built. The capture, enforcement, and skill-install edges are confirmed present and Claude-Code-shaped on both new harnesses. The one unproven capture item is whether a configured hook actually FIRES on this Gemini build (the next non-invasive Phase 0 probe).
- HARNESS-TARGET RISK (surfaced 2026-06-17 from a live `gemini` startup banner, TIME-SENSITIVE): the `gemini` CLI now prints "Gemini CLI will stop serving requests to Google One and unpaid tiers in 1 day on June 18. Please migrate to Antigravity CLI" (the dev box's plan is "Gemini Code Assist for individuals", an affected tier), and Google steers users to ANTIGRAVITY CLI (`curl -fsSL https://antigravity.google/cli/install.sh | bash`). This reframes the "Gemini" harness: the trial target may be Antigravity CLI (or a paid Gemini Code Assist / Vertex tier), not the free `gemini` CLI, which may stop serving on this box 2026-06-18. MUCH OF THE GEMINI PHASE 0 TRANSFERS regardless: Antigravity already shares `~/.gemini` (the `antigravity/` dir and the original transcript samples predate the `gemini` CLI install), so the `$set` transcript reader and the `~/.agents/skills` seam carry over; what needs ITS OWN verification is Antigravity CLI's hooks and enforcement surface and whether it writes the same `tmp/<project>/chats` transcripts. OPEN QUESTION for the user: which Gemini surface is the company actually evaluating? That decides the Gemini integration target, and it is time-sensitive given the June 18 sunset. DECISION (2026-06-17, user): for THIS iteration the Gemini target is the regular `gemini` CLI (the user is fairly sure that is what the company runs); Antigravity is DEFERRED to a future iteration and held as the LOWEST-priority open question until the user inspects the work install tomorrow. The user also notes the company may be pinned to an OLDER gemini version for VPN compatibility and may have to stay on it, so Antigravity adoption is uncertain even after the sunset. See the work-machine forensic checklist and version-pin constraint in Phase 0b below. RESOLVED 2026-06-18 (user): the company is NOT using Antigravity in any near-term horizon, and the work VDI has no `antigravity/` dir, so the Gemini target is DEFINITIVELY the regular `gemini` CLI for this whole effort and Antigravity is DROPPED from scope (not merely deferred). The work-VDI forensics (Phase 0b) further confirm that CLI is the current 0.47.0 with hooks, which closes the harness-target risk.

## Phase 0b: work-machine Gemini forensics (run-and-paste, for the actual trial install)

CONSTRAINT (2026-06-17, user): the company likely PINS AN OLDER `gemini` CLI version that works inside its VPN, and may have to stay on it, so the dev box (v0.47.0) may be AHEAD of the trial machine, the inverse of the Codex dev-box-behind-Mac situation. The Gemini integration must target the TRIAL build's actual capabilities: an older build could write a different `tmp/<project>/chats` shape or lack hooks entirely, in which case Gemini capture falls back to a transcript TAILER (the Codex-rollout-tailer analog) instead of hooks. The June 18 Antigravity sunset may not even apply under the VPN and version pin, so treat it as a WATCH item, not a forcing function for this iteration. PLATFORM NOTE (2026-06-17, user): the work machine being probed THIS week is a WINDOWS VDI driven via PowerShell (not bash), and the actual Mac trial install is PHASE 2 (next week), so this week is Windows-VDI recon. The run-and-paste checklist therefore exists in BOTH PowerShell (for the VDI now) and bash (for the Mac), hosted at md.niftymonkey.dev `/v/KoX46TJT`. Windows is thus a real Gemini topology to support (`%USERPROFILE%\.gemini`, Windows paths, no POSIX shell), consistent with Regimen's native-Windows-first-class goal; the bash block below is the Mac/Linux variant. EMPIRICAL VALIDATION (2026-06-17, the forensic `.ps1` was run from WSL2 against the Windows host's PowerShell 5.1 AND 7.5, both pass): three findings. (1) The Windows host runs a far OLDER gemini, `0.1.12` (WSL2 has `0.47.0`), and `0.1.12` has NO `hooks` subcommand (`gemini hooks` just reprints general help), proving live that an older pinned Gemini has no hook capture seam, so capture there must use the transcript tailer, not hooks. (2) The Windows gemini is a `.ps1` shim (`C:\nvm4w\nodejs\gemini.ps1`), so PowerShell 5.1 BLOCKS it under the default Restricted ExecutionPolicy (`SecurityError`); it runs only via `-ExecutionPolicy Bypass` or under PowerShell 7, so the forensic script is launched `powershell -ExecutionPolicy Bypass -File ...`. (3) The Windows host has its OWN `~/.gemini` (dual-home with WSL2, the Codex pattern again), and the 0.1.12 help exposes `--telemetry-otlp-endpoint`, a built-in OTLP path worth noting for the bridge. The validated `.ps1` is hosted at `/v/KoX46TJT`.

WORK-VDI FORENSICS RECEIVED (2026-06-18, run-and-paste output saved as `investigation.txt` in the hub working tree; the actual corporate Windows VDI, user `p006441J`, PowerShell 5.1). HEADLINE REVERSAL of the version-pin worry: the work VDI runs gemini 0.47.0, the SAME current build as the WSL2 dev box, NOT the old 0.1.12 that the dev-box's OWN Windows side carried (those were two different Windows hosts; the trial VDI is current). So the trial machine HAS the `gemini hooks` subcommand (and `gemini hooks migrate` from Claude Code) plus the full enforcement surface (`--approval-mode default|auto_edit|yolo|plan`, `--policy`/`--admin-policy`, the Policy Engine that deprecates `--allowed-tools`), which means GEMINI CAPTURE ON THE TRIAL MACHINE USES HOOKS, the Claude-Code-shaped path, NOT a transcript tailer. The version-pin/older-build risk is therefore NOT in effect on this machine (keep it only as a watch item should a VPN pin later force a downgrade). TRANSCRIPT FORMAT CONFIRMED on the real machine: `%USERPROFILE%\.gemini\tmp\<project-alias>\chats\session-<ISO>-<hex>.jsonl` (here `...\.gemini\tmp\p006441j\chats\session-2026-06-18T15-33-7e909a83.jsonl`, a real ~525KB session), the same `$set`-snapshot shape the dev-box reader already handles, so the Gemini judge-time reader carries over unchanged. CONFIG HOME = `%USERPROFILE%\.gemini` (`GEMINI_CONFIG_DIR` and `GEMINI_CLI_HOME` both unset), native Windows paths, OAuth-authenticated (`oauth_creds.json`); the bundled hook docs/examples ship (`bundle/docs/hooks/{index,reference,writing-hooks,best-practices}.md`, `bundle/examples/hooks/`). No `antigravity/` dir on the VDI (`antigravity present: False`). REMAINING SMALL GAPS in this paste: the skills wiring (`~/.gemini/skills` vs the cross-tool `~/.agents/skills`) on Windows was not captured (the output jumped to the bundle docs), and the startup banner (tier/sunset) was not pasted (moot now that Antigravity is ruled out). NET: Phase 0b is answered for the load-bearing questions; Gemini on the trial machine is the low-lift hooks + confirmed-transcript path, and Decision 4's no-capture-tailer call holds firmer because the trial build has hooks.

The dev box cannot tell us what the corporate machine runs. Run these READ-ONLY commands on the work machine and paste the output back (none print secrets; tier and plan come from the startup banner). The `2>&1` on the help and hooks lines is intentional: on an older build the error itself is the signal (the subcommand may not exist).

```
# 1. Version and surface (regular gemini CLI vs Antigravity, and which build)
gemini --version
ls -la ~/.gemini/antigravity

# 2. Capability probe: does THIS build support hooks and enforcement
gemini --help 2>&1 | grep -niE 'hook|approval|policy|yolo|config|home|trust|dir'
gemini hooks --help 2>&1 | head -20

# 3. Config home, overrides, and layout
echo "GEMINI_CONFIG_DIR=$GEMINI_CONFIG_DIR GEMINI_CLI_HOME=$GEMINI_CLI_HOME"
ls -la ~/.gemini

# 4. Transcript location and structure-only shape of the newest session (no content printed)
ls -lat ~/.gemini/tmp/*/chats/ | head
F=$(ls -t ~/.gemini/tmp/*/chats/session-*.jsonl | head -1); echo "$F"; jq -c 'if has("$set") then {set_keys:(.["$set"]|keys)} elif has("type") then {type, content_kind:(.content|type)} else keys end' "$F" | sort | uniq -c

# 5. Skills wiring and the bundled hook docs (confirms the feature exists on this build)
ls -la ~/.gemini/skills ~/.agents/skills
ls "$(npm root -g)/@google/gemini-cli/bundle/docs/hooks" "$(npm root -g)/@google/gemini-cli/bundle/examples/hooks"
```

Also paste the `gemini` STARTUP BANNER (the plan/tier line and any deprecation or migration notice): it tells us the tier and whether the sunset applies to your setup. What each block answers: (1) which Gemini and which version, (2) whether the capture and Enforcement seams (hooks, approval/policy) exist on that build, (3) the config home to install into, (4) whether the transcript reader we verified on v0.47 matches the trial build, (5) the skill-install target and feature presence. If command 4 prints nothing or errors, that build may not persist `tmp/chats` transcripts, which itself decides capture strategy.

## Workstream A: Breadth - the three-pillar installer and capture/enforce edges for all four harnesses

Goal: `regimen install --harness <name>` stands up Feedback (capture + judge), Guidance (skills), and Enforcement (gates) for any of the four. Most of this is mechanical given the existing Codex implementation, with one genuinely deep exception called out below.

**The one hard part (the load-bearing risk): the judge-time transcript reader.** Capture wiring, translation, install, enforcement, and skill install are all shallow path/name/event swaps once a harness descriptor exists. The transcript reader is not: each harness stores sessions in a different on-disk format, and `src/loader/rollout/codex-reader.ts` is a hardwired fold over Codex rollout JSONL with no abstraction, imported by name in `src/judged/assess.ts`. Every harness past Codex needs a net-new reader that produces the same v1 events plus judge-time content chunks from a different format, behind an interface that does not exist yet. This is real format-comprehension work per harness and it gates the judge for that harness, so it dominates the effort. Treat it as the first-class deliverable of each harness bring-up, not an afterthought.

**A.0 Harness-parametric refactor (do first; unblocks every harness).** Introduce a single harness descriptor/registry that maps each harness to: config home (env var plus default dir), hooks-config file path and format, the capture event-name set, the capture producer path, the skill target convention, and the session-resolution strategy. Then thread it through the six Codex-named seams so they take a harness rather than hardcoding it:
1. `resolveCodexHome` becomes `resolveHarnessHome(harness, ...)` in both feedback and enforcement; replace `--codex-home` with `--harness <name>` (home becomes derivable, with an explicit override).
2. `planCodexHooks` becomes `planCaptureHooks(harness, ...)`; `planGateHooks` takes a harness so events, producer command, and target file come from the descriptor.
3. Extract a `TranscriptReader` and `SessionResolver` port; make the Codex reader plus `src/codex/*` the Codex implementation behind it; change `src/judged/assess.ts` to resolve the reader/locator by harness instead of importing Codex by name. Generalize the `dataDir/codex/sessions` stamp path to `dataDir/<harness>/sessions`.
4. Gate command builders set `REGIMEN_HARNESS` from the descriptor, not the literal `codex`; fold the `rm-rf-gate-codex.ts` / `rm-rf-gate.ts` split into one parameterized gate.
5. `feedback install` and `installSkill` route through the descriptor; the hub forwards `--harness` instead of `--codex-home`.

This is a refactor of working, tested code, so it is TDD over an existing green suite, not a redesign. Use `architect-deep` only for the descriptor boundary itself.

OPEN DESIGN QUESTION (the FIRST thing the architect-deep pass must resolve, before any build): WHERE does the harness descriptor/registry live, and how do the repos consume it? Options: (a) a HUB-OWNED shared module that `regimen-feedback` and `regimen-enforcement` both import (one canonical source, but a real cross-repo dependency); (b) DUPLICATED per repo (no shared dep, but the capture edge and the enforcement edge can drift); (c) a PUBLISHED shared package (clean isolation, but adds release machinery). The descriptor is the central new artifact of the whole multi-harness effort and it spans all three repos (feedback threads it through capture wiring, the reader, and session resolution; enforcement through gate wiring; the hub forwards `--harness`), so this ownership decision is load-bearing and gates the refactor. Per ADR-0004 (repo boundaries) and ADR-0009 (the hub owns program-level cross-cutting concerns), a hub-owned descriptor is the likely answer, but architect-deep should weigh it against the cross-repo-coupling cost first. The REPO SEQUENCING for the refactor (which repo's change lands first) falls out of this decision and is the explicit first confirmation gate with the user.

**A.0 RESOLVED DESIGN (2026-06-18; architect-deep plus three read-only code scouts over feedback, enforcement, and the hub. Design only, no builds, awaiting the confirmation gate.)**

DECISION 1, descriptor ownership (the load-bearing first question): per-repo descriptors, NOT a hub-owned shared module and NOT a published package. The scouts overturned the earlier "hub-owned is likely" guess on the ADRs' own terms. The hub is a pure subprocess orchestrator that imports zero instrument internals and forwards `--harness <name>` as an opaque string, so it consumes no descriptor and has nothing to own and import; ADR-0009 explicitly forbids a dependency edge from the program root onto an instrument, which rules out option (a). There is no workspace, monorepo, or publish setup across the four repos (all Bun, all private, fully independent), so option (c) would add real release machinery and couple the repos against ADR-0004's independence principle. Feedback and Enforcement ALREADY duplicate the `Harness` enum, `asHarness`, `traceIdFor`, and the data-dir resolver on purpose, reconciled by the published `store-write-contract.md`. The descriptor's shared surface is the same kind of small frozen data, so it follows the same proven pattern: each repo owns its descriptor, and a new published "harness descriptor contract" freezes the shared subset both repos must reproduce identically.

DECISION 2, the data/behavior split (this is what makes Decision 1 clean rather than a compromise). The "descriptor" is two materially different things with two different owners. First, a pure-DATA descriptor of static facts: config-home env var plus default dir, the shared hooks-file path and its nested format, the skills subdir, plus the role-specific event set, producer command, and leaf marker. This is reproducible as frozen data in a contract doc and in both repos, and it is exactly the cross-repo shared surface. Second, feedback-private BEHAVIOR: the transcript reader, the session resolver, and the translator, which are functions that never cross a repo boundary and that Enforcement never needs. The in-repo module boundary (data vs behavior) lines up exactly with the cross-repo ownership boundary (shared vs feedback-private). That alignment is why the descriptor must stay pure data with behavior resolved separately: a descriptor carrying function refs could not be reproduced as frozen contract data across repos, so the split is FORCED by Decision 1, not just a cleanliness preference. (This is the one point where the four interface designs diverged. Two put the reader and resolver as function refs on a single descriptor object for a minimal single-lookup interface; the data/behavior split wins here only because the cross-repo ownership decision demands a data-shaped descriptor. In a single-repo world the minimal one-object design would have been the call.)

The three-layer model that results: (1) a shared data contract (harness enum, config home, shared hooks-file path plus format, skills subdir) as a new published contract doc, the analog of `store-write-contract.md`, reproduced as data in each repo; (2) per-repo descriptor data (Feedback: capture events, capture producer command, capture leaf marker; Enforcement: gate event, gate command builders, the `REGIMEN_HARNESS` value) as repo-private data on each repo's descriptor; (3) per-repo behavior registries (Feedback: a reader port and a resolver port keyed by harness) as repo-private functions, separate from the data descriptor.

DECISION 3, the two feedback-side seams (both clear the two-adapter bar, so both are real seams, not hypothetical). The `TranscriptReader` is one method, `read(content, {complete}) -> RolloutReadResult`, category 1 (pure string to structured, no I/O). `RolloutReadResult` is already harness-agnostic, so the seam is nearly free to cut and the ~650-line Codex fold becomes the Codex adapter in place with no logic change; two real adapters (Codex now, Claude next in A.1); tested by feeding fixture content directly, and the existing Codex reader tests retarget at the port verbatim. The `SessionResolver` is `resolveCurrent(ctx) -> sessionId | null` plus `locate({sessionsDir, sessionId}) -> {path, open} | null`, category 2 (filesystem, tested against a temp dir), with two STRUCTURALLY different adapters: Codex runs stamp then newest-scan then filename-regex, Claude takes the session id directly with no stamp and no scan. The strategy varies, not just the constants, which is exactly why this is a port and not parameterized constants. All four interface designs independently folded the "is this the live/open transcript?" decision INTO the resolver (the `open` flag on `locate`'s result), removing assess's current `newestRolloutName` plus basename coupling; adopt it, so assess forwards `complete: !open` and never learns how openness is decided. The composition root (`assess.ts`) keeps the `readFileSync`, so the seams never do I/O; assess takes a `harness` name plus an optional `sessionId` (resolved by the descriptor when omitted), does one `harnessSupport(harness)` lookup, and the entire fail-closed, anchor-insert, quarantine, and judge tail stays harness-blind and unchanged. The registry is one `harnessSupport(harness) -> {descriptor, reader, resolver} | undefined` composer, mirroring the existing `TRANSLATORS` map and living beside it; the existing translator registry stays as-is (it is on the loader/capture path, already harness-parametric and tested, so do not churn it).

DECISION 4, what NOT to build (seam discipline, one adapter equals a hypothetical seam). No `CaptureStrategy` union and no capture-as-tailer arm: today there is exactly one capture shape (the Codex hook-merge planner), and the Gemini-as-tailer path is only a "maybe" pending the version-pin finding, so capture stays descriptor DATA consumed by the existing planner; if Gemini later forces a tailer, that is a future seam with a real second adapter. Also rejected as one-adapter dressing: a `ConfigHomeResolver` port (the variance is two strings, already descriptor data) and a `CaptureHookPlanner` port (only Codex has a hooks planner today).

CONFIRMED REPO SEQUENCING (user-approved 2026-06-18, feedback then enforcement then hub). (1) Feedback repo FIRST, the deep, load-bearing one: introduce the data descriptor plus the reader and resolver ports plus the `harnessSupport` registry, thread it through `assess.ts`, the install wiring, and session resolution, and lift the Codex code into the Codex adapters in place. Feedback is first because it carries the riskiest surface (the reader and resolver ports, the gating risk this doc calls out) and the fullest descriptor, so proving the shape here makes Enforcement's smaller copy a trim rather than a guess, and Claude A.1 builds directly on it; the shared-data contract doc is drafted ALONGSIDE this work (the shared subset becomes clear as the descriptor takes shape), producer before consumer. (2) Enforcement repo SECOND: introduce its smaller data descriptor reproducing the frozen contract, fold `rm-rf-gate-codex.ts` and `rm-rf-gate.ts` into one parameterized gate that reads `REGIMEN_HARNESS` (the shell gates already do this), and parametrize `planGateHooks`, the gate command builders, and `resolveCodexHome` into `resolveHarnessHome`. (3) Hub THIRD: replace the `--codex-home` forward with `--harness <name>` in the locator, plan, and orchestration, with the hub still importing no instrument internals. There is no hard cross-repo build dependency (option b has no shared module to publish first), so this order is about de-risking, not compilation; each repo's change is independently green and mergeable.

CONFIRMED BRANCH/MERGE CADENCE (user-approved 2026-06-18; carried from the Codex push). A feature branch per repo per step (for example `a0-harness-descriptor` in feedback and enforcement, `a0-harness-forward` in the hub), in the relevant repo; this driver doc stays a living working-tree file, not committed unless asked. TDD over the existing green suite (real red-green-refactor, stub-before-red): this is a refactor, so the existing tests stay green and new tests cover the registry, the two ports, and the unknown-harness error, with the per-unit done-contract being `bun run check` green plus the unit's new tests. The CodeRabbit review gate runs on the EXACT merged commits, re-run after any amend or rebase; squash-merge only on explicit go; never auto-merge or auto-push. Each build unit routes off-thread to one delegated agent with the machine-checkable done-contract, while architect-deep (this) and the merge gate stay in the main thread.

DECISION 5, monorepo-readiness (new, from the user 2026-06-18). The user is leaning toward collapsing the multi-repo layout (the split that put feedback, enforcement, and the OTLP bridge in their own repos with `regimen` as a thin hub) into a single repo where those become subdirectories or workspace packages, because the split has created more complexity than it bought; the OTLP bridge was originally separated to keep it orthogonal to this effort, and that separation no longer feels worth its cost. Recommendation, accepted unless the user overrides: do A.0 NOW in the current multi-repo layout but build it CONSOLIDATION-FRIENDLY, and run the monorepo consolidation as its OWN architect-deep plus ADR effort AFTER the readiness work, not before it. Why after and not first: (a) the readiness work has a real external schedule (the work-VDI forensics this week, the Mac install next week, the four-harness trial), and a full repo restructure is a yak-shave that would block it; (b) A.0 is what REVEALS the real shared surface (the descriptor's data subset), so consolidating after A.0 means consolidating around a known shape rather than guessing it, which is producer-before-consumer applied to repo structure; (c) the consolidation is mechanical and low-risk once A.0 isolates the shared surface, so doing it first buys nothing; (d) the monorepo supersedes ADR-0004 (multi-repo by instrument) and touches the install front door, the clone-root resolution, and `bun link`, so it deserves its own deliberate pass rather than a rushed prelude.

The consolidation-friendliness CONSTRAINTS A.0 holds to (so the later monorepo is a delete-and-import, not a rewrite): (1) the shared-data subset lives in ONE clearly-marked module per repo (for example `harness-contract.ts`), authored to be BYTE-IDENTICAL across feedback and enforcement, so consolidation is "move one copy to a shared workspace package, delete the other, repoint two imports," and the hub's contract doc is what keeps the two copies identical until then; (2) the descriptor stays pure DATA (Decision 2), trivially extractable to a shared package, and the data/behavior split survives the monorepo unchanged (a shared data package imported by both, with the reader and resolver behavior staying feedback-private and never imported by enforcement, mono or multi); (3) NO new cross-repo build or release machinery (option c, a published package, stays rejected and is now doubly wrong given the monorepo intent), so there is nothing to unwind; (4) do not deepen the hub's subprocess-only coupling in ways that fight a future in-process model, the hub stays subprocess-only for now (it must, multi-repo) and A.0 does not make a later in-process switch harder. Net: this design is already robust to the structure change, because the only part of A.0 that was coupled to repo structure was the ownership decision, and the data/behavior split makes that decision a one-line import swap later.

RESOLVED SUB-QUESTION (user 2026-06-18): the shared-data contract doc lives in the hub's `docs/`, the analog of `store-write-contract.md` but program-level (ADR-0009 makes the hub the owner of program-level cross-cutting concerns, and the shared hooks-file path and format is precisely what the installer reconciles). Reframed by DECISION 5: the doc is both the shared-surface spec AND the explicit extraction target for the monorepo; once consolidated, its "reproduce identically in both repos" clause collapses into a single shared import and the doc becomes ordinary package documentation.

**A.1 Claude (next harness).** Already has `hooks/capture.ts` (stamps `claude`) and `translateClaude`, plus a reference gate. What it still needs: a Claude transcript reader behind the new port, session-resolution routing, and installer wiring for Claude's config home and skill target. Claude's session id is passed directly (`--session` / `CLAUDE_CODE_SESSION_ID`), so no per-cwd stamp is required. Lowest-lift harness and the user's daily driver, so dogfooding is continuous.

**A.2 Gemini CLI and A.3 Copilot CLI (by trial schedule).** Both are deliberately Claude-Code-shaped, so capture, enforcement, and skill-install are low lift; the reader is the real work (and the only documented unknowns). Verified surfaces:
- **Gemini** (`~/.gemini`, override `GEMINI_CONFIG_DIR`): a full hook system in `settings.json` (Claude-compatible, ships a `--from-claude` migrator), `BeforeTool` deny (or a declarative policy engine), transcripts at `~/.gemini/tmp/<project_hash>/chats/` as JSON, SKILL.md in `~/.gemini/skills/` or the cross-tool `~/.agents/skills/`. Unknown to verify on a real session: the exact per-message transcript JSON field names.
- **Copilot** (`~/.copilot`, override `COPILOT_HOME`): hooks in `~/.copilot/hooks/*.json` (also reads `.claude/settings.json`), a **fail-closed** `preToolUse` command-hook deny (ideal for hard enforcement) plus a `permissionRequest` hook, transcripts at `~/.copilot/session-state/<id>/events.jsonl` plus a `session-store.db` SQLite, SKILL.md in `~/.copilot/skills/`. Set `remoteExport: false` for local-only. Unknown to verify: the `events.jsonl` event-log normalization shape (it is an event log, not a single conversation document, so a thin normalizer is warranted).

**A.4 End-to-end validation per harness.** For each harness: one real session captured, a `feedback assess` verdict via a live judge call, a gate denial landing in the store, and both bundled skills installed. This is the generalized acceptance the Codex push deferred to the Mac, now run once per harness on the real machine(s).

## Workstream B: Depth - the cross-harness evaluation-answering capability (the headline, minimal version)

This is the payoff. It is genuinely new: today's `feedback assess` is one LLM judgment over **one** conversation, written to a judged-signal schema and read back per conversation (every judged read is `WHERE session_id = ?`). Answering a fixed question set across a corpus is a different shape. The schema does not block it (harness is recoverable from `conversations` by join; ADR-0008 deliberately keeps harness and model off the judged row as read-time slice dimensions), so this is an aggregation read path plus a new answering pass, not a migration. Minimal build:

**B.1 Read-time harness/model slice on the judged store.** Join judged signals and narratives back to `conversations.harness` (and scoped events for model) so reads can group and compare by harness. Wiring only; closest existing issue is #23, whose acceptance already says "sliced by model and harness."

**B.2 A corpus-selection primitive.** Choose which conversations feed a given answer: by harness, date window, intent, repo/cwd. Does not exist today; presupposed by #23/#22.

**B.3 The question-to-answer pass (new orchestration, distinct from per-conversation assess).** Feed the company's evaluation questions plus the selected recorded feedback (judged signals, narratives, and the evidence behind them) to the LLM and get back answers grounded and anchored in real sessions. Recommended shape for comparability: answer each question **per harness** (grounded in that harness's corpus) plus a cross-harness summary, matching the PRD's "comparable across harnesses." Record provenance (which runs and sessions fed each answer) so an answer stays falsifiable, the same anchoring discipline the per-conversation judge already uses. Closest issues: #10 (rollup-verdict structure) and #24 (respond-step surfacing), under epic #4.

**B.4 A surface to read the answers.** Minimal first: a CLI command that prints the per-harness answers. The richer human browse CLI (#15) and the time-series bridge (#32) are deferred.

**Dependency:** B is only as good as its corpus. "Across all four harnesses" requires Workstream A capture to exist for those harnesses (regimen-feedback #4 is the gating coverage dependency). Until A.1 to A.3 land, B answers over whatever harnesses are captured (Codex now, Codex plus Claude after A.1).

## The company's evaluation questions (INPUT - to be supplied)

> PLACEHOLDER. The official evaluation questions go here. They are a load-bearing input to Workstream B: their granularity (per-session vs per-harness-aggregate), whether they want qualitative answers or scored ones, and whether they map to existing signals (Intent, Outcome) or need new ones, all shape B.1 to B.3. Paste them in and this section drives the capability's design. Until then, B is built against a small set of representative stand-in questions and re-pointed when the real ones land.

## Deferred / identified (the long arc; not required for the minimal headline)

Surfaced and explicitly held back. Pull in only if the spine above lands with time to spare.

- **Richer signals and grain.** #22 (assignment segmentation, intent and outcome, plus the seven reserved signals beyond Intent and Outcome). Today every conversation is one whole-conversation assignment, so cross-conversation rollups are coarse until this lands. Evaluation questions that lean on correction rate, drift, struggle, or dissatisfaction need this.
- **Fuller rollups.** #23 (cross-conversation rollups by intent, beyond the minimal slice in B.1), #10 (the durable rollup-verdict structure), #24 (recurring-pattern surfacing with concrete suggestions).
- **Cross-harness normalization.** regimen-feedback #10 and #12 (how sub-agent activity and assistant turn-endings/notifications are represented consistently across harnesses), #33 (surface native guardian/permission decisions as harness-neutral Enforcement signals), the ADR-0007 follow-on (per-record shape recognition in each reader so it is not pinned to one build).
- **Self-observation and longitudinal.** #34 (make the agent's self-assessment a first-class signal), #32 (telemetry bridge for judged signals over time, sliced by model/harness/intent), #31 (kit-change timeline so trend shifts tie to interventions).
- **Hardening.** #15 (human-facing `feedback list`/`show`), #6 and #7 (Enforcement done right: the stronger permission-request surface over best-effort pre-tool gates), #30 (other dropped capture properties: command text, prompt text, subagent attribution, tool outcome), regimen-feedback #7 (tool success/failure), plus the known bugs/flakes (#21, #23, #28).

## How this work runs (discipline carried from the Codex push)

- **Route investigation and builds off-thread from the first move.** Scout a seam with a read-only `Explore` agent that returns conclusions and pointers, not file dumps; hand each build unit to one delegated agent with a machine-checkable done-contract (`bun run check` green plus the unit's new tests); keep only the design decision, `architect-deep` for genuinely new boundaries, and the merge gate in the main thread. The `work-router` skill governs this.
- **TDD the code** (the `tdd` skill: real red-green-refactor, stub-before-red). **Producer before consumer** (verify a capture/translator/reader output before building what reads it). **Harness-agnostic by default** (the descriptor is the only place a harness name should live). **No em dashes anywhere.**
- **Cadence:** code lands on a feature branch per harness/step, in the relevant repo; the review gate (CodeRabbit) must pass on the exact merged commits; squash-merge on the user's explicit go. The driver doc (this file) is a living working-tree file: update it in place, do not commit it unless asked.

## Open questions

- The official evaluation questions (the placeholder above). Their shape drives Workstream B.
- The trial schedule across the four harnesses (which is evaluated when), which sets the A.2/A.3 order after Claude.
- Whether the answering pass should answer per-harness-then-compare or produce one cross-harness verdict per question (recommendation: per-harness plus a summary, for comparability).
- Which machine(s) host which harness, and whether any are corporate-locked the way the Codex Mac is. Per-harness end-to-end validation (A.4) depends on this.
- (LARGELY RESOLVED 2026-06-18) Gemini's transcript shape is confirmed on the real work VDI (the `$set`-snapshot `tmp/<alias>/chats/session-*.jsonl`, gemini 0.47.0 with hooks, so Gemini capture there is the hooks path, not a tailer) and Copilot's `events.jsonl` was mapped from a real session in Phase 0; what remains is per-build reconfirmation on each actual trial machine (the ADR-0007 per-record shape discipline), not a documented unknown.

## Pointers

- Historical driver and the proven Codex spine: `plans/codex-trial-readiness.md`.
- Purpose and stories: `PRD.md` (Problem Statement; "Evaluating a new harness or model"). Architecture and the three instruments: `ARCHITECTURE.md`. Vocabulary: `DOMAIN-LANGUAGE.md`. Program plan: `docs/plan.md`. Surfacing principles: `docs/feedback-surfacing.md`.
- ADRs: `docs/adr/0004` (repo boundaries; names `regimen-enforcement`), `0005` (data architecture), `0006` (loader), `0007` (transcript readers and format versioning), `0008` (judged-signal schema; harness/model as read-time slices), `0009` (installer is a hub concern).
- Repos: hub `/home/mlo/dev/niftymonkey/regimen`, `regimen-feedback`, `regimen-enforcement`, `regimen-otlp-bridge`.
- Key seams to generalize: `regimen-feedback/src/loader/rollout/codex-reader.ts` and `src/codex/` (the reader/session port), `src/cli/install/codex-hooks.ts` (capture wiring), `src/judged/digest.ts` (the single-session read path B.1 generalizes), `regimen-enforcement/src/install/` (gate wiring), hub `src/locator.ts` and `src/plan.ts` (the `--codex-home` leak to make `--harness`).
- Issues in play: epic #4; breadth regimen-feedback #4, #10, #12; depth/long-arc #10, #22, #23, #24, #34, #32, #31; hardening #6, #7, #15, #30, #33.
- External harness docs to verify against the installed builds: Gemini CLI hooks/skills/session-management docs (google-gemini/gemini-cli), GitHub Copilot CLI hooks/config/skills reference (docs.github.com Copilot CLI).
