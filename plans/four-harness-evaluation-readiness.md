# Regimen Four-Harness Evaluation Readiness

> The driver for the next push. Two goals: (1) make Regimen usable across all four agent CLIs the company is evaluating, not just Codex, and (2) use Regimen's own Feedback layer to ANSWER the company's evaluation questions from recorded sessions across all four. This document, not the board, decides what gets worked on next. Successor to `plans/codex-trial-readiness.md`, which is now the historical record: the Codex judgment-layer spine it delivered is the proven template this push generalizes.

## Start here (read this first, do not re-derive it)

### Why Regimen exists (the main point; everything below serves this)

An engineer using an AI coding agent every day has a problem that is mostly about their own practice, not the model. When the work goes badly they often cannot say why; when it goes well they cannot reliably repeat what they did. There is no observability for the interaction between the engineer and the agent. **Regimen exists to close that gap.** The one question it is built to answer: *did the agent do what I wanted, or did I have to do a lot of correcting?* The only honest way to answer that is to record how the work actually went and be able to ask.

That answer-seeking is the **Feedback** pillar, and it is why the other two pillars exist in the order they do:

- **Guidance** instructs the agent through skills it is asked to follow. Advisory; the agent may or may not comply. Useful on its own. This is how you make a spotted problem go away once.
- **Enforcement** compels an outcome through any deterministic mechanism that takes the choice away from the model (hooks, permission and tool gating, pre-merge gates, sandboxing, schema-constrained output). Added when a behavior matters too much to leave to the model, for the iffy cases where Guidance alone is not reliable.
- **Feedback** observes how the work actually went and shows where the interaction is strong and weak. Added when you want to know whether Guidance and Enforcement are actually working, and what else needs sharpening. **Feedback is what makes the other two improvable.**

Regimen is **harness-agnostic and model-agnostic by design**: every schema, signal, interface, and config holds for any agent CLI and any model, and harness-specific detail is confined to a thin capture/adapter edge per harness. In the design's own words, adding a harness is meant to be "one new capture hook file plus one new translator." This push is the first real test of that promise across four harnesses at once.

### What changed (2026-06, confirmed 2026-06-17)

The original plan was a solo trial of **one** harness. The user was told they were in the Gemini evaluation, then moved to the Codex evaluation specifically to guarantee a CLI option, which is why the Codex trial-readiness work came first. While the user was on vacation (early June 2026) the company decided they will evaluate **all four** harnesses:

1. **Codex** (OpenAI) - the spine is built and live-proven.
2. **Claude Code** (Anthropic) - the original capture path; chosen as the next harness to bring up (lowest lift, the user's daily driver).
3. **GitHub Copilot CLI** (`copilot`, `@github/copilot`; the current agentic CLI, not the deprecated `gh copilot` suggester).
4. **Gemini CLI** (`gemini`, google-gemini/gemini-cli).

This turns Regimen's latent multi-harness design (the PRD already carries user stories about "trying a new agent CLI or model" and "signals comparable across harnesses," sliceable by model and harness) into the actual job.

### The headline outcome this push is for

The company has already landed on a fixed set of **evaluation questions** every engineer must answer about each harness. The payoff that makes this push worth doing: **feed those questions to an LLM that answers them grounded in the feedback Regimen recorded across all four harnesses.** That is the core "did this harness serve me?" question asked at evaluation scale, across weeks and across four CLIs, producing the deliverable the company actually needs instead of a from-memory guess at the end of the trial.

The user's stated bare minimum for this push: understand and execute the next steps to make the three-pillar installer work for all four harnesses, and stand up a minimal version of that cross-harness answering capability.

### What already exists (the template; reuse, do not rebuild)

- **The Codex judgment-layer spine is complete and live-proven** (S1 schema/rubric, S2 content reader, S3 `feedback assess`, S4 in-session skill). A real `feedback assess` over a real Codex conversation returned an accurate, evidence-anchored verdict. This is the template every other harness follows.
- **The hub + installer restructuring (ADR-0009) is done**: `regimen install` is a one-command three-pillar front door; `regimen-enforcement` is its own repo; `feedback install` is Feedback-only.
- **The store, envelope, and translator-dispatch spine is already harness-parametric.** The `Harness` enum already lists `claude`, `codex`, `gemini`, `copilot` (and more); the buffer, validation, and store accept all four today.
- **Every captured conversation already records its harness** (`conversations.harness`, `events.harness`). The data needed to slice and compare by harness already lands; nothing downstream reads it yet.
- **Claude already has a translator and a reference gate.** It is the closest harness to ready.

The honest gap carried in from the Codex push: the full install-to-judge loop has still not been exercised end to end on the trial Mac (no machine in hand). That validation (Codex's old "1.12 acceptance") now generalizes to all four harnesses.

### Status and next action (2026-06-17)

Phase 0 and 0b forensics are COMPLETE for all four harnesses: Codex done, Copilot mapped (the cleanest transcript), Gemini mapped (with the version-pin caveat and a tested work-machine `.ps1` at md.niftymonkey.dev `/v/KoX46TJT`), Claude reader still to build. User-side items in flight: run the Gemini forensic script on the work VDI, confirm with the company which Gemini surface is evaluated (gemini CLI vs Antigravity), and the Mac install in Phase 2 (next week). CHOSEN NEXT BUILD (user, 2026-06-17): Workstream A.0, the harness-parametric refactor, starting with an architect-deep design of the harness-descriptor boundary, then a routed TDD build on a feature branch. Repo sequencing and branch/merge cadence to be confirmed before any build or merge.

## Phase 0: install all four locally and read what they actually write (do this first)

Before building a reader or installer for any new harness, install all four on this WSL2 dev box and look at what each one actually creates on disk, the same on-disk forensics that grounded the Codex push (its Phase 0 read the real `~/.codex` topology rather than trusting docs). Claude and Codex are already installed here; this adds Gemini CLI and Copilot CLI, so it is two installs plus one real session each. Per harness, capture the verified truth and fold it into the harness descriptor: the config home and its env override, the hooks-config file and format, a real hook payload shape on stdin, the transcript path and on-disk format (and the session id), the skill/context install target, and the pre-tool deny mechanism. This is what turns the documented surfaces (which already look low-lift and Claude-Code-shaped) into facts, and it directly closes the two unknowns the doc research could not settle: Gemini's exact transcript JSON field names and Copilot's `events.jsonl` event-log shape. The WSL2 box is for mechanics only; the corporate trial machine(s) may differ, exactly as this dev box differed from the Codex Mac, so treat anything machine-specific as reconfirmed-on-the-real-machine.

STATUS (2026-06-17): both new CLIs installed on this WSL2 box, `gemini` 0.47.0 and `copilot` 1.0.63, under the nvm Node v22.18.0 PATH (same default-node caveat as the Codex CLI: missing from a fresh default-node shell). Forensic surprise worth carrying to the trial machine: both config homes already existed BEFORE the CLI install, `~/.gemini` from Google Antigravity (it has an `antigravity/` dir and is already OAuth-authenticated) and `~/.copilot` from the VS Code Copilot integration (an `ide/` dir), so on this box the home is shared across surfaces the same way `~/.codex` is shared by CLI and app on the Mac. The cross-tool `~/.agents/skills/` dir already exists and is populated (26 skills), which is directly the skill-install seam's target. Read-only forensic sweep of the real on-disk topology (and any existing transcripts) routed off-thread.

PHASE 0 FINDINGS (2026-06-17, read-only forensics; secrets and message content redacted):
- GEMINI transcript format is CAPTURED from 3 existing Antigravity sessions at `~/.gemini/tmp/<project-alias>/chats/session-*.jsonl`. Important nuance the docs did not surface: the file is an EVENT-SOURCED state-patch JSONL, not a plain append of complete messages. Line 0 is a session-init object `{sessionId, projectHash, startTime, lastUpdated, kind}`; later lines are EITHER a message object OR a `$set` mutation (for example `{"$set": {"messages": [...], "lastUpdated": ...}}`). A message is `{id, timestamp, type: "user"|"gemini", content: [{text}], and on gemini turns thoughts[], tokens{}, model}`. So the Gemini reader must collapse the `$set` patches (or extract message objects directly) rather than treat each line as one event, and must exclude `thoughts[]` (reasoning) from judge content the way the Codex reader excludes chain-of-thought. Model observed: `gemini-3-flash-preview`. CONFIRMED 2026-06-17 by a headless `gemini --skip-trust -p` probe: the standalone CLI writes the IDENTICAL `$set` structure under `~/.gemini/tmp/<cwd-alias>/chats/`, and one trivial turn produced a 101K file (each `$set` re-embeds the full `messages` array, so the reader takes the LATEST snapshot, never folds line-by-line). Headless runs require `--skip-trust` or `GEMINI_CLI_TRUST_WORKSPACE=true` (a trust gate worth noting for any automated capture). TOOL REPRESENTATION RESOLVED 2026-06-17 from a real ~10-tool-call agentic session: records append as top-level lines with a `type` discriminator (`user`, `gemini`, and a new `info` type seen), interleaved with `$set` patches (mostly `lastUpdated`, occasionally a `messages` snapshot), so the reader must handle BOTH append-style message lines AND `$set` mutations. `content` is POLYMORPHIC: a plain string (most `gemini` turns here) OR an array of parts. Tool RESULTS appear as `type:"user"` messages with `content:[{functionResponse:{id,name,response}}]`, and one user message can BATCH several (10 messages carried 13 results, so parallel calls group), so tool name and output are recoverable. NOTABLE GAP: the structured tool CALL (a `functionCall` part with args) was NOT persisted in this chat log, only the model's prose plus the `functionResponse` round-trip, so tool-call ARGUMENTS are not derivable from the transcript alone (the hook/`BeforeTool` path carries args, an argument that Gemini capture needs the hook for full tool fidelity, with the transcript reader as the reduced-fidelity judge-time fallback). The earlier 101K-for-one-turn was injected context plus large `response` payloads, NOT snapshot duplication. CAPTURE-EDGE (hook firing) PARTIALLY PROBED 2026-06-17: hooks ARE a first-class feature on v0.47 (a `gemini hooks` command exists, including `gemini hooks migrate` from Claude Code), and enforcement has dedicated surfaces (`--approval-mode default|auto_edit|yolo|plan`, plus `--policy`/`--admin-policy` files, the Gemini enforcement seam). A headless `SessionStart` probe via an isolated `GEMINI_CONFIG_DIR` did NOT fire the hook, but that is INCONCLUSIVE: `GEMINI_CONFIG_DIR` appears NOT honored on v0.47 (no `tmp/` was written into the isolated dir, so gemini fell back to the real `~/.gemini`, which has no hooks, and never loaded the probe hook). SCHEMA NOW CONFIRMED from the docs and example bundled IN the installed package (`bundle/docs/hooks/reference.md`, `bundle/examples/hooks/`): hooks live in `settings.json` under a `hooks` object in the NESTED form `hooks.<Event> = [ { matcher?, sequential?, hooks: [ { type: "command", command, name?, timeout? } ] } ]`, NOT the flat `[{matcher,type,command}]` my first probe used (which is a second reason it never fired, on top of the config-dir issue). The v0.47 event taxonomy is `SessionStart`, `SessionEnd`, `BeforeAgent`, `AfterAgent`, `BeforeModel`, `AfterModel`, `BeforeToolSelection`, `BeforeTool`, `AfterTool`, `PreCompress`, `Notification`, `Stop` (Gemini-native names, not Claude's `Pre/PostToolUse`). The stdin payload is `{session_id, transcript_path, cwd, hook_event_name, timestamp}` plus `tool_name`/`tool_input` on `BeforeTool` and `tool_response` on `AfterTool`, essentially the SAME shape as Codex and Claude, so the Gemini capture hook plus translator is a near-trivial port. `BeforeTool.tool_input` carries the tool-call ARGS that the transcript drops, confirming hooks are the fidelity path for Gemini tool detail. ENFORCEMENT seam confirmed: a `BeforeTool` hook returns `{"decision":"deny","reason":...}` or exits 2 (stderr as the reason), alongside the `--approval-mode` and `--policy`/`--admin-policy` surfaces. STILL OPEN, the only remaining Phase 0 item: confirm a configured hook actually FIRES on this install. An automated re-run was denied, so this is a HITL step, a user-run session with the corrected nested-schema hook in a scratch project `.gemini/settings.json`. Do NOT mutate the user's authenticated `~/.gemini/settings.json`. UPDATE 2026-06-17: a user-run `gemini` session was tried but launched from the repo cwd with normal config, so the scratch hook was out of scope and `/tmp/gemini-hook-test.log` was never written; firing is still unconfirmed. This item may also RELOCATE, see the harness-target risk below: if the Gemini target becomes Antigravity CLI, the hook-firing check moves to Antigravity's surface. SCOPE/TRUST RESOLVED from the bundled `docs/hooks/index.md`: project `.gemini/settings.json` hooks ARE supported (precedence project then user then system then extensions), but Gemini FINGERPRINTS project hooks and treats a new or changed one as UNTRUSTED, warning before it executes, so a fresh scratch-project hook does NOT fire silently in a headless `-p` run (that, plus the scratch `settings.json` not actually being present on the manual attempt, is why no marker was ever written). The trusted install target is USER scope `~/.gemini/settings.json` (the analog of installing into `~/.codex`); hooks are also managed via `/hooks panel` and `/hooks enable <name>`, and run with a sanitized env exposing `GEMINI_SESSION_ID`, `GEMINI_CWD`, `GEMINI_PROJECT_DIR` (plus a `CLAUDE_PROJECT_DIR` compatibility alias). CONCLUSION (stop iterating): the hook MECHANISM is fully confirmed from the in-package docs (schema, all 11 events, payload, enforcement, trust model, management commands), so empirical fire-on-THIS-install is DEFERRED to install time on the actual Gemini target rather than chased further, since the install path is user-scope and the Gemini target itself is unsettled pending the June 18 sunset decision.
- SKILL-INSTALL SEAM SIMPLIFICATION: `~/.agents/skills/` is the real cross-tool skills dir (24 skills), and `~/.gemini/skills/`, `~/.copilot/skills/`, and `~/.gemini/antigravity/global_skills/` are all SYMLINKS into it. So the harness-agnostic skill installer can target `~/.agents/skills/` once and be picked up by Gemini, Copilot, and Antigravity through the existing convention, instead of copying per harness. Confirm Claude and Codex honor the same `~/.agents/skills/` path before treating it as universal. PRECEDENCE confirmed 2026-06-17: when a skill exists in both `~/.agents/skills/` and `~/.gemini/skills/`, Gemini loads the `~/.agents/skills/` copy and logs a `Skill conflict detected` warning, so `~/.agents/skills/` is the canonical write target and a dual-install only adds noise.
- COPILOT transcript is CAPTURED and is the CLEANEST of the four (verified 2026-06-17 from a real `client_name: github/cli` session). `~/.copilot/session-state/<session-id>/events.jsonl` is plain one-event-per-line JSONL with a uniform envelope `{type, id, timestamp, parentId, data}` and a rich, judge-time-complete taxonomy: `session.start` (carries cwd/gitRoot/branch/commit), `user.message`, `assistant.message` (model, `toolRequests[]`, encrypted `reasoningOpaque`, tokens), `assistant.turn_start`/`turn_end`, `tool.execution_start`/`tool.execution_complete` (paired by `toolCallId`, with a `success` boolean and `result`), `permission.requested`/`permission.completed` (an `approved`/`denied`/`ask_later` audit trail), `hook.start`/`hook.end`, `system.message`, `session.model_change`, and `session.shutdown` (token and cost metrics plus `codeChanges.{linesAdded,linesRemoved,filesModified}`). Implications: (a) the Copilot reader is LOW risk, a straight line-by-line fold, no `$set` collapse and no rollout-vs-event dedup; (b) reasoning is ENCRYPTED at rest (`reasoningOpaque`), so chain-of-thought is auto-excluded from judge content with no special handling; (c) ENFORCEMENT is first-class in the transcript, the `permission.*` events are a ready-made deny signal and `permissions-config.json` is the path-scoped and command-identifier allow-deny config (with fail-closed `preToolUse`/`permissionRequest` hooks per the docs); (d) `~/.copilot/session-store.db` (sessions, turns, checkpoints, trajectory) is a convenience index, NOT the source of truth, treat `events.jsonl` as canonical, the same call made for Codex's `state_5` sqlite. Auth rides the `gh` GitHub credentials (logged in as `niftymonkey`). Config home `~/.copilot` (override `COPILOT_HOME`); custom hooks (none configured yet) go under `~/.copilot/hooks/` or a `settings.json`. Skills symlink into `~/.agents/skills/` like Gemini's.
- SYNTHESIS: the transcript-reader risk is UNEVEN, not uniform. Copilot is the easiest (clean event log), Gemini is the trickier of the two new ones (the `$set` snapshot fold), Codex is done, and Claude still needs its reader built. The capture, enforcement, and skill-install edges are confirmed present and Claude-Code-shaped on both new harnesses. The one unproven capture item is whether a configured hook actually FIRES on this Gemini build (the next non-invasive Phase 0 probe).
- HARNESS-TARGET RISK (surfaced 2026-06-17 from a live `gemini` startup banner, TIME-SENSITIVE): the `gemini` CLI now prints "Gemini CLI will stop serving requests to Google One and unpaid tiers in 1 day on June 18. Please migrate to Antigravity CLI" (the dev box's plan is "Gemini Code Assist for individuals", an affected tier), and Google steers users to ANTIGRAVITY CLI (`curl -fsSL https://antigravity.google/cli/install.sh | bash`). This reframes the "Gemini" harness: the trial target may be Antigravity CLI (or a paid Gemini Code Assist / Vertex tier), not the free `gemini` CLI, which may stop serving on this box 2026-06-18. MUCH OF THE GEMINI PHASE 0 TRANSFERS regardless: Antigravity already shares `~/.gemini` (the `antigravity/` dir and the original transcript samples predate the `gemini` CLI install), so the `$set` transcript reader and the `~/.agents/skills` seam carry over; what needs ITS OWN verification is Antigravity CLI's hooks and enforcement surface and whether it writes the same `tmp/<project>/chats` transcripts. OPEN QUESTION for the user: which Gemini surface is the company actually evaluating? That decides the Gemini integration target, and it is time-sensitive given the June 18 sunset. DECISION (2026-06-17, user): for THIS iteration the Gemini target is the regular `gemini` CLI (the user is fairly sure that is what the company runs); Antigravity is DEFERRED to a future iteration and held as the LOWEST-priority open question until the user inspects the work install tomorrow. The user also notes the company may be pinned to an OLDER gemini version for VPN compatibility and may have to stay on it, so Antigravity adoption is uncertain even after the sunset. See the work-machine forensic checklist and version-pin constraint in Phase 0b below.

## Phase 0b: work-machine Gemini forensics (run-and-paste, for the actual trial install)

CONSTRAINT (2026-06-17, user): the company likely PINS AN OLDER `gemini` CLI version that works inside its VPN, and may have to stay on it, so the dev box (v0.47.0) may be AHEAD of the trial machine, the inverse of the Codex dev-box-behind-Mac situation. The Gemini integration must target the TRIAL build's actual capabilities: an older build could write a different `tmp/<project>/chats` shape or lack hooks entirely, in which case Gemini capture falls back to a transcript TAILER (the Codex-rollout-tailer analog) instead of hooks. The June 18 Antigravity sunset may not even apply under the VPN and version pin, so treat it as a WATCH item, not a forcing function for this iteration. PLATFORM NOTE (2026-06-17, user): the work machine being probed THIS week is a WINDOWS VDI driven via PowerShell (not bash), and the actual Mac trial install is PHASE 2 (next week), so this week is Windows-VDI recon. The run-and-paste checklist therefore exists in BOTH PowerShell (for the VDI now) and bash (for the Mac), hosted at md.niftymonkey.dev `/v/KoX46TJT`. Windows is thus a real Gemini topology to support (`%USERPROFILE%\.gemini`, Windows paths, no POSIX shell), consistent with Regimen's native-Windows-first-class goal; the bash block below is the Mac/Linux variant. EMPIRICAL VALIDATION (2026-06-17, the forensic `.ps1` was run from WSL2 against the Windows host's PowerShell 5.1 AND 7.5, both pass): three findings. (1) The Windows host runs a far OLDER gemini, `0.1.12` (WSL2 has `0.47.0`), and `0.1.12` has NO `hooks` subcommand (`gemini hooks` just reprints general help), proving live that an older pinned Gemini has no hook capture seam, so capture there must use the transcript tailer, not hooks. (2) The Windows gemini is a `.ps1` shim (`C:\nvm4w\nodejs\gemini.ps1`), so PowerShell 5.1 BLOCKS it under the default Restricted ExecutionPolicy (`SecurityError`); it runs only via `-ExecutionPolicy Bypass` or under PowerShell 7, so the forensic script is launched `powershell -ExecutionPolicy Bypass -File ...`. (3) The Windows host has its OWN `~/.gemini` (dual-home with WSL2, the Codex pattern again), and the 0.1.12 help exposes `--telemetry-otlp-endpoint`, a built-in OTLP path worth noting for the bridge. The validated `.ps1` is hosted at `/v/KoX46TJT`.

The dev box cannot tell us what the corporate machine runs. Run these READ-ONLY commands on the work machine and paste the output back (none print secrets; tier and plan come from the startup banner). The `2>&1` on the help and hooks lines is intentional: on an older build the error itself is the signal (the subcommand may not exist).

```
# 1. Version and surface (regular gemini CLI vs Antigravity, and which build)
gemini --version
ls -la ~/.gemini/antigravity

# 2. Capability probe: does THIS build support hooks and enforcement
gemini --help 2>&1 | grep -niE 'hook|approval|policy|yolo|config|home|trust|dir'
gemini hooks --help 2>&1 | head -20

# 3. Config home, overrides, and layout
echo "GEMINI_CONFIG_DIR=$GEMINI_CONFIG_DIR GEMINI_CLI_HOME=$GEMINI_CLI_HOME"
ls -la ~/.gemini

# 4. Transcript location and structure-only shape of the newest session (no content printed)
ls -lat ~/.gemini/tmp/*/chats/ | head
F=$(ls -t ~/.gemini/tmp/*/chats/session-*.jsonl | head -1); echo "$F"; jq -c 'if has("$set") then {set_keys:(.["$set"]|keys)} elif has("type") then {type, content_kind:(.content|type)} else keys end' "$F" | sort | uniq -c

# 5. Skills wiring and the bundled hook docs (confirms the feature exists on this build)
ls -la ~/.gemini/skills ~/.agents/skills
ls "$(npm root -g)/@google/gemini-cli/bundle/docs/hooks" "$(npm root -g)/@google/gemini-cli/bundle/examples/hooks"
```

Also paste the `gemini` STARTUP BANNER (the plan/tier line and any deprecation or migration notice): it tells us the tier and whether the sunset applies to your setup. What each block answers: (1) which Gemini and which version, (2) whether the capture and Enforcement seams (hooks, approval/policy) exist on that build, (3) the config home to install into, (4) whether the transcript reader we verified on v0.47 matches the trial build, (5) the skill-install target and feature presence. If command 4 prints nothing or errors, that build may not persist `tmp/chats` transcripts, which itself decides capture strategy.

## Workstream A: Breadth - the three-pillar installer and capture/enforce edges for all four harnesses

Goal: `regimen install --harness <name>` stands up Feedback (capture + judge), Guidance (skills), and Enforcement (gates) for any of the four. Most of this is mechanical given the existing Codex implementation, with one genuinely deep exception called out below.

**The one hard part (the load-bearing risk): the judge-time transcript reader.** Capture wiring, translation, install, enforcement, and skill install are all shallow path/name/event swaps once a harness descriptor exists. The transcript reader is not: each harness stores sessions in a different on-disk format, and `src/loader/rollout/codex-reader.ts` is a hardwired fold over Codex rollout JSONL with no abstraction, imported by name in `src/judged/assess.ts`. Every harness past Codex needs a net-new reader that produces the same v1 events plus judge-time content chunks from a different format, behind an interface that does not exist yet. This is real format-comprehension work per harness and it gates the judge for that harness, so it dominates the effort. Treat it as the first-class deliverable of each harness bring-up, not an afterthought.

**A.0 Harness-parametric refactor (do first; unblocks every harness).** Introduce a single harness descriptor/registry that maps each harness to: config home (env var plus default dir), hooks-config file path and format, the capture event-name set, the capture producer path, the skill target convention, and the session-resolution strategy. Then thread it through the six Codex-named seams so they take a harness rather than hardcoding it:
1. `resolveCodexHome` becomes `resolveHarnessHome(harness, ...)` in both feedback and enforcement; replace `--codex-home` with `--harness <name>` (home becomes derivable, with an explicit override).
2. `planCodexHooks` becomes `planCaptureHooks(harness, ...)`; `planGateHooks` takes a harness so events, producer command, and target file come from the descriptor.
3. Extract a `TranscriptReader` and `SessionResolver` port; make the Codex reader plus `src/codex/*` the Codex implementation behind it; change `src/judged/assess.ts` to resolve the reader/locator by harness instead of importing Codex by name. Generalize the `dataDir/codex/sessions` stamp path to `dataDir/<harness>/sessions`.
4. Gate command builders set `REGIMEN_HARNESS` from the descriptor, not the literal `codex`; fold the `rm-rf-gate-codex.ts` / `rm-rf-gate.ts` split into one parameterized gate.
5. `feedback install` and `installSkill` route through the descriptor; the hub forwards `--harness` instead of `--codex-home`.

This is a refactor of working, tested code, so it is TDD over an existing green suite, not a redesign. Use `architect-deep` only for the descriptor boundary itself.

OPEN DESIGN QUESTION (the FIRST thing the architect-deep pass must resolve, before any build): WHERE does the harness descriptor/registry live, and how do the repos consume it? Options: (a) a HUB-OWNED shared module that `regimen-feedback` and `regimen-enforcement` both import (one canonical source, but a real cross-repo dependency); (b) DUPLICATED per repo (no shared dep, but the capture edge and the enforcement edge can drift); (c) a PUBLISHED shared package (clean isolation, but adds release machinery). The descriptor is the central new artifact of the whole multi-harness effort and it spans all three repos (feedback threads it through capture wiring, the reader, and session resolution; enforcement through gate wiring; the hub forwards `--harness`), so this ownership decision is load-bearing and gates the refactor. Per ADR-0004 (repo boundaries) and ADR-0009 (the hub owns program-level cross-cutting concerns), a hub-owned descriptor is the likely answer, but architect-deep should weigh it against the cross-repo-coupling cost first. The REPO SEQUENCING for the refactor (which repo's change lands first) falls out of this decision and is the explicit first confirmation gate with the user.

**A.1 Claude (next harness).** Already has `hooks/capture.ts` (stamps `claude`) and `translateClaude`, plus a reference gate. What it still needs: a Claude transcript reader behind the new port, session-resolution routing, and installer wiring for Claude's config home and skill target. Claude's session id is passed directly (`--session` / `CLAUDE_CODE_SESSION_ID`), so no per-cwd stamp is required. Lowest-lift harness and the user's daily driver, so dogfooding is continuous.

**A.2 Gemini CLI and A.3 Copilot CLI (by trial schedule).** Both are deliberately Claude-Code-shaped, so capture, enforcement, and skill-install are low lift; the reader is the real work (and the only documented unknowns). Verified surfaces:
- **Gemini** (`~/.gemini`, override `GEMINI_CONFIG_DIR`): a full hook system in `settings.json` (Claude-compatible, ships a `--from-claude` migrator), `BeforeTool` deny (or a declarative policy engine), transcripts at `~/.gemini/tmp/<project_hash>/chats/` as JSON, SKILL.md in `~/.gemini/skills/` or the cross-tool `~/.agents/skills/`. Unknown to verify on a real session: the exact per-message transcript JSON field names.
- **Copilot** (`~/.copilot`, override `COPILOT_HOME`): hooks in `~/.copilot/hooks/*.json` (also reads `.claude/settings.json`), a **fail-closed** `preToolUse` command-hook deny (ideal for hard enforcement) plus a `permissionRequest` hook, transcripts at `~/.copilot/session-state/<id>/events.jsonl` plus a `session-store.db` SQLite, SKILL.md in `~/.copilot/skills/`. Set `remoteExport: false` for local-only. Unknown to verify: the `events.jsonl` event-log normalization shape (it is an event log, not a single conversation document, so a thin normalizer is warranted).

**A.4 End-to-end validation per harness.** For each harness: one real session captured, a `feedback assess` verdict via a live judge call, a gate denial landing in the store, and both bundled skills installed. This is the generalized acceptance the Codex push deferred to the Mac, now run once per harness on the real machine(s).

## Workstream B: Depth - the cross-harness evaluation-answering capability (the headline, minimal version)

This is the payoff. It is genuinely new: today's `feedback assess` is one LLM judgment over **one** conversation, written to a judged-signal schema and read back per conversation (every judged read is `WHERE session_id = ?`). Answering a fixed question set across a corpus is a different shape. The schema does not block it (harness is recoverable from `conversations` by join; ADR-0008 deliberately keeps harness and model off the judged row as read-time slice dimensions), so this is an aggregation read path plus a new answering pass, not a migration. Minimal build:

**B.1 Read-time harness/model slice on the judged store.** Join judged signals and narratives back to `conversations.harness` (and scoped events for model) so reads can group and compare by harness. Wiring only; closest existing issue is #23, whose acceptance already says "sliced by model and harness."

**B.2 A corpus-selection primitive.** Choose which conversations feed a given answer: by harness, date window, intent, repo/cwd. Does not exist today; presupposed by #23/#22.

**B.3 The question-to-answer pass (new orchestration, distinct from per-conversation assess).** Feed the company's evaluation questions plus the selected recorded feedback (judged signals, narratives, and the evidence behind them) to the LLM and get back answers grounded and anchored in real sessions. Recommended shape for comparability: answer each question **per harness** (grounded in that harness's corpus) plus a cross-harness summary, matching the PRD's "comparable across harnesses." Record provenance (which runs and sessions fed each answer) so an answer stays falsifiable, the same anchoring discipline the per-conversation judge already uses. Closest issues: #10 (rollup-verdict structure) and #24 (respond-step surfacing), under epic #4.

**B.4 A surface to read the answers.** Minimal first: a CLI command that prints the per-harness answers. The richer human browse CLI (#15) and the time-series bridge (#32) are deferred.

**Dependency:** B is only as good as its corpus. "Across all four harnesses" requires Workstream A capture to exist for those harnesses (regimen-feedback #4 is the gating coverage dependency). Until A.1 to A.3 land, B answers over whatever harnesses are captured (Codex now, Codex plus Claude after A.1).

## The company's evaluation questions (INPUT - to be supplied)

> PLACEHOLDER. The official evaluation questions go here. They are a load-bearing input to Workstream B: their granularity (per-session vs per-harness-aggregate), whether they want qualitative answers or scored ones, and whether they map to existing signals (Intent, Outcome) or need new ones, all shape B.1 to B.3. Paste them in and this section drives the capability's design. Until then, B is built against a small set of representative stand-in questions and re-pointed when the real ones land.

## Deferred / identified (the long arc; not required for the minimal headline)

Surfaced and explicitly held back. Pull in only if the spine above lands with time to spare.

- **Richer signals and grain.** #22 (assignment segmentation, intent and outcome, plus the seven reserved signals beyond Intent and Outcome). Today every conversation is one whole-conversation assignment, so cross-conversation rollups are coarse until this lands. Evaluation questions that lean on correction rate, drift, struggle, or dissatisfaction need this.
- **Fuller rollups.** #23 (cross-conversation rollups by intent, beyond the minimal slice in B.1), #10 (the durable rollup-verdict structure), #24 (recurring-pattern surfacing with concrete suggestions).
- **Cross-harness normalization.** regimen-feedback #10 and #12 (how sub-agent activity and assistant turn-endings/notifications are represented consistently across harnesses), #33 (surface native guardian/permission decisions as harness-neutral Enforcement signals), the ADR-0007 follow-on (per-record shape recognition in each reader so it is not pinned to one build).
- **Self-observation and longitudinal.** #34 (make the agent's self-assessment a first-class signal), #32 (telemetry bridge for judged signals over time, sliced by model/harness/intent), #31 (kit-change timeline so trend shifts tie to interventions).
- **Hardening.** #15 (human-facing `feedback list`/`show`), #6 and #7 (Enforcement done right: the stronger permission-request surface over best-effort pre-tool gates), #30 (other dropped capture properties: command text, prompt text, subagent attribution, tool outcome), regimen-feedback #7 (tool success/failure), plus the known bugs/flakes (#21, #23, #28).

## How this work runs (discipline carried from the Codex push)

- **Route investigation and builds off-thread from the first move.** Scout a seam with a read-only `Explore` agent that returns conclusions and pointers, not file dumps; hand each build unit to one delegated agent with a machine-checkable done-contract (`bun run check` green plus the unit's new tests); keep only the design decision, `architect-deep` for genuinely new boundaries, and the merge gate in the main thread. The `work-router` skill governs this.
- **TDD the code** (the `tdd` skill: real red-green-refactor, stub-before-red). **Producer before consumer** (verify a capture/translator/reader output before building what reads it). **Harness-agnostic by default** (the descriptor is the only place a harness name should live). **No em dashes anywhere.**
- **Cadence:** code lands on a feature branch per harness/step, in the relevant repo; the review gate (CodeRabbit) must pass on the exact merged commits; squash-merge on the user's explicit go. The driver doc (this file) is a living working-tree file: update it in place, do not commit it unless asked.

## Open questions

- The official evaluation questions (the placeholder above). Their shape drives Workstream B.
- The trial schedule across the four harnesses (which is evaluated when), which sets the A.2/A.3 order after Claude.
- Whether the answering pass should answer per-harness-then-compare or produce one cross-harness verdict per question (recommendation: per-harness plus a summary, for comparability).
- Which machine(s) host which harness, and whether any are corporate-locked the way the Codex Mac is. Per-harness end-to-end validation (A.4) depends on this.
- Gemini's exact transcript JSON field schema and Copilot's `events.jsonl` shape, both to confirm on a real session before trusting their readers.

## Pointers

- Historical driver and the proven Codex spine: `plans/codex-trial-readiness.md`.
- Purpose and stories: `PRD.md` (Problem Statement; "Evaluating a new harness or model"). Architecture and the three instruments: `ARCHITECTURE.md`. Vocabulary: `DOMAIN-LANGUAGE.md`. Program plan: `docs/plan.md`. Surfacing principles: `docs/feedback-surfacing.md`.
- ADRs: `docs/adr/0004` (repo boundaries; names `regimen-enforcement`), `0005` (data architecture), `0006` (loader), `0007` (transcript readers and format versioning), `0008` (judged-signal schema; harness/model as read-time slices), `0009` (installer is a hub concern).
- Repos: hub `/home/mlo/dev/niftymonkey/regimen`, `regimen-feedback`, `regimen-enforcement`, `regimen-otlp-bridge`.
- Key seams to generalize: `regimen-feedback/src/loader/rollout/codex-reader.ts` and `src/codex/` (the reader/session port), `src/cli/install/codex-hooks.ts` (capture wiring), `src/judged/digest.ts` (the single-session read path B.1 generalizes), `regimen-enforcement/src/install/` (gate wiring), hub `src/locator.ts` and `src/plan.ts` (the `--codex-home` leak to make `--harness`).
- Issues in play: epic #4; breadth regimen-feedback #4, #10, #12; depth/long-arc #10, #22, #23, #24, #34, #32, #31; hardening #6, #7, #15, #30, #33.
- External harness docs to verify against the installed builds: Gemini CLI hooks/skills/session-management docs (google-gemini/gemini-cli), GitHub Copilot CLI hooks/config/skills reference (docs.github.com Copilot CLI).
